# FishingExpanded 根因批次：BATCH-039 持久战奖励与鱼竿熟练度曲线

> 活动卡只保存结论和证据链接；长日志、构建输出和反编译材料放在证据目录。
> 顶部当前状态覆盖更新，不在文件末尾追加版本时间线。

<!-- CURRENT-STATE-BEGIN -->
## 续接状态

| 分诊字段 | 值 |
|---|---|
| 当前唯一执行批次 | BATCH-039 |
| 本轮用户问题范围 | ① 小游戏坚持 30 秒仍失败 → 50% 概率获得 +3 钓鱼料理；坚持 60 秒仍失败 → 必得海泡布丁；发放时随机显示 20 条含鱼名+职阶的诙谐文案；② 绿条旁浮动提示统一 2 秒 → 5 秒淡出；③ 30 秒整追加一条“[职阶]的力气达到巅峰”类提示（10 条随机）；④ “钓鱼控制力”改名为“鱼竿熟练度”；⑤ 鱼竿熟练度 α 曲线改为皇冠数分段线性（0→0%、5→2%、10→7%、20→20%、30→35%、61→100%，点间线性） |
| 本批次纳入 Case | FE-039-1 持久战奖励+巅峰提示；FE-039-2 浮动提示 5 秒淡出；FE-039-3 控制力改名；FE-039-4 α 曲线 |
| 共享第一处分歧与所有权链证据 | 同一轮用户确认的功能改版：奖励/巅峰提示共用 BobberBar 实例战斗计时；α 曲线唯一源头 `DifficultyManager.GetAlpha`；浮动提示共用 `FloatingTip.Lifetime`。按一个功能批次实施，每现象独立验收 |
| 排队或暂不纳入 Case | 无 |
| 合并/拆分决定及依据 | 合并为一个功能批次：全部修改处于同一实例生命周期与同一 α 所有权链；不是已部署修复被推翻，无反证计数累计 |
| 本轮准入证据 | 2026-08-10 用户逐项确认：① 30 秒池=海之菜肴/烩鱼汤/龙虾浓汤（不含海泡布丁）；② 60 秒只发海泡布丁（不叠加 30 秒抽奖）；③ 机制适用所有非鱼王鱼；④ α 锚点 0/5/10/20/30/61 → 0%/2%/7%/20%/35%/100%，点位间线性 |
| 现有实现复核 | α 唯一源头 `DifficultyManager.GetAlpha`（皇冠÷61 线性，BATCH-034，调用点=BobberBar 构造快照/史诗失败判定/控制台）；浮动提示 `BobberBarPatches.FloatingTip.Lifetime=2f`（BATCH-038 双通道）；失败单发点 `BobberBar.Update_Postfix`（`FailureRecorded`，BATCH-030）；物品发放先例 `Farmer.caughtFish` Postfix 星之果茶（`addItemByMenuIfNecessary`）；薄控制命令先例 `fish_assist`（会话标志、构造边界消费） |
| 允许修改范围 | `Source\Patches\BobberBarPatches.cs`、`Source\Services\DifficultyManager.cs`、`Source\Services\HUDNotifier.cs`、`Source\Patches\CollectionsPagePatches.cs`（仅注释）、`Source\ModEntry.cs`（命令/日志/自测）；`Source\i18n\zh.json`、`Source\i18n\default.json`；文档：GAME-DESIGN.md、BUG-LEDGER.md、TESTING.md、TESTING-GUIDE.md、MAINTENANCE-INDEX.md、本卡 |
| 冻结 Case/禁止范围 | 难度/星标/经验/品质/数量/尺寸结算所有权；Transpiler 注入点与 IL 栈序；助战概率（保持皇冠/61 线性）；鱼王豁免边界；部署目录 config.json；不启动游戏、不部署、不 `git add -A` |

| 字段 | 值 |
|---|---|
| 批次目标 | 持久战安慰奖励（30/60 秒失败）、30 秒巅峰提示、浮动提示 5 秒淡出、控制力→鱼竿熟练度改名、α 分段线性曲线 |
| 可观测性决定 | 复用现有小游戏开始/失败日志；新增低频单发日志（每实例/每失败各 ≤1 条）：巅峰提示、奖励发放（含耗时、奖励物品、玩家 ID、测试强制标记）；无逐帧输出；`fish_bonus`/`fish_addstars` 输出改称“鱼竿熟练度”；`fish_selftest` 增加 α 锚点与 i18n 键只读自测（纯函数，不写存档） |
| 自动化验收决定 | 复用真实 BobberBar 小游戏路线（`fish_setlevel` 调高难度 + 战斗 30/60 秒后失败）验证计时与 50% 概率；新增薄控制命令 `fish_persisttest <30|60>`（会话标志、构造边界消费、调用生产失败边界发放，不写存档、不加第二个状态所有者，先例 `fish_assist`）作为确定性入口；存档影响=有（奖励进背包）→ 使用统一全量 `Saves` 沙箱；成功判据=日志与背包出现对应奖励且提示排队显示；失败判据=重复发放、奖励错误、标志泄漏、逐帧日志 |
| 当前阶段 | R0/R1/R2 完成，Release Rebuild ✅，待真实游戏验收 |
| 当前工作树 | 未提交（HEAD `c3bc3b4`，仅 BATCH-039 预期文件修改；未跟踪仅 `WIKI.md`；未使用 `git add -A`） |
| 最后构建 | Release Rebuild ✅ 0 警告 0 错误（DLL SHA256 `610760761F7C342194FF4FA47DEABDB1FF90AB838AA07BA2FDACB861F0D37E74`；反编译确认奖励/巅峰提示/5 秒淡出/α 分段曲线已编译进 DLL） |
| 唯一下一步 | 真实游戏验收：`fish_setlevel` 高难度 + 战斗 30/60 秒后失败、`fish_persisttest <30|60>` 确定性入口、`fish_selftest` α 锚点核验；验收通过后询问是否授权部署 |

| Case | 当前状态 | 下一门禁 |
|---|---|---|
| FE-039-1 | R0/R1/R2 完成，构建 ✅ | 真实小游戏验收（30/60 秒失败奖励） |
| FE-039-2 | R0/R1/R2 完成，构建 ✅ | 真实画面验收（5 秒淡出） |
| FE-039-3 | R0/R1/R2 完成，构建 ✅ | 图鉴画面验收（鱼竿熟练度） |
| FE-039-4 | R0/R1/R2 完成，构建 ✅ | `fish_selftest` 锚点核验 |

| 部署事实 | 值 |
|---|---|
| DLL SHA-256 | `610760761F7C342194FF4FA47DEABDB1FF90AB838AA07BA2FDACB861F0D37E74`（未部署；未授权） |
<!-- CURRENT-STATE-END -->

## 基础 Mod 与原生完整调用链

- 当前安装 DLL：`D:\GGGGG\K1515` 安装目录旧产物（BATCH-038 未部署，本轮不核对部署目标，只改源码）。
- Expanded 接管入口：
  - `BobberBar` 构造 Postfix：创建 `InstanceData`（鱼 ID、难度等级、α 快照、鱼饵、战斗计时起点），鱼王提前 return（天然豁免）。
  - `BobberBar.update` Prefix：战斗计时累计、30 秒巅峰提示单发、力竭节点（仅 ≥100）、有效难度写入。
  - `BobberBar.update` Postfix：失败单发边界（`FailureRecorded`）→ 扣等级、失败提示、**BATCH-039 奖励发放**；成功边界。
  - `BobberBar.draw` Postfix：浮动提示绘制（双通道）。
  - `DifficultyManager.GetAlpha`：α 唯一源头（构造快照、史诗失败判定、控制台展示消费）。
  - `CollectionsPage` 描述 Postfix：皇冠加成行（i18n `collections.crownControl`）。
- 原生上游/提交：原生 `BobberBar` 淡出动画期间 `distanceFromCatching<=0` 判定失败；成功在淡出结束时 `FishingRod.pullFishFromWater`；物品入背包走 `Farmer.addItemByMenuIfNecessary`（原生溢出菜单）。
- 第一处分歧：无（功能新增，非修复）。
- 未读取或仍不确定的环节：无（全部修改点已在当前源码逐行复核）。
- 旧版只读参考：BATCH-034（α 线性）、BATCH-038（2 秒浮动提示）卡与 GAME-DESIGN 旧段落仅作变更来源。

## 状态与生命周期

| 状态/事务 | 创建者 | 唯一写入者 | 消费者 | 容量/频率 | 失效条件 | 清理 |
|---|---|---|---|---|---|---|
| `BattleElapsedSeconds`（原 ExhaustionElapsedSeconds） | BobberBar 构造 | `Update_Prefix` 每帧累计（仅未结算） | 巅峰提示/持久战奖励/力竭节点/挑战鱼饵 300s | 每实例 1 个 float | 结算后停止累计 | 实例清理随 ConditionalWeakTable |
| `PeakTipShown` | BobberBar 构造 | `Update_Prefix`（30 秒单发） | 无（仅防重） | 每实例 1 bool | 实例清理 | 同上 |
| `ForcedPerseveranceSeconds` | BobberBar 构造（消费 ModEntry 会话标志） | `Update_Postfix` 奖励判定读取 | 奖励判定 | 每实例 1 float | 实例清理 | 同上 |
| `_forcePerseveranceSeconds` | `fish_persisttest` | 同命令；构造边界消费后清空 | BobberBar 构造 | 会话 1 值 | 下一次小游戏构造 | 消费式清除 |
| 持久战奖励发放 | `Update_Postfix` 失败分支 | 同分支（`FailureRecorded` 防重） | 玩家背包/日志/提示队列 | 每失败 ≤1 次 | 无（单发） | 无 |
| 浮动提示（Action/Other） | 各触发点 | `AddTip` | `draw` Postfix | 每通道 ≤8 | 5 秒生命周期 | AgeTips 移除 |

## R0

- 战斗计时扩为全量实例累计（原仅 ≥100 力竭门内），力竭衰减仍只对 ≥100 生效；新增 30 秒巅峰提示单发（鱼其他提示通道，i18n `hud.peak.1~10`）。
- 失败单发边界新增持久战奖励：≥60 秒必得海泡布丁 (O)265；30~60 秒 50% 随机 +3 料理（(O)228/(O)728/(O)730）；60 秒不叠加；奖励提示 20 条随机（i18n `hud.battleReward.1~20`，FIFO 队列）。
- 浮动提示 `Lifetime` 2f → 5f（双通道统一）。
- `GetAlpha` 改为皇冠数分段线性（0/5/10/20/30/61 → 0/2/7/20/35/100%，点间线性，超 61 钳 100%）；新增纯函数 `GetAlphaFromCrowns` 供自测。
- 改名：i18n `collections.crownControl` 中英文案、代码注释、控制台输出“手感进度”改“鱼竿熟练度”。
- 薄控制命令 `fish_persisttest <30|60>`（构造边界消费，鱼王豁免时丢弃）。
- R1 待删除旧路径：`ExhaustionElapsedSeconds` 名称与“仅 ≥100 累计”注释整体替换；2 秒/1 秒+0.5 秒文档残留清理。

## R1：旧路径退休

| 被替代项 | 删除/截断证据 | 是否仍有调用者 | 保留理由/退出条件 |
|---|---|---|---|
| `ExhaustionElapsedSeconds` 字段名与“仅 ≥100 累计”语义 | 本次 rename 全量替换；力竭逻辑仍用同一计时 | 无（全量改名） | 无 |
| 浮动提示 2 秒生命周期 | `Lifetime` 5f，双通道共用 | 无 | 无 |
| α=皇冠÷61 线性公式 | `GetAlpha` 换分段线性；`GetAlphaFromCrowns` 为唯一纯函数 | `GetAlpha` 仍为唯一入口 | 无 |
| “钓鱼控制力/手感进度”展示措辞 | i18n/注释/控制台输出替换；历史治理卡不改写 | 无（历史材料只读） | 无 |

- 修改前写入者数量：计时 1 个、α 1 个、奖励 0 个。
- 修改后写入者数量：计时 1 个、α 1 个、奖励 1 个（失败单发边界）。
- 运行时代码净增长：奖励判定+发放（约 50 行）+ 巅峰提示（约 20 行）+ 曲线（约 15 行）；理由=新功能需新行为，未复制任何既有所有者；不新增状态写入者。

## R2：场景与反向测试

| 场景 | 预期 | 不能发生 | 静态/运行结果 |
|---|---|---|---|
| 主机单人 30 秒失败 | 50% 概率 +3 料理之一，提示排队显示，日志单条 | 奖励重复、100% 必发、错误物品 | 待构建 |
| 主机单人 60 秒失败 | 必得海泡布丁，不叠加 30 秒抽奖 | 同时拿到料理+布丁 | 待构建 |
| 30 秒整未失败（继续战斗） | 巅峰提示出现一次，战斗继续 | 提示重复、影响结算 | 待构建 |
| 30 秒内失败 | 无奖励无巅峰提示 | 提前发放 | 待构建 |
| 成功路径 | 计时停止、无奖励 | 成功也发奖励 | 待构建 |
| 鱼王 | 无实例数据，天然豁免；测试标志被丢弃 | 鱼王给奖励 | 待构建 |
| 难度 <100 鱼 | 计时照常、30 秒/60 秒奖励照常 | 力竭衰减误触（仍不衰减） | 待构建 |
| 挑战鱼饵 | 计时照常、奖励照常；300 秒时限语义不变 | 挑战鱼饵 5 分钟判定被新计时改变 | 待构建 |
| 本地分屏/联机 | 按实例 Owner 发放，互不串写 | 副屏奖励进主玩家背包 | 待构建 |
| 背包满 | 原生溢出菜单承接 | 物品丢失或重复 | 待构建 |
| 浮动提示 5 秒 | 双通道全部 5 秒淡出 | 部分通道仍 2 秒 | 待构建 |
| α 曲线锚点 | 0/5/10/20/30/61 → 0/2/7/20/35/100%，中点线性 | 旧线性值、越界超 100% | 待构建 |
| 性能最坏情况 | 新增日志每实例 ≤2 条、无逐帧 | 日志刷屏 | 待构建 |
| 已验收相邻回归 | 力竭节点/助战/跳鱼文案/挑战鱼饵/星之果茶不受影响 | 节点时间错位、助战概率变化 | 待构建 |

## 构建、部署与集中测试

- 构建结果：待执行（Release Rebuild）。
- 版本/哈希：待构建记录。
- 部署文件与目标：不部署（未授权）。
- 本次单局路线：`fish_setlevel <id> <高等级>` → 真实小游戏战斗 30/60 秒后失败 → 核对背包/提示/日志；`fish_persisttest <30|60>` 确定性入口；`fish_selftest` 锚点与 i18n 键。
- 日志/截图/存档证据：待真实验收。
- 每个 Case 的实际结果：待标记。

## 收尾与归档

- 已完成：设计确认、现有实现复核、允许范围与门禁记录。
- 当前不确定性：无（本轮为纯新增功能，不涉及已部署修复被推翻）。
- 下一条准确操作：实施代码与 i18n → Release Rebuild → 文档同步 → 静态门禁核验。
- 总账与测试路线是否已覆盖更新：待同步。
- 关闭或被替代后是否可移入 `Governance/Archive-ReadOnly`：验收完成后按治理规则处理。
