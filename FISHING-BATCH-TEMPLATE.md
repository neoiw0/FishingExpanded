# FishingExpanded 根因批次：<稳定批次名>

> 活动表只保存结论和证据链接；长日志、构建输出和反编译材料放在证据目录。
> 顶部当前状态覆盖更新，不在文件末尾追加版本时间线。

<!-- CURRENT-STATE-BEGIN -->
## 续接状态

| 分诊字段 | 值 |
|---|---|
| 当前轮次 | `ROUND-YYYYMMDD-NN` |
| 当前活动类别 | 类别 ID / `无` |
| 整合候选状态 | 收集与分类 / 逐类处理中 / 待统一构建 / 已统一构建 / 已部署待集中测试 / 集中测试中 / 本轮测试完成 |
| 当前权威活动卡 | |
| 本轮用户问题范围 | |
| 本轮纳入 Case | |
| 共享第一处分歧与所有权链证据 | |
| 排队或暂不纳入 Case | |
| 合并/拆分决定及依据 | |
| 本轮准入证据 | |
| 现有实现复核 | |
| 允许修改范围 | |
| 冻结 Case/禁止范围 | |

| 字段 | 值 |
|---|---|
| 当前类别目标 | |
| 可观测性决定 | 复用标签 / 新增标签 / 免除理由；触发条件、限频方式、成功与失败判据、关闭条件 |
| 自动化验收决定 | 场景；存档影响；触发入口；成功与失败判据；关闭条件；免除理由（仅静态且不改变运行行为时） |
| 当前类别阶段 | 粗分 / 取证 / 设计 / R0 / R1 / R2 / 代码侧结束 / 无活动类别 |
| 当前工作树 | 干净 / 完整修改 / 部分修改 / 未知 |
| 本轮统一构建 | 未执行 / DLL、PDB、manifest 哈希 |
| 唯一下一步 | 类别处理中只写当前类别动作；类别队列全部结束后才允许统一 Build、Deploy 或集中测试 |
| 已消费动作 | 无 / `Build:<轮次>:<DLL SHA-256>`；`Deploy:<轮次>:<DLL SHA-256>`；`Runtime:<轮次>:<场景>:<DLL SHA-256>` |
| 重复执行授权 | 无 / `用户明确要求；原因=<具体原因>` / `自动授权重做；原因=<具体原因>；证据=<证据位置或哈希>`（自授权仅限有具体可核验理由的重做） |
| 本批可委派任务/委派记录 | 可委派任务清单；`Delegated` 任务 ID 或 `NotBeneficial` 对比证据；并行批次（≥2 个任务时） |

<!-- ROUND-CATEGORY-QUEUE-BEGIN -->
| 类别 ID | 包含 Case | 当前状态 | 结束路径 | 侦测代码/日志 | SMAPI 命令或稳定 UI 入口 |
|---|---|---|---|---|---|
| CAT-01 | `ISSUE-01` | 待分析 / 分析中 / 实施中 / 侦测实施中 / 代码侧结束 | 根因修复 / 诊断候选 | 标签、字段、限频、清理 | 触发、顺序、成功、失败 |
<!-- ROUND-CATEGORY-QUEUE-END -->

| Case | 当前状态 | 下一门禁 |
|---|---|---|
| ISSUE-01 | 根因未证实 / 取证中 / 等待玩家日志 / 设计完成 / R0完成 / R1完成 / R2完成 / 已构建 / 已部署 / 等待真实验收 / 部分验收 / 已验收 | |

> 机制断言：纯静态/无行为修改可填 `无（理由）`。

| 集中测试顺序 | 类别/场景 | 命令或 UI 路线 | 独立判据 | 机制断言 |
|---:|---|---|---|---|
| 1 | CAT-01 / `ISSUE-01` | | 通过 / 失败 / 未执行 | |

| 部署事实 | 值 |
|---|---|
| DLL SHA-256 | |
<!-- CURRENT-STATE-END -->

## 现象索引与持续计数

| Case | 玩家可见现象 | 历次已部署修复数 | 最近反证 | 根因组 | 状态 | 最短验收 |
|---|---|---:|---|---|---|---|
| ISSUE-01 | | 0 | | RC-01 | 已报告 | |

反证累计 ≥2 的 Case 停止行为补丁，先完成诊断取证、所有权审计和方案设计（高级复核门禁）并绑定新证据后再实施；第三次标记 `architecture-review`。

## RC-01 根因卡

- 根因状态：未证实 / 等待玩家日志 / 已证实
- 第一处分歧：
- 决定性证据：
- 竞争解释表（至少 2 个互斥假设，其中至少 1 个来自 Fishing 源码之外（外部 Mod/原生行为/存档数据），除非写明排除理由；无法区分时根因状态必须为“未证实”，只允许诊断候选）。静态契约可完整证明且不改变运行行为，或外部解释已明确排除（附排除证据）时，可填豁免：`竞争解释豁免（理由=...）`；豁免理由需说明为何不存在其他互斥假设（如检查过的外部 Mod/原生路径、调用链唯一性、无运行行为依据）。

| 假设 | 预测可观察量 | 排除证据/判据 | 状态（仍成立/已排除） |
|---|---|---|---|
| | | | |
| | | | |
- 最后已知正常版本/行为：
- 过去失败方案及为何失败：
- 唯一所有者：
- 最短区分动作：
- 诊断标签/触发窗口：
- 诊断是否只读且不改变行为：
- 诊断构建/部署与哈希：
- 等待玩家返回的日志及位置：
- 日志返回后的成功/失败判据：
- 反证记录（原根因/反证证据/新增竞争假设；无反证填`无`）：

## RC-01 多人/分屏影响矩阵

> 运行器无联机/分屏能力时，本矩阵是多人验证的替代证据；无影响必须写“无（理由）”。

| 写入路径/行为 | 主机/客户端/副屏 | 权威写入者 | 实例/进程静态 | 消息/广播/同步 | 断线/换日/标题清理 | 自动化覆盖 |
|---|---|---|---|---|---|---|
| | | | | | | |

## 状态与生命周期

| 状态/事务 | 创建者 | 唯一写入者 | 消费者 | 容量/频率 | 失效条件 | 换日/标题/分屏/远程清理 |
|---|---|---|---|---|---|---|
| | | | | | | |

## 原生完整调用链

- 当前安装 DLL 版本/哈希：
- Expanded 接管入口：
- 原生上游入口和状态字段：
- 原生提交方法：
- 后续回调、同步和生命周期：
- Harmony 拦截点：
- 第一处分歧：
- 尚未读取或仍不确定的环节：
- 旧版只读参考及可接受差异：

## R0

- 修改第一处错误决策：
- 新权威入口：
- R1 待删除旧路径：

## R1：旧路径退休

| 被替代项 | 删除/截断证据 | 是否仍有调用者 | 保留理由/退出条件 |
|---|---|---|---|
| 旧字段/方法/消息/缓存/注册/清理 | | | |

- 修改前写入者数量：
- 修改后写入者数量：
- 运行时代码新增/删除：
- 净增长理由：

## R2：场景与反向测试

| 场景 | 预期 | 不能发生 | 静态/运行结果 |
|---|---|---|---|
| 主机单人 | | | |
| 本地主屏/副屏 | | | |
| 远程与四人混合 | | | |
| 普通鱼/非鱼类/鱼王 | | | |
| 数量、动画与 ItemGrabMenu | | | |
| 难度结算/HUD/图鉴/手持展示 | | | |
| 换日/重载/标题/断线 | | | |
| 性能最坏情况 | | | |
| 已验收相邻回归 | | | |

## 构建、部署与集中测试

- 构建结果：
- 版本/哈希：
- 部署文件与目标：
- 本次单局路线：
- 日志/截图/存档证据：
- 每个 Case 的实际结果：

## 收尾与归档

- 已完成：
- 当前不确定性：
- 下一条准确操作：
- 总账与测试路线是否已覆盖更新：
- 关闭或被替代后是否可移入 `Governance/Archive-ReadOnly`：
