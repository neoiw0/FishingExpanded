# FishingExpanded — 游戏设计文档

本文件记录用户确认的钓鱼扩展系统完整设计方案，作为实现的唯一权威来源。

## 核心机制

### 1. 难度等级系统

#### 1.1 统计规则
- **统计粒度**：每种鱼独立统计（按存档+玩家，多人联机不共享）
- **多人部署**：在线联机时主机和每一位农场客都必须安装本 Mod；每位玩家只读取和写入自己的 `Farmer.modData`。
- **品质合并**：不同品质（普通/银/金/铱）算作同一种鱼
- **等级变化**：
  - 成功钓起：根据脱杆次数加等级
    - 0次脱杆（完美）：+10
    - 1次脱杆：+5
    - 2次脱杆：+2
    - 3次及以上：+1
    - 先按上述数值计算本次收益，再应用称号区间边界；收益不能使等级跨越当前区间，但“完美钓鱼 +10”仍是本次计算的原始收益。
  - 钓鱼失败：等级 -1
- **越级限制**：
  - 一次加等级时，最多只能晋升到下一个称号区间的上限
  - 不允许一次跨越多个称号
  - 示例：
    - 当前等级1（精英）+ 完美钓鱼（+10）→ 最多升到6（骑士上限），不能直接到9（伯爵）
    - 当前等级7（领主）+ 完美钓鱼（+10）→ 最多升到15（伯爵上限），不能直接到17（大公）
  - 称号区间上限表：
    - 弱小(<1) → 精英上限(3)
    - 精英(1-3) → 骑士上限(6)
    - 骑士(4-6) → 领主上限(8)
    - 领主(7-8) → 伯爵上限(15)
    - 伯爵(9-15) → 大公上限(22)
    - 大公(16-22) → 亲王上限(33)
    - 亲王(23-33) → 帝王上限(45)
    - 帝王(34-45) → 神皇上限(66)
    - 神皇(46-66) → 神王上限(88)
    - 神王(67-88) → 神王上限(100)
    
- **等级范围**：
  - 普通鱼类：[-10, 100]
  - 非鱼类（垃圾/藻类等）：[-10, 8]，用户确认的上限为8
  - 鱼王类（传奇鱼）：不使用本系统规则
- **封顶提示**：
  - 普通鱼达到100级：显示特殊提示"你已经成为{鱼名}中的神明，这一刻你是鱼，也是人，更是王。"
  - 非鱼类达到8级：显示"对于{鱼名}而言，你已是帝王"
- **触底提示**：
  - 难度-10时再失败：显示"最平庸的{鱼名}依然太难了，请升级鱼竿，钓鱼等级，使用料理增加钓鱼等级，使用陷阱/浮木渔具等"

#### 1.2 难度等级效果

##### 负数区间 [-10, 0]（降低难度）
线性插值，所有效果在-10和0之间连续渐变：

**Difficulty 数值调整**：
- -10：difficulty × 0.5
- 0：difficulty × 1.0

**蓄力槽保护**（鱼逃跑时减速）：
| 蓄力槽阈值 | -10时减速倍率 | 0时减速倍率 |
|---|---|---|
| ≤40% | 0.99 | 1.0 |
| ≤20% | 0.60 | 1.0 |
| ≤1% | 0.20 | 1.0 |

示例：难度-5时，蓄力槽40%处减速倍率 = 0.99 + (1.0 - 0.99) × 5/10 = 0.995

**全局蓄力槽保护**（任意非鱼王难度等级，BATCH-020 新增）：
- **适用范围**：所有非鱼王难度等级（包括正数、负数和0）；鱼王类保持完整规则豁免
- **20%处**：蓄力槽减少速度降为原本的 80%（减速倍率 0.80）
- **1%处**：蓄力槽减少速度降为原本的 50%（减速倍率 0.50）
- **与负数保护的叠加规则**：两者同时生效时，取效果更强的（最小倍率）
- **通用冲突规则**：任意其他会降低蓄力槽下降速度的规则同时生效时，统一取效果更强的倍率，即所有适用倍率中的最小值。
  - 示例：负数难度在20%处可能是0.60，全局保护是0.80，最终取0.60
  - 示例：正数难度在20%处负数保护不生效（1.0），全局保护是0.80，最终取0.80

##### 零点（基础难度规则原版一致）
难度等级=0时，基础难度、数量、经验和品质等核心规则与原版一致；全局蓄力槽保护仍然应用。

##### 正数区间 [0, 100]（提升奖励与挑战）
线性插值，所有效果在0和100之间连续渐变：

| 属性 | 0时 | 100时 | 公式 |
|---|---|---|---|
| difficulty倍数 | 1 | 50 | 1 + 49 × (level/100) |
| 获得数量倍数 | 1 | 200 | 1 + 199 × (level/100) |
| 经验倍数 | 1 | 200 | 1 + 199 × (level/100) |
| 品质提升 | 0级 | +20级 | floor(level / 5) |

**品质提升规则**：
- 每5个难度等级提升1级品质
- 不足5级不提升（如等级4→品质+0，等级5→品质+1）
- 提升后品质 = 原品质 + floor(level/5)
- 品质映射：0=普通，1=银，2=金，4=铱（跳过3）
- 最高品质：铱星（4）

**视觉尺寸**：
- fishSize数值 = 原值 × 数量倍数
- 绘制缩放 = 原缩放 × (数量倍数)^(1/3)

### 2. HUD 提示系统

#### 2.1 成功钓起提示
- 普通鱼未封顶时：左下角显示"下一次你将向{鱼名}中的{称号}发起挑战"
- 当本次成功后的难度等级小于等于0时，不显示上述挑战称号提示。
- 普通鱼已达到100级，或100级封顶后再次成功时：左下角显示"你已经成为{鱼名}中的神明，这一刻你是鱼，也是人，更是王。"
- 非鱼类已达到8级，或8级封顶后再次成功时：左下角显示"对于{鱼名}而言，你已是帝王"
- 鱼王类：随机显示10种独特文案之一，例如"嗷！孤傲的王！"

**称号对应表**：
| 难度等级 | 称号 |
|---|---|
| <1 | 额...稍微强一点的个体 |
| 1-3 | 精英 |
| 4-6 | 骑士 |
| 7-8 | 领主 |
| 9-15 | 伯爵 |
| 16-22 | 大公 |
| 23-33 | 亲王 |
| 34-45 | 帝王 |
| 46-66 | 神皇 |
| 67-88 | 神王 |
| 89-100 | 神王 |

#### 2.2 失败提示
- 等级<0（非触底）："你对{鱼名}的了解加深了"，**括号中显示难度绝对值**（如等级-5显示"（5）"）
- 等级≥0："还是挑战{鱼名}中更平庸的个体吧"
- 等级=-10（触底）："最平庸的{鱼名}**（10）**依然太难了，请升级鱼竿，钓鱼等级，使用料理增加钓鱼等级，使用陷阱/浮木渔具等"

### 3. 超大鱼展示系统

#### 3.1 触发条件（三个条件同时满足）
1. 鱼在工具栏被选中且举起（手柄仅选中不举起不算）
2. 数量倍数 n > 15（即难度等级≥8，因为倍数公式为 1+199×level/100）
3. 玩家自钓到该鱼后尚未进入 FarmHouse 室内

#### 3.2 NPC 反应规则
**冒泡反应**：
- 触发范围：玩家5格内的NPC
- 显示方式：头顶冒泡，随机100种文案
- 文案要求：提及鱼种类 + 具体 fishSize 数值（注意：是 fishSize 整数，不是倍数）
- 频率限制：每个NPC每天最多冒泡1次

**主动对话**：
- 首次对话：替换为赞美文案（与冒泡使用相同文案池）
- 二次对话：恢复原对话
- 频率限制：每个NPC每天最多替换对话1次
- 计数独立：冒泡和对话分别计数

**重复触发**：
- 同一NPC当天对不同种类超大鱼可分别触发
- 同一种类鱼当天只触发一次

#### 3.3 尺寸重置规则
- 玩家进入 FarmHouse 室内后，当前持有的所有超大鱼永久失去放大效果
- 下次钓到新的超大鱼时，重新计算倍数

### 4. 收藏与永久加成

#### 4.1 触发条件
钓到某种鱼，且该鱼的**调整后difficulty ≥ 120**

示例：
- 原始difficulty = 3
- 难度等级 = 100（倍数50）
- 调整后difficulty = 3 × 50 = 150 ≥ 120 ✓

#### 4.2 收藏星标
- 位置：Collections 菜单中该鱼图标的左上角
- 样式：小星星标记
- 持久化：保存到存档数据

#### 4.3 钓鱼等级加成
- 加成方式：隐藏 +0.5（不改变显示数字）
- 影响范围：所有依赖钓鱼等级的计算（绿条高度、宝箱概率等）
- 累加规则：每种鱼达标一次，可无限累加
- 无上限：理论上可达到 10（显示）+ 无限×0.5（隐藏）
- **成功门槛**：只有成功钓起且本次小游戏的调整后 `difficulty ≥ 120` 才获得星标和加成；仅进入或失败不获得。

#### 4.4 鱼图鉴显示增强
- 位置：Collections 菜单中每种鱼的描述文本
- 新增一行显示：
  - "挑战等级：{称号}（等级{X}）"
  - "钓鱼技能加成：+{Y}"（Y = 星标鱼种数 × 0.5）
- 示例："挑战等级：伯爵（等级12） | 钓鱼技能加成：+2.5"
- 没有有效钓鱼技能加成时，不显示“钓鱼技能加成”一项。
- 当前难度等级小于等于0时，不显示“挑战等级”一项。

#### 4.5 星标鱼开局挑战宣言
- 触发条件：钓鱼小游戏出现时，当前鱼种已经拥有图鉴星标；鱼王不可能获得星标，因此不触发本规则。
- 显示位置：左下角 HUD，且每个小游戏实例只显示一次。
- 文案结构：`{尊敬词}{鱼名}{鱼的等级}{毅然决然词}{应战说法}。`
- `{尊敬词}` 根据当前难度等级对应的称号选择，每个称号提供3种随机替换。
- `{毅然决然词}` 从50种随机替换中选择；`{应战说法}` 从20种随机替换中选择，三部分独立随机。
- 该提示只展示当前权威难度事实，不修改难度、星标、经验或其他持久数据。

#### 4.6 小游戏出现时的钓鱼等级建议
- 触发时机：钓鱼小游戏出现后，在小游戏构造边界读取当前玩家的有效钓鱼等级；有效等级包含已经取整的隐藏钓鱼技能加成。
- 鱼王类完全豁免本规则，不显示建议或“不可能”提示。
- 当 `难度等级 / 10 > 玩家钓鱼等级` 时，左下角显示：`建议达到{难度等级/10}以上钓鱼等级再挑战，同时使用食物增加钓鱼等级，升级鱼竿并使用陷阱鱼饵/浮木鱼饵。`
- 当 `难度等级 / 10 > 玩家钓鱼等级 × 2` 时，在上述建议文案开头额外加上：`这是不可能的高难度挑战！`
- `{难度等级/10}` 使用当前难度等级除以10后的数值显示，保留必要的小数位，不把比较值向下取整。
- 该规则只显示提示，不修改钓鱼等级、难度、鱼获、经验或蓄力槽。

### 5. 物品处理

#### 5.1 钓鱼动画与数量转化
- **视觉表现**：钓鱼时始终显示为钓到1条鱼的动画
- **数量转化时机**：鱼进入背包或溢出界面时才应用数量倍数
- **实现边界**：数量倍数在原生 `FishingRod.CreateFish()` 创建最终物品后应用，因此背包和原生 `ItemGrabMenu` 共用同一个物品，不建立第二套溢出逻辑。
- **原生机制兼容**：
  - 游戏原生鱼饵可能钓到多条鱼（如海泡鱼饵）
  - 原生多条鱼显示多个飞行动画
  - 本Mod的倍数机制不影响原生多条鱼功能
  - 特殊情况：原生机制钓到2条 × Mod倍数10 = 最终20条

#### 5.2 多条鱼堆叠
- 按原版堆叠规则处理（同种类同品质可堆叠）
- 堆叠上限：参考原版逻辑（通常为999）

#### 5.3 背包溢出
- **原生边界**：背包满时由原生 `ItemGrabMenu` 接管溢出鱼，让玩家选择如何处理
- **数量时机**：数量倍数在进入背包或进入溢出界面前完成转换；不得因数量倍数而丢失鱼获
- **本Mod继承原生行为**：当数量倍数很高（如100倍）导致背包装不下时，继续使用原生菜单，不建立第二套溢出界面

## 实现优先级

### Phase 1：核心机制（必须）
1. 难度等级统计与存档持久化
2. Difficulty 数值动态调整
3. 获得数量/经验/品质计算
4. HUD 提示系统

### Phase 2：视觉与交互（重要）
1. 超大鱼举起动画缩放
2. NPC 冒泡反应（100种文案）
3. NPC 对话替换
4. Collections 星标显示

### Phase 3：高级功能（可选）
1. 钓鱼等级隐藏加成
2. 蓄力槽保护机制（负数难度）
3. 尺寸重置判定（进入FarmHouse）

## 已确认的原生行为（反编译审计结果）

### ✅ 多条鱼处理逻辑
**位置**：`FishingRod.pullFishFromWater()` → `doPullFishFromWater()`

**机制**：
- 通过 `numberOfFishCaught` 参数控制数量
- 如果 `numberOfFishCaught > 1`，创建多个 `TemporaryAnimatedSprite` 飞行动画
- 每个额外的鱼延迟 `(i-1) × 100ms` 播放动画
- 最终调用 `Farmer.caughtFish(fishId, fishSize, fromFishPond, numberOfFishCaught)` 创建物品

**溢出处理**：
- 以用户确认的设计合同为准：背包满时由原生 `ItemGrabMenu` 处理溢出鱼
- 不得把“直接丢失”作为已确认行为；若运行证据显示丢失，必须重新开启独立 Case

### ✅ NPC 气泡系统
**API 入口**：`NPC.doEmote(int emoteIndex)` 和 `NPC.showTextAboveHead(string text)`

**重要发现**：
- `doEmote()` 只能显示预定义的表情图标（12=爱心, 28=惊讶等）
- **自定义文本必须使用 `showTextAboveHead()`**
- 或者修改 `NPC.CurrentDialogue` 实现对话替换

### ✅ 蓄力槽减速机制
**位置**：`BobberBar.update()` 中的蓄力槽计算

**关键字段**：
- `distanceFromCatching`：蓄力槽进度 [0, 1]
- `distanceFromCatchPenaltyModifier`：减速倍率（默认1.0）
- `bobberInBar`：鱼是否在绿条内

**减速公式**：
```csharp
distanceFromCatching -= 0.003f * distanceFromCatchPenaltyModifier;
```

## 状态边界

本文件只定义用户确认的游戏设计，不再维护逐批次的实现状态。当前构建、部署、实测和待验收状态统一读取 `BUG-LEDGER.md` 与 `MAINTENANCE-INDEX.md`；旧版本状态保留在历史报告中作为证据，不作为当前结论。

---

## 6. 特殊鱼类规则

### 6.1 鱼王类（传奇鱼）
- **适用鱼种**：五大传奇鱼（不含扩展传奇鱼）
  - 传奇鱼 (Legend) - ID: (o)163
  - 突变鲤鱼 (Mutant Carp) - ID: (o)682
  - 鮟鱇鱼 (Angler) - ID: (o)160
  - 冰川鱼 (Glacierfish) - ID: (o)775
  - 赤红鱼 (Crimsonfish) - ID: (o)159
  
- **规则豁免**：
  - 不使用难度等级系统（不记录成功/失败）
  - 不应用数量/品质/难度倍数
  - 其他鱼类的钓鱼等级加成对鱼王无效
  - 不显示挑战称号或等级提示
  - 不触发视觉缩放、NPC反应

- **全局保护边界**：鱼王不使用20%/1%蓄力槽保护。当前代码让鱼王在 `BobberBar` 构造边界完整退出自定义实例状态，继续保持这条完整豁免路径可以避免新增状态、清理和特殊更新分支；全局保护的“任意难度等级”指所有非鱼王难度等级。
  
- **独特提示**：钓到鱼王时，随机显示10种独特文案之一
  1. "嗷！孤傲的王！"
  2. "传说中的巨兽，终于现身！"
  3. "这是属于你的荣耀时刻！"
  4. "世间仅此一只，已入你囊中！"
  5. "山巅之王，俯首称臣！"
  6. "传说落幕，新的传奇诞生！"
  7. "你已成为渔夫中的传奇！"
  8. "王者归来，唯你独尊！"
  9. "这一刻，你是鱼，也是王！"
  10. "传说终结于此，荣耀属于你！"

### 6.2 非鱼类（垃圾/藻类）
- **适用物品**：Category不等于-4的可钓物品
  - 垃圾：破报纸、破CD、垃圾、烂靴子、浸水报纸等
  - 藻类：海草、绿藻、白藻等
  - 其他非鱼类可钓物品
  
- **等级上限**：8级（封顶）
  
- **规则应用**：
  - 等级范围：[-10, 8]
  - 达到8级后不再增长（成功时等级保持8）
- **封顶后的成功**：达到8级后后续成功的实际难度收益为 `+0`，不得继续增加底层难度统计。
  - 难度/数量/品质倍数正常计算（基于等级8的倍数）
  - 视觉缩放、NPC反应、星标系统正常工作
  
- **封顶提示**：达到8级后再钓到时显示"对于{物品名}而言，你已是帝王"；达到上限后的成功不再增加底层难度统计。
