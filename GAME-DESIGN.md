# FishingExpanded — 游戏设计文档

本文件记录用户确认的钓鱼扩展系统完整设计方案，作为实现的唯一权威来源。

## 核心机制

### 1. 难度等级系统

#### 1.1 统计规则
- **统计粒度**：每种鱼独立统计（按存档+玩家，多人联机不共享）
- **多人部署**：在线联机时主机和每一位农场客都必须安装本 Mod；每位玩家只读取和写入自己的 `Farmer.modData`（`Character.modData` 注册进原生 NetFields，农场客数据同步进主机存档）。
- **双人同屏（本地分屏）**：支持；每位本地玩家是独立 `Game1` 实例（`Game1.player` 随屏幕切换），本 Mod 难度/星标/加成/展示事实一律按玩家 `UniqueMultiplayerID` 隔离，副屏玩家不读写主玩家数据（BATCH-027，2026-08-06 用户确认）。
- **品质合并**：不同品质（普通/银/金/铱）算作同一种鱼
- **等级变化**：
  - 成功钓起：根据脱杆次数加等级
    - 0次脱杆（完美）：+10
    - 1次脱杆：+5
    - 2次脱杆：+2
    - 3次及以上：+1
    - 额外等级收益（2026-08-06 用户确认，BATCH-029）：每次成功额外增加 `round(调整后难度系数 / 50)` 级（四舍五入到整数；例：调整后难度 500 → +10），与脱杆收益叠加后统一按称号区间封顶；减少肝度。
    - 先按上述数值计算本次收益，再应用称号区间边界；收益不能使等级跨越当前区间，但“完美钓鱼 +10”仍是本次计算的原始收益。
  - 钓鱼失败：等级 -1
- **越级限制**：
  - 一次加等级时，最多只能晋升到下一个称号区间的上限
  - 不允许一次跨越多个称号
  - 示例：
    - 当前等级1（精英）+ 完美钓鱼（+10）→ 最多升到6（骑士上限），不能直接到9（伯爵）
    - 当前等级7（领主）+ 完美钓鱼（+10）→ 最多升到15（伯爵上限），不能直接到17（大公）
  - 称号区间上限表：
    - 弱小(<1) → 精英上限(3)
    - 精英(1-3) → 骑士上限(6)
    - 骑士(4-6) → 领主上限(8)
    - 领主(7-8) → 伯爵上限(15)
    - 伯爵(9-15) → 大公上限(22)
    - 大公(16-22) → 亲王上限(33)
    - 亲王(23-33) → 帝王上限(45)
    - 帝王(34-45) → 神皇上限(66)
    - 神皇(46-66) → 神王上限(88)
    - 神王(67-88) → 创世神上限(99)
    - 创世神(89-99) → 太一上限(100)
    - 太一(100) → 太一上限(100)
    
- **等级范围**：
  - 普通鱼类：[-10, 100]
  - 非鱼类（垃圾/藻类等）：[-10, 8]，用户确认的上限为8
  - 鱼王类（传奇鱼）：不使用本系统规则
- **封顶提示**：
  - 普通鱼达到100级：显示特殊提示"你已经成为{鱼名}中的神明，这一刻你是鱼，也是人，更是王。"
  - 非鱼类达到8级：显示"对于{鱼名}而言，你已是帝王"
- **触底提示**：
  - 难度-10时再失败：显示"最平庸的{鱼名}依然太难了，请升级鱼竿，钓鱼等级，使用料理增加钓鱼等级，使用陷阱/软木塞浮标等"

#### 1.2 难度等级效果

##### 负数区间 [-10, 0]（降低难度）
线性插值，所有效果在-10和0之间连续渐变：

**Difficulty 数值调整**：
- -10：difficulty × 0.5
- 0：difficulty × 1.0

**蓄力槽保护**（鱼逃跑时减速）：
| 蓄力槽阈值 | -10时减速倍率 | 0时减速倍率 |
|---|---|---|
| ≤40% | 0.99 | 1.0 |
| ≤20% | 0.60 | 1.0 |
| ≤1% | 0.20 | 1.0 |

示例：难度-5时，蓄力槽40%处减速倍率 = 0.99 + (1.0 - 0.99) × 5/10 = 0.995

**全局蓄力槽保护**（任意非鱼王难度等级，BATCH-020 新增）：
- **适用范围**：所有非鱼王难度等级（包括正数、负数和0）；鱼王类保持完整规则豁免
- **20%处**：蓄力槽减少速度降为原本的 80%（减速倍率 0.80）
- **1%处**：蓄力槽减少速度降为原本的 50%（减速倍率 0.50）
- **与负数保护的叠加规则**：两者同时生效时，取效果更强的（最小倍率）
- **通用冲突规则**：任意其他会降低蓄力槽下降速度的规则同时生效时，统一取效果更强的倍率，即所有适用倍率中的最小值。
  - 示例：负数难度在20%处可能是0.60，全局保护是0.80，最终取0.60
  - 示例：正数难度在20%处负数保护不生效（1.0），全局保护是0.80，最终取0.80

##### 零点（基础难度规则原版一致）
难度等级=0时，基础难度、数量、经验和品质等核心规则与原版一致；全局蓄力槽保护仍然应用。

##### 正数区间 [0, 100]（提升奖励与挑战）
线性插值（difficulty 倍数自 2026-08-10 起为分段线性），所有效果在0和100之间连续渐变：

| 属性 | 0时 | 100时 | 公式 |
|---|---|---|---|
| difficulty倍数 | 1 | 50 | 分段线性锚点：0→×1.0、4→×1.2、20→×5、50→×20、100→×50；每段斜率递增（0~4：0.05/级、4~20：0.2375/级、20~50：0.5/级、50~100：0.6/级），越往后每级加得越多（2026-08-10 用户确认，BATCH-037） |
| 获得数量倍数 | 1 | 200 | 1 + 199 × (level/100) |
| 经验倍数 | 1 | 200 | 1 + 199 × (level/100) |
| 品质提升 | 0级 | +20级 | floor(level / 5) |

难度倍数示例：4级=×1.2、20级=×5、50级=×20、100级=×50；10级=×2.625、30级=×10、80级=×38。

**品质提升规则**：
- 每5个难度等级提升1级品质
- 不足5级不提升（如等级4→品质+0，等级5→品质+1）
- 提升后品质 = 原品质 + floor(level/5)
- 品质映射：0=普通，1=银，2=金，4=铱（跳过3）
- 最高品质：铱星（4）

##### 高难度鱼跳机制（调整后难度 150+，2026-08-06 用户新增）

**适用范围**：
- 仅非鱼王；鱼王小游戏完整豁免。
- “钓鱼难度”指小游戏实际难度（原版 difficulty × 难度等级倍数，即星标判定用的调整后 difficulty，与等级 -10~100 不是同一数值）。
- 调整后难度 < 150 不参与。

**触发间隔（按调整后难度分档）**：

| 调整后难度 | 触发间隔 |
|---|---|
| 150 ~ 250 | 每 8 秒 |
| 251 ~ 350 | 每 6 秒 |
| 351 ~ 450 | 每 5 秒 |
| 451 ~ 550 | 每 4 秒 |
| 551 及以上 | 每 3 秒 |

**触发行为**：
- 鱼位于鱼可移动范围**下 25% 区域**（底部四分之一）时：0.5 秒后立刻跳到**上 25% 区域**（顶部四分之一内随机位置）。
- 鱼位于**上 25% 区域**时：0.5 秒后立刻跳到**下 25% 区域**（底部四分之一内随机位置）。
- 计时到点但鱼不在上/下 25% 区域：等待触发，统一每 1 秒检测一次，直到鱼进入上或下 25% 区域后按上述 0.5 秒延迟跳。
- 0.5 秒延迟期间鱼自行游出 25% 区域仍照跳（跳向相对方向的 25% 区域）。
- **上一次触发（跳跃完成）后才能开始新的计时**；跳跃为瞬间位移，不改变 fishSize、数量、品质、难度或结算状态。

##### 高难度运动公式修正（调整后难度 >100，2026-08-06 用户新增）

**适用范围**：仅非鱼王；“难度”指小游戏实际难度（原版 difficulty × 难度等级倍数，即调整后 difficulty）。难度 ≤100 时原生运动公式完全不变；鱼王完整豁免。

**修正规则**：
- **初始目标**：难度 >100 时，鱼的初始目标位置固定为顶部（原生公式 `(100-难度)/100×548` 在 >100 时算出负数被钳成“无目标”，修正为顶部）。
- **换目标频率**：难度 >150 时，三处换目标概率一律按 150 计算（150 以内按实际值）：
  - 大范围换目标：`min(难度,150)/4000`（每帧概率）
  - 小偏移换目标（±50~100 像素）：`min(难度,150)/2000`（每帧概率）
  - dart 型鱼额外换目标：`min(难度,150)/1000`（每帧概率），且 dart 偏移量 `±(50~100+难度×2)` 同样按 150 封顶计算（2026-08-06 用户确认）
- **加速度**：难度 >100 时，加速度分母固定为 `随机(10,30)`；高于 100 的部分线性增幅到加速度：`加速度 = 距离/随机(10,30) × (1 + 档位 × (难度-100)/100)`，无上限；档位按难度等级（2026-08-09 用户确认，BATCH-034；2026-08-09 晚用户确认该档位公式正确并还原 BATCH-036 临时改动）：等级 <98 档位=0.1（降为现增幅的 10%，示例：难度190 → ×1.09），等级 ≥98 档位=1（保持 100% 增幅，示例：难度190 → ×1.9）。
  - 难度 100~150 区间：换目标频率按实际值（未超 150）、分母为 `随机(10,30)`、增幅 1.0~1.5 线性，与难度 100 处原生公式无缝衔接（100 处原生分母恰为 `随机(10,30)+0`、初始目标恰为顶部）。

**原生代码对齐**（2026-08-06 核对 `_analysis/StardewValley.BobberBar.decompiled.cs`）：
- 原生 `Data/Fish` 行为字段 → motionType：`mixed`=0、`dart`=1、`smooth`=2、`sinker`=3、`floater`=4。
- 本文“dart 型鱼”即原生 `dart`（motionType 1）：`难度/1000` 概率块与偏移 `±(50~100+难度×2)` 是该类型专属；原生 `floater`（4）/`sinker`（3）不参与，只额外有恒速漂移。
- 大范围换目标块（`难度/4000`）对所有类型生效，但 `smooth`（motionType 2）频率 ×20 且仅当当前目标为 -1 时触发；×20 与本条封顶同属同一公式（封顶后为 150×20/4000），无需单独处理。
- 小偏移块（`难度/2000`，±50~100）对除 `smooth` 外的类型生效。
**与鱼跳机制的叠加**：公式修正接管鱼在两次跳跃之间的原生运动表现；鱼跳机制按各自分档间隔强制瞬移，两者互不冲突、互不修改对方状态。

##### 手感系统（玩家操作手感，2026-08-09 用户确认，BATCH-034）

**进度来源（α）**：α = 玩家已收集的可计数皇冠数 ÷ 61（61 = 56 条原生普通真鱼 + 5 条原版传奇鱼）。其他 Mod 鱼获得的皇冠只显示、不计入 α；原版 5 条传奇鱼钓到一次直接给皇冠并计入 α。α=0 时小游戏行为与原生完全一致；α=1 为终点手感；中间按 α 线性插值。

**终点手感（α=1）**：
- 按下：绿条速度直接变为 30 px/帧（向上）；松开：直接变为 30 px/帧（向下）；无加速度过程、无条内阻尼、无惯性。
- 撞边：完全钳制（贴边停住、速度归零、不反弹）。
- 条内阻尼（原生 ×0.6 / 陷阱浮标 ×0.3）与陷阱浮标牵引随 α 线性取消。

**线性分配**：
- 输入响应：`速度 = (1-α) × (原速度 + 原生加速度×帧缩放) + α × 终点速度`（α=0 恰为原生积分，α=1 恰为直接定速，中间连续过渡）。
- 撞边：反弹保留系数 `2/3 × (1-α)`（α=0 原生反弹，α=1 完全钳制）。

**帧率解耦（整个钓鱼小游戏）**：绿条运动、鱼运动、平滑、随机概率全部改为 delta-time 基准，60fps 下与原版行为完全一致（逐帧增量乘 `dt×60`；概率按 `1-(1-p)^(dt×60)` 换算；平滑按 `1-0.8^(dt×60)` 换算，60fps 时精确等于原生 0.2）。蓄力槽进度与宝箱进度保持原生逐帧（60fps 基准）不在此列。

**高难度鱼跳文案**：150+ 跳鱼机制保持；每次瞬移在鱼旁边显示文案——向上跳（目标区 0~133）随机显示 15 条“鱼跃”类文案，向下跳（目标区 399~532）随机显示 15 条“甩尾！”类文案；文案停留 1 秒后 0.5 秒淡出（2026-08-09 用户确认）。

**视觉尺寸**：
- fishSize数值 = 原值 × 等级尺寸倍率（2026-08-06 用户确认，BATCH-029）：正等级每级 +10%（100级=11×）、负等级每级 -5%（-10级=0.5×，防御钳制 ≥0.1）、0级=1×；与数量倍数解耦，数量倍数只用于鱼获数量
- 绘制缩放 = 原缩放 × 线性值：`缩放 = 1 + (数量倍数 - 1) × 0.0136102`，即 0级=1.0、100级≈3.7084（= 原25级大小 51^(1/3)）、每级≈+0.0270843（2026-08-06 用户确认，BATCH-026）

**视觉锚点合同**：
- 手持鱼：以鱼图源矩形的底边中点为锚点。放大后底边中点保持在原生绘制的同一位置，鱼图向上和左右扩展。
- 钓鱼小游戏：鱼标保持原生大小和中心位置，不做视觉缩放（2026-08-06 用户确认）。
- 钓鱼结算面板：面板内展示鱼名与尺寸的示意图保持原生大小，不做视觉缩放（2026-08-06 用户确认）。
- 钓鱼结算真鱼：玩家身边从水里钓起并举起的真鱼（结算第二处鱼图及原生多鱼图，原生位置 `(0,-56)`、原点 `(8,8)`、`3f`，点击后 `doneHoldingFish` 收包）以每一张鱼图源矩形的底边中点为锚点放大；放大后鱼图底边中点保持在原生基准位置；原生多鱼机制产生的多张鱼图仍各自使用底边中点锚点。
- 从水里飞向玩家的飞行动画：放大，以每一张鱼图源矩形的正中央为锚点；放大后鱼图中心保持在原生飞行轨迹上。原生多鱼机制仍保持原生数量与飞行动画数量。
- 锚点只影响绘制位置和缩放，不改变fishSize、鱼获数量、动画数量、品质或结算状态。

### 2. HUD 提示系统

#### 2.1 成功钓起提示
- 普通鱼未封顶时：左下角显示"下一次你将向{鱼名}中的{称号}发起挑战"
- 当本次成功后的难度等级小于1（0 级和负数）时，仍显示上述提示，称号为“额...稍微强一点的个体”（2026-08-07 用户确认：零以下胜利应显示弱称号提示，取代旧设计“小于等于0不显示”；BATCH-030）。
- 普通鱼已达到100级，或100级封顶后再次成功时：左下角显示"你已经成为{鱼名}中的神明，这一刻你是鱼，也是人，更是王。"
- 非鱼类已达到8级，或8级封顶后再次成功时：左下角显示"对于{鱼名}而言，你已是帝王"
- 鱼王类：随机显示10种独特文案之一，例如"嗷！孤傲的王！"

**称号对应表**：
| 难度等级 | 称号 |
|---|---|
| <1 | 额...稍微强一点的个体 |
| 1-3 | 精英 |
| 4-6 | 骑士 |
| 7-8 | 领主 |
| 9-15 | 伯爵 |
| 16-22 | 大公 |
| 23-33 | 亲王 |
| 34-45 | 帝王 |
| 46-66 | 神皇 |
| 67-88 | 神王 |
| 89-99 | 创世神 |
| 100 | 太一（2026-08-10 由“混沌”改名，BATCH-038） |

#### 2.2 失败提示
- 等级<0（非触底）："你对{鱼名}的了解加深了"，**括号中显示难度绝对值**（如等级-5显示"（5）"）
- 等级≥0："还是挑战{鱼名}中更平庸的个体吧"
- 连续失败史诗提示（2026-08-06 用户确认，BATCH-029）：同一鱼种连续失败 ≥2 次（成功清零计数）且本次小游戏调整后难度 ≥150 时，失败提示替换为："收集更多徽章（鱼类收集品页的皇冠），获得更多加成，再挑战史诗级强者吧"；第 1 次失败、调整后难度 <150 或鱼王仍显示普通失败提示。
- 满皇冠史诗替换（2026-08-10 用户确认，BATCH-038）：史诗条件成立且玩家已获得全部可计数皇冠（α≥1）时，不再提示收集更多皇冠，改为："你需要进入人鱼合一的状态，你必须是鱼，才能赢下这场挑战。"（i18n `hud.fail.union`）；未满皇冠仍显示旧史诗文案（`hud.fail.epic`）。
- 等级=-10（触底）："最平庸的{鱼名}**（10）**依然太难了，请升级鱼竿，钓鱼等级，使用料理增加钓鱼等级，使用陷阱/软木塞浮标等"
- **提示优先级**（多条件同时满足时按此顺序显示，BATCH-031 设计核对确认与代码 ShowFailureNotification 一致）：史诗提示 > 触底提示 > 负等级提示 > 普通失败提示。


#### 2.3 HUD 提示排队（2026-08-08 用户确认）
- 所有本 Mod 左下角 HUD 提示统一经 FIFO 队列显示：同一屏幕同一时间只显示一条本 Mod 提示。
- 后续提示排队等待：前一条从原生 `Game1.hudMessages` 消失后自动显示下一条（每条保持原生默认显示时长，约 3.5 秒淡出）。
- **覆盖范围**：成功提示、失败提示、等级建议、星标挑战宣言、星之果茶掉落消息，全部排队。
- **队列上限**：5 条；满时丢弃最旧一条（保留最新）。
- 只排队本 Mod 自己的提示；原生或其他 Mod 的 HUD 消息不受影响、不参与排队。
- **例外（BATCH-038）**：钓鱼小游戏内的浮动提示（鱼动作提示/鱼其他提示）不经 FIFO 队列，在钓鱼条两侧独立显示（见第 7 节）；成功/失败/等级建议/星之果茶等左下角消息仍排队。
- 双人同屏：各屏幕按玩家独立排队（BATCH-027 模式）；在线联机每位本地玩家各自排队。
- 返回标题或切换存档时清空队列。
### 3. 超大鱼展示系统

#### 3.1 触发条件（三个条件同时满足）
1. 鱼在工具栏被选中且举起（手柄仅选中不举起不算）
2. 数量倍数 n > 15（即难度等级≥8，因为倍数公式为 1+199×level/100）
3. 玩家自钓到该鱼后尚未进入 FarmHouse 室内

#### 3.2 NPC 反应规则
**冒泡反应**：
- 触发范围：玩家5格内的NPC
- 显示方式：头顶冒泡，随机100种文案
- 文案要求：提及鱼种类 + 具体尺寸，显示厘米（fishSize×2.54 取整，与手持鱼旁原生尺寸数字一致；2026-08-08 用户确认，BATCH-033）
- 频率限制：每个NPC每天最多冒泡1次

**主动对话**：
- 首次对话：替换为赞美文案（与冒泡使用相同文案池）
- 二次对话：恢复原对话
- 频率限制：每个NPC每天最多替换对话1次
- 计数独立：冒泡和对话分别计数

**重复触发**：
- 同一NPC当天对不同种类超大鱼可分别触发
- 同一种类鱼当天只触发一次

#### 3.3 尺寸重置规则
- 玩家进入 FarmHouse 室内后，当前持有的所有超大鱼永久失去放大效果
- 下次钓到新的超大鱼时，重新计算倍数

### 4. 收藏与永久加成

#### 4.1 触发条件
- 普通鱼（含扩展传奇 898–902 与 Mod 鱼）：成功钓起且该鱼的**调整后difficulty ≥ 120**
- 原版 5 条传奇鱼（159/160/163/682/775）：钓到一次直接获得（无难度门槛，2026-08-09 用户确认）

示例：
- 原始difficulty = 3
- 难度等级 = 100（倍数50）
- 调整后difficulty = 3 × 50 = 150 ≥ 120 ✓

#### 4.2 收藏星标
- 位置：Collections 菜单中该鱼图标的左上角
- 样式：金色皇冠标记（原生 `Characters\Farmer\hats` 的 Infinity Crown 贴图，源矩形 (20,800,20,20)，左上角绘制 24×24；2026-08-06 用户确认星标改皇冠，BATCH-029）
- 持久化：保存到存档数据

#### 4.3 皇冠与手感进度（2026-08-09 用户确认，BATCH-034）
- 皇冠来源：① 普通鱼（含扩展传奇 898–902 与 Mod 鱼）成功钓起且本次调整后 difficulty ≥120；② 原版 5 条传奇鱼钓到一次直接给皇冠。
- 手感进度 α = 可计数皇冠数 ÷ 61（可计数 = 56 条原生普通真鱼 + 5 条原版传奇鱼）；Mod 鱼皇冠只显示、不计入 α。
- 隐藏钓鱼等级加成已整体删除（原每皇冠 +0.2，BATCH-034）。
- 流动金色皇冠（2026-08-10 用户确认，BATCH-038）：难度等级 ≥95 的鱼在挑战鱼饵生效的情况下成功钓起（无论是否超过 5 分钟时限，超时只取消数量加成），该鱼皇冠升级为流动效果的金色（Collections 页金/白呼吸 + 1±0.05 缩放脉冲）；独立存档字段 `ChallengeCrowns`，旧存档缺失默认空。

#### 4.4 鱼图鉴显示增强
- 位置：Collections 菜单中每种鱼的描述文本
- 新增一行显示：
  - "挑战等级：{称号}（等级{X}）"
- 示例："挑战等级：伯爵（等级12）"
- 当前难度等级小于等于0时，不显示“挑战等级”一项。
- 有皇冠的鱼追加显示其提供的加成："钓鱼控制力+1"（i18n `collections.crownControl`，中英双语；2026-08-09 用户确认，BATCH-036）。纯展示，不新增状态；鱼王豁免挑战等级但皇冠行照常显示（原版 5 传奇一次钓获直接给皇冠，BATCH-034）。

#### 4.5 星标鱼开局挑战宣言
- 触发条件：钓鱼小游戏出现时，当前鱼种已经拥有图鉴星标；鱼王不可能获得星标，因此不触发本规则。
- 显示位置：左下角 HUD，且每个小游戏实例只显示一次。
- 文案结构：`{尊敬词}{鱼名}{鱼的等级}{毅然决然词}{应战说法}。`（等级 <1 的弱称号除外，见下）
- 等级 <1 的鱼：宣言不包含“额...稍微强一点的个体”称号文本，只显示 `{尊敬词}{鱼名}{毅然决然词}{应战说法}。`（该称号只用于零以下胜利提示；2026-08-07 用户确认，BATCH-030）。
- `{尊敬词}` 根据当前难度等级对应的称号选择，每个称号提供3种随机替换。
- `{毅然决然词}` 从50种随机替换中选择；`{应战说法}` 从20种随机替换中选择，三部分独立随机。
- 该提示只展示当前权威难度事实，不修改难度、星标、经验或其他持久数据。

#### 4.6 小游戏出现时的钓鱼等级建议
- 触发时机：钓鱼小游戏出现后，在小游戏构造边界读取当前玩家的钓鱼等级（BATCH-034：隐藏加成已整体删除，使用原生钓鱼等级）。
- 鱼王类完全豁免本规则，不显示建议或“不可能”提示。
- 当 `难度等级 / 10 > 玩家钓鱼等级` 时，左下角显示：`建议达到{难度等级/10}以上钓鱼等级再挑战，同时使用食物增加钓鱼等级，升级鱼竿并使用陷阱浮标/软木塞浮标。`
- 当 `难度等级 / 10 > 玩家钓鱼等级 × 2` 时，在上述建议文案开头额外加上：`这是不可能的高难度挑战！`
- `{难度等级/10}` 使用当前难度等级除以10后的数值显示，保留必要的小数位，不把比较值向下取整。
- 该规则只显示提示，不修改钓鱼等级、难度、鱼获、经验或蓄力槽。

#### 4.7 皇冠助战（2026-08-09 用户确认，BATCH-035）
- **触发**：非鱼王钓鱼小游戏构造时判定一次（一次小游戏最多一条助战鱼）；鱼王完整豁免。
- **概率**：总助战概率 = 10% × (可计数皇冠数 ÷ 61)，线性（无皇冠=0%、满 61 皇冠=10%）；Mod 鱼皇冠只显示、不计入。
- **选鱼**：命中后从玩家可计数皇冠鱼中**均匀随机**选一条助战鱼（所有皇冠鱼被选中概率相同；2026-08-09 用户修正）。
- **效果**：临时钓鱼等级 = 0~40，**随被选中助战鱼的难度排位 r∈[0,1] 线性倾斜**（权重 w(L)=1+s·(L-20)/20，s=-29/31+58r/31；2026-08-09 用户澄清：所有皇冠鱼被选中概率相同，但等级随机且最高难度鱼更可能给高级助战）：最高难度鱼 P(40)≈4.72%、P(0)≈0.16%、平均 26.55；最低难度鱼镜像 P(0)≈4.72%、P(40)≈0.16%、平均 13.45；40 级概率最高鱼=最低鱼 30 倍；最低+最高两条鱼平均助战等级恰为 20；中位鱼=均匀；最低鱼也可能抽到 40 级。只把本次小游戏绿条高度 +临时等级×8px（原生公式 `bobberBarHeight = 96 + 钓鱼等级×8` 的增量，保留训练鱼竿与浮标原生计算），**不修改玩家钓鱼等级字段、不入存档、不影响经验/数量/品质/难度**。
- **文案**：触发时在“鱼其他提示”通道（钓鱼条左侧，见第 7 节）显示 20 条随机文案（i18n `hud.assist.1~20`，中英双语，含鱼名+鱼职阶称号，如“荣耀的{{fishName}}{{rankName}}前来护驾！”），2 秒内上移 30px 并线性淡出，纯显示不改变任何状态（BATCH-038：由“1 秒停留+0.5 秒淡出”改为浮动提示通道）。
- **尺寸口径不变**：显示的鱼尺寸保持原生口径（英语英寸、其他语言厘米，BATCH-033），助战不参与尺寸计算。
- **测试入口**：`fish_bonus` 显示当前助战概率；`fish_assist` 强制下一次小游戏触发助战（薄控制，不写存档）。

### 5. 物品处理

#### 5.1 钓鱼动画与数量转化
- **视觉表现**：钓鱼时始终显示为钓到1条鱼的动画
- **数量转化时机**：鱼进入背包或溢出界面时才应用数量倍数
- **实现边界**：数量倍数在原生 `FishingRod.CreateFish()` 创建最终物品后应用，因此背包和原生 `ItemGrabMenu` 共用同一个物品，不建立第二套溢出逻辑。
- **原生机制兼容**：
  - 游戏原生鱼饵可能钓到多条鱼（如海泡鱼饵）
  - 原生多条鱼显示多个飞行动画
  - 本Mod的倍数机制不影响原生多条鱼功能
  - 特殊情况：原生机制钓到2条 × Mod倍数10 = 最终20条

#### 5.2 多条鱼堆叠
- 按原版堆叠规则处理（同种类同品质可堆叠）
- 堆叠上限：参考原版逻辑（通常为999）

#### 5.3 背包溢出
- **原生边界**：背包满时由原生 `ItemGrabMenu` 接管溢出鱼，让玩家选择如何处理
- **数量时机**：数量倍数在进入背包或进入溢出界面前完成转换；不得因数量倍数而丢失鱼获
- **本Mod继承原生行为**：当数量倍数很高（如100倍）导致背包装不下时，继续使用原生菜单，不建立第二套溢出界面


#### 5.4 星之果茶掉落（2026-08-08 用户新增）
- **触发**：普通鱼（非鱼王）成功钓起，且该鱼本次小游戏进入时的难度等级 ≥ 50。
- **概率**：`难度等级 / 4`%（即 等级 / 400）；示例：50 级 → 12.5%、80 级 → 20%、100 级 → 25%。
- **掉落**：1 瓶星之果茶（`(O)StardropTea`），走成功结算唯一边界（`Farmer.caughtFish` Postfix，与等级/星标同一防重 `SuccessRecorded`）；背包有空位直接入包，背包满时由原生 `addItemByMenuIfNecessary` 溢出菜单处理。
- **提示**：掉落时额外显示一条左下角消息，15 条文案随机（中英文 i18n，`hud.starfruitTea.1` ~ `.15`），文风幽默、符合游戏场景；该消息与其他提示一起排队。
- **豁免**：鱼王完整豁免；非鱼类上限 8 达不到 50 级，天然不参与。
- **多人**：掉落判定与发放只对本地玩家（`IsLocalPlayer` 边界）。
## 实现优先级

### Phase 1：核心机制（必须）
1. 难度等级统计与存档持久化
2. Difficulty 数值动态调整
3. 获得数量/经验/品质计算
4. HUD 提示系统

### Phase 2：视觉与交互（重要）
1. 超大鱼举起动画缩放
2. NPC 冒泡反应（100种文案）
3. NPC 对话替换
4. Collections 星标显示

### Phase 3：高级功能（可选）
1. ~~钓鱼等级隐藏加成~~（BATCH-034 已整体删除）
2. 蓄力槽保护机制（负数难度）
3. 尺寸重置判定（进入FarmHouse）

## 已确认的原生行为（反编译审计结果）

### ✅ 多条鱼处理逻辑
**位置**：`FishingRod.pullFishFromWater()` → `doPullFishFromWater()`

**机制**：
- 通过 `numberOfFishCaught` 参数控制数量
- 如果 `numberOfFishCaught > 1`，创建多个 `TemporaryAnimatedSprite` 飞行动画
- 每个额外的鱼延迟 `(i-1) × 100ms` 播放动画
- 最终调用 `Farmer.caughtFish(fishId, fishSize, fromFishPond, numberOfFishCaught)` 创建物品

**溢出处理**：
- 以用户确认的设计合同为准：背包满时由原生 `ItemGrabMenu` 处理溢出鱼
- 不得把“直接丢失”作为已确认行为；若运行证据显示丢失，必须重新开启独立 Case

### ✅ NPC 气泡系统
**API 入口**：`NPC.doEmote(int emoteIndex)` 和 `NPC.showTextAboveHead(string text)`

**重要发现**：
- `doEmote()` 只能显示预定义的表情图标（12=爱心, 28=惊讶等）
- **自定义文本必须使用 `showTextAboveHead()`**
- 或者修改 `NPC.CurrentDialogue` 实现对话替换

### ✅ 蓄力槽减速机制
**位置**：`BobberBar.update()` 中的蓄力槽计算

**关键字段**：
- `distanceFromCatching`：蓄力槽进度 [0, 1]
- `distanceFromCatchPenaltyModifier`：减速倍率（默认1.0）
- `bobberInBar`：鱼是否在绿条内

**减速公式**：
```csharp
distanceFromCatching -= 0.003f * distanceFromCatchPenaltyModifier;
```

## 状态边界

本文件只定义用户确认的游戏设计，不再维护逐批次的实现状态。当前构建、部署、实测和待验收状态统一读取 `BUG-LEDGER.md` 与 `MAINTENANCE-INDEX.md`；旧版本状态保留在历史报告中作为证据，不作为当前结论。

---

## 6. 特殊鱼类规则

### 6.1 鱼王类（传奇鱼）
- **适用鱼种**：五大传奇鱼（不含扩展传奇鱼）
  - 传奇鱼 (Legend) - ID: (o)163
  - 突变鲤鱼 (Mutant Carp) - ID: (o)682
  - 鮟鱇鱼 (Angler) - ID: (o)160
  - 冰川鱼 (Glacierfish) - ID: (o)775
  - 赤红鱼 (Crimsonfish) - ID: (o)159
  
- **规则豁免**：
  - 不使用难度等级系统（不记录成功/失败）
  - 不应用数量/品质/难度倍数
  - 其他鱼类的钓鱼等级加成对鱼王无效
  - 不显示挑战称号或等级提示
  - 不触发视觉缩放、NPC反应

- **全局保护边界**：鱼王不使用20%/1%蓄力槽保护。当前代码让鱼王在 `BobberBar` 构造边界完整退出自定义实例状态，继续保持这条完整豁免路径可以避免新增状态、清理和特殊更新分支；全局保护的“任意难度等级”指所有非鱼王难度等级。
  
- **独特提示**：钓到鱼王时，随机显示10种独特文案之一
  1. "嗷！孤傲的王！"
  2. "传说中的巨兽，终于现身！"
  3. "这是属于你的荣耀时刻！"
  4. "世间仅此一只，已入你囊中！"
  5. "山巅之王，俯首称臣！"
  6. "传说落幕，新的传奇诞生！"
  7. "你已成为渔夫中的传奇！"
  8. "王者归来，唯你独尊！"
  9. "这一刻，你是鱼，也是王！"
  10. "传说终结于此，荣耀属于你！"
- **皇冠**：钓到一次直接给皇冠并计入手感进度 α（2026-08-09 用户确认）。

### 6.2 非鱼类（垃圾/藻类）
- **适用物品**：Category不等于-4的可钓物品
  - 垃圾：破报纸、破CD、垃圾、烂靴子、浸水报纸等
  - 藻类：海草、绿藻、白藻等
  - 其他非鱼类可钓物品
  
- **等级上限**：8级（封顶）
  
- **规则应用**：
  - 等级范围：[-10, 8]
  - 达到8级后不再增长（成功时等级保持8）
- **封顶后的成功**：达到8级后后续成功的实际难度收益为 `+0`，不得继续增加底层难度统计。
  - 难度/数量/品质倍数正常计算（基于等级8的倍数）
  - 视觉缩放、NPC反应、星标系统正常工作
  
- **封顶提示**：达到8级后再钓到时显示"对于{物品名}而言，你已是帝王"；达到上限后的成功不再增加底层难度统计。

### 7. 力竭机制、提示双通道与挑战鱼饵（2026-08-10 用户确认，BATCH-038）

#### 7.1 力竭机制（调整后难度 ≥100 的非鱼王）
- **参与条件**：小游戏进入时调整后难度 ≥100 且非鱼王。难度 <100 的鱼不参与，全程保持原生难度。
- **节点与百分比**：1/3/5/7/9/12/15 分钟 → 1%/3%/10%/20%/35%/50%/100%；节点之间线性插值（含 0→1 分钟段）。
- **公式**：`有效难度 = 调整后难度 + (80 - 调整后难度) × 百分比`；15 分钟（100%）时难度降到 80，超过 15 分钟保持 80 不再下降。
- **所有难度同时下降**：难度随节点下降时，鱼的难度档位（跳鱼/星标等判定）整体按有效难度执行；**奖励不下降**——数量/等级/皇冠结算以开局调整后难度快照为准（`GetAdjustedDifficulty` 保持开局值）。
- **计时**：只在小游戏进行中累计（update 驱动，暂停/菜单不计时）。
- **节点提示**：每个节点到达时在“鱼其他提示”通道显示一条随机文案，模板 `[职阶]...`（i18n `hud.exhaust.{1|3|5|7|9|12|15}.{1..10}`，每个节点 10 套诙谐文案，中英双语）；示例：1 分钟“[职阶]势头没那么猛了”、15 分钟“[职阶]已经极度疲惫”。
- **挑战鱼饵共存**：挑战鱼饵生效时难度不衰减（有效难度恒等于开局值），但节点提示仍按时间显示，并在文案后追加一句随机文案（i18n `hud.exhaust.append.{1..10}`，如“但这场对决，它还想继续”）。

#### 7.2 提示双通道（钓鱼小游戏内浮动提示）
- **鱼动作提示**（跳鱼鱼跃/甩尾类，BATCH-034 文案池沿用）：显示 2 秒，起点固定在触发时位置，不再随鱼上下移动；2 秒内慢慢上移 30px 并线性淡出；同通道可同时显示多条。
- **鱼其他提示**：助战、力竭节点等非动作提示改在钓鱼条另一侧显示（左侧锚点 `xPositionOnScreen+44`，绿条中部高度），同样 2 秒内上移 30px 线性淡出、可同时多条。
- **上限**：每通道最多 8 条同时显示，超限丢最旧一条。
- **纯显示**：浮动提示不写任何游戏状态、不入 FIFO 队列、不影响结算。

#### 7.3 脱杆惩罚取消（难度等级 >0）
- **尺寸**：难度等级 >0 时脱杆不再缩小鱼尺寸（原生 800ms 缩水计时器在 Update 边界持续重置）；尺寸数字按结算边界统一应用等级倍率（正等级每级 +10%、负等级每级 -5%、0 级不乘）。
- **品质**：难度等级 >0 时脱杆不再降低品质，按原生完美规则提升（鱼品质 ≥2 → 铱、≥1 → 金，原生已完美时幂等）。
- **等级收益照旧**：脱杆 10/5/2/1 + 额外 `round(调整后难度/50)` 的等级收益规则不变。
- **尺寸修复背景**：100 级狗鱼只有 30 多厘米的根因是原生 `BobberBar` 脱杆 800ms 缩水（`fishSizeReductionTimer`）累计作用 + 尺寸数字未乘等级倍率；难度等级 >0 禁用缩水并在结算边界统一乘倍率，收藏记录尺寸同步（`Farmer.caughtFish` Prefix）。

#### 7.4 万能鱼饵加成（难度等级 >0）
- 难度等级 >0 且原生本应给两条鱼（`numCaught >= 2`，非挑战鱼饵）时，最终数量在原基础上多给 10 条（原生 2 条 → 12 条），再乘等级数量倍数。

#### 7.5 挑战鱼饵改版（调整后难度 >100）
- **5 分钟时限**：5 分钟（300 秒）内成功钓到 → 最终数量 ×1.5（向上取整）再乘等级数量倍数。
- **超时只取消数量加成**：超过 5 分钟只取消 ×50% 数量加成，其他全部照常——等级收益、普通皇冠、流动金色皇冠、品质、尺寸都不受影响（例：30 分钟钓到 98 级鱼仍给流动金色皇冠）。
- **难度不衰减**：挑战鱼饵生效时力竭难度衰减不适用（有效难度恒等于开局值）。
- **无其他鱼助战**：挑战鱼饵生效时不触发皇冠助战。
- **原生脱杆失败禁用**：挑战难度 >100 时原生“3 次脱杆失败”规则禁用，可继续挑战到成功。
- **流动金色皇冠**：难度等级 ≥95 且挑战鱼饵生效时成功（不区分是否超时）→ 该鱼皇冠升级为流动金色（见 4.3）。

#### 7.6 手感优化（BATCH-038 叠加 BATCH-034 α 手感）
- α=1 时定速最大值由 30 减 30% → 21（`30 × (1 - 0.3α)`，随 α 线性）；仍无加速度/条内阻尼/惯性、撞边完全钳制。
- 叠加相对速度系数：绿条中间朝鱼中间移动 ×(1+0.2α)（α=1 → ×1.2），相背离 ×(1-0.2α)（α=1 → ×0.8），随 α 线性。
- α=0 保持原生积分手感不变。

#### 7.7 称号与满皇冠史诗提示
- 100 级称号由“混沌”改为“太一”（i18n `rank.taiyi`，中英双语；99 级仍为创世神）。
- 满皇冠（α≥1）且史诗失败条件成立（同一鱼种连续失败 ≥2 次且调整后难度 ≥150）时，失败提示改为：“你需要进入人鱼合一的状态，你必须是鱼，才能赢下这场挑战。”（不再提示收集更多皇冠）。
