# FishingExpanded — 游戏设计文档

本文件记录用户确认的钓鱼扩展系统完整设计方案，作为实现的唯一权威来源。

## 术语表（BATCH-060，2026-08-15 补充）

| 术语 | 定义 |
|---|---|
| 难度等级 | 每鱼独立统计的成长值，范围 [-10, 100]（非鱼类 [-10, 8]），由成功/失败增减；驱动全部倍率与称号（§1.1） |
| 调整后难度 | 小游戏实际难度 = 原生 difficulty × 难度等级倍数（§1.2）；星标（≥120）、跳鱼（≥150）、力竭/挑战鱼饵（≥100/≥100）等判定均基于它 |
| 有效难度 | 战斗中实时难度 = 调整后难度经力竭衰减后的值（15 分钟降到 80；挑战鱼饵下恒等于开局值）（§7.1） |
| 难度档位 | 按有效难度划分的机制档位（跳鱼间隔 150/250/350/450/550 分档等）（§1.2） |
| 数量倍数 | 最终鱼获条数乘数 = 难度等级 ×1，0 级及以下 = 1（§1.2，BATCH-062；覆盖 BATCH-060 的 round(level×0.5)） |
| 可计数皇冠 | 56 条原生普通真鱼 + 5 条原版传奇鱼（共 61）的皇冠；Mod 鱼/扩展传奇等 61 池外条目图鉴显示星星、不计入 α 与助战（§4.3，2026-08-15 用户指令） |
| 鱼竿熟练度 α | 可计数皇冠数分段线性（0/5/10/20/30/61 → 0%/2%/7%/20%/35%/100%），驱动小游戏手感（§4.3） |
| 挑战星 | 挑战鱼饵的原生 3 星计数；5:00 起每分钟掉 1 颗（≥95 豁免），每颗 −20% 鱼获（§7.5） |
| 背板种子 | 挑战鱼饵下同鱼同等级行为的固定随机种子，成功钓起后清除（§7.10） |

## 核心机制

### 1. 难度等级系统

#### 1.1 统计规则
- **统计粒度**：每种鱼独立统计（按存档+玩家，多人联机不共享）
- **多人部署**：在线联机时主机和每一位农场客都必须安装本 Mod；每位玩家只读取和写入自己的 `Farmer.modData`（`Character.modData` 注册进原生 NetFields，农场客数据同步进主机存档）。
- **双人同屏（本地分屏）**：支持；每位本地玩家是独立 `Game1` 实例（`Game1.player` 随屏幕切换），本 Mod 难度/星标/加成/展示事实一律按玩家 `UniqueMultiplayerID` 隔离，副屏玩家不读写主玩家数据（BATCH-027，2026-08-06 用户确认）。
- **品质合并**：不同品质（普通/银/金/铱）算作同一种鱼
- **等级变化**：
  - 成功钓起：根据脱杆次数加等级
    - 0次脱杆（完美）：+10
    - 1次脱杆：+5
    - 2次脱杆：+2
    - 3次及以上：+1
    - 额外等级收益（2026-08-06 用户确认，BATCH-029）：每次成功额外增加 `round(调整后难度系数 / 50)` 级（四舍五入到整数；例：调整后难度 500 → +10），与脱杆收益叠加后统一按称号区间封顶；减少肝度。
    - 固定钓鱼等级系数（2026-08-16 用户确认，BATCH-067）：脱杆基数与额外等级收益之和整体乘 `max(固定钓鱼等级, 1) × 0.1`——固定钓鱼等级 = 玩家基础钓鱼等级（`Farmer.fishingLevel` 字段，0~10，**不含**食物/饮料 buff 与助战临时等级）；1 级 ×0.1、10 级 ×1.0（现有增长速度 = 满级玩家速度）、0 级钳制 0.1；钓鱼等级越高加得越快。
    - 成功保底（2026-08-16 用户确认，BATCH-067）：乘系数并四舍五入后结果不足 1 时按 +1 计算，成功钓起至少 +1 级（已封顶的鱼仍为 +0，不违反封顶）。
    - 先按上述数值计算本次收益，再应用称号区间边界；收益不能使等级跨越当前区间，但“完美钓鱼 +10”仍是本次计算的原始收益。
    - 89 级为“超高难挑战区间”入口：88 级及以下每次成功最多只能升到 89；89 级以上（含 89）每次成功最多 +6 级（2026-08-12 用户确认 BATCH-049/050 为 +3；2026-08-15 用户确认改为 +6，BATCH-060；仍受 100 封顶约束）。
  - 钓鱼失败：等级 -1（挑战鱼饵下不掉等级，BATCH-058；连续失败计数照常 +1）
- **越级限制**：
  - 一次加等级时，最多只能晋升到下一个称号区间的上限
  - 不允许一次跨越多个称号
  - 示例：
    - 当前等级1（精英）+ 完美钓鱼（+10）→ 最多升到6（骑士上限），不能直接到9（伯爵）
    - 当前等级7（领主）+ 完美钓鱼（+10）→ 最多升到15（伯爵上限），不能直接到17（大公）
  - 称号区间上限表：
    - 弱小(<1) → 精英上限(3)
    - 精英(1-3) → 骑士上限(6)
    - 骑士(4-6) → 领主上限(8)
    - 领主(7-8) → 伯爵上限(15)
    - 伯爵(9-15) → 大公上限(22)
    - 大公(16-22) → 亲王上限(33)
    - 亲王(23-33) → 帝王上限(45)
    - 帝王(34-45) → 神皇上限(66)
    - 神皇(46-66) → 神王上限(88)
    - 神王(67-88) → 龙神王上限(99)
    - 龙神王(89-99) → 太一上限(100)
    - 太一(100) → 太一上限(100)
    
- **等级范围**：
  - 普通鱼类：[-10, 100]
  - 非鱼类（垃圾/藻类等）：[-10, 8]，用户确认的上限为8
  - 鱼王类（传奇鱼）：不使用本系统规则
- **封顶提示**：
  - 普通鱼达到100级：显示特殊提示"你已经成为{鱼名}中的神明，这一刻你是鱼，也是人，更是王。"
  - 非鱼类达到8级：显示"对于{鱼名}而言，你已是帝王"
- **触底提示**：
  - 难度-10时再失败：显示"最平庸的{鱼名}依然太难了，请升级鱼竿，钓鱼等级，使用料理增加钓鱼等级，使用陷阱/软木塞浮标等"

#### 1.2 难度等级效果

##### 负数区间 [-10, 0]（降低难度）
线性插值，所有效果在-10和0之间连续渐变：

**Difficulty 数值调整**：
- -10：difficulty × 0.5
- 0：difficulty × 1.0

**蓄力槽保护**（鱼逃跑时减速）：
| 蓄力槽阈值 | -10时减速倍率 | 0时减速倍率 |
|---|---|---|
| ≤40% | 0.99 | 1.0 |
| ≤20% | 0.60 | 1.0 |
| ≤1% | 0.20 | 1.0 |

示例：难度-5时，蓄力槽40%处减速倍率 = 0.99 + (1.0 - 0.99) × 5/10 = 0.995

**全局蓄力槽保护**（任意非鱼王难度等级，BATCH-020 新增）：
- **适用范围**：所有非鱼王难度等级（包括正数、负数和0）；鱼王类保持完整规则豁免
- **20%处**：蓄力槽减少速度降为原本的 80%（减速倍率 0.80）
- **1%处**：蓄力槽减少速度降为原本的 50%（减速倍率 0.50）
- **与负数保护的叠加规则**：两者同时生效时，取效果更强的（最小倍率）
- **通用冲突规则**：任意其他会降低蓄力槽下降速度的规则同时生效时，统一取效果更强的倍率，即所有适用倍率中的最小值。
  - 示例：负数难度在20%处可能是0.60，全局保护是0.80，最终取0.60
  - 示例：正数难度在20%处负数保护不生效（1.0），全局保护是0.80，最终取0.80

##### 零点（基础难度规则原版一致）
难度等级=0时，基础难度、数量、经验和品质等核心规则与原版一致；全局蓄力槽保护仍然应用。

##### 正数区间 [0, 100]（提升奖励与挑战）
线性插值（difficulty 倍数自 2026-08-10 起为分段线性），所有效果在0和100之间连续渐变：

| 属性 | 0时 | 100时 | 公式 |
|---|---|---|---|
| difficulty倍数 | 1 | 50 | 分段线性锚点：0→×1.0、4→×1.2、20→×5、50→×20、100→×50；每段斜率递增（0~4：0.05/级、4~20：0.2375/级、20~50：0.5/级、50~100：0.6/级），越往后每级加得越多（2026-08-10 用户确认，BATCH-037） |
| 获得数量倍数 | 1 | 100 | 难度等级×1（四舍五入取消；0 级及以下 = 1；100 级 = 100 条）（2026-08-15 用户确认，BATCH-062，覆盖 BATCH-060 的 round(level×0.5)） |
| 经验倍数 | 1 | 20 | 1~10 级：max(1, round(level×0.5))（现状，10 级 = ×5）；10~100 级：线性从 ×5 到 ×20（`5 + (level−10)×15/90`，100 级 = ×20）；负数 = 1（BATCH-068 用户确认：10 难度能力保持 ×5，之后平滑，100 难度等级 ×20；与数量倍数解耦 BATCH-062） |

**经验基数规则（2026-08-16 用户确认，BATCH-068）**：原生经验公式 `经验 = max(1, (品质+1)×3 + 难度/3)`（宝箱 +120%、完美 +140%、Boss ×5，FishingRod 原生 1188-1203）中的**难度输入钳制到 [原生难度, 120]**（120 = 原生最高难度 110 以上一点，用户确认）：
- 传入难度 < 原生难度（负数难度等级 ×0.5~0.95、力竭衰减有效难度降到 80）：按**原生难度**计算——经验不低于未装模组
- 传入难度 > 120（正数高等级调整后难度）：按 **120** 计算——经验基数不随调整后难度爆炸（100 级普通品质基数最多 43，×50 倍数 ≈ 2150）
- 区间内（原生 ≤ 传入 ≤ 120）：按实际调整后难度计算（现状不变）
- 重算后仍乘经验倍数（负数倍数 = 1 自然不变；倍数曲线见上表：10 级 ×5、100 级 ×20 线性）
| 品质提升 | 原品质 | 铱星 | 门槛式“提升到”（非累加）：10 级→银、25 级→金、50 级→铱；最终品质 = max(原品质, 等级门槛)（2026-08-15 用户确认，BATCH-060） |

难度倍数示例：4级=×1.2、20级=×5、50级=×20、100级=×50；10级=×2.625、30级=×10、80级=×38。

**品质提升规则（BATCH-060，2026-08-15 用户确认；取代原 floor(level/5) 累加式）**：
- 达到门槛即“提升到”对应品质，不逐级累加：10 级→银（1）、25 级→金（2）、50 级→铱（4）
- 最终品质 = max(原品质, 等级门槛)；10 级以下不提升（保持原品质）
- 品质映射：0=普通，1=银，2=金，4=铱（跳过3）
- 非鱼类上限 8 级，天然达不到 10 级门槛，无品质提升

##### 高难度鱼跳机制（调整后难度 150+，2026-08-06 用户新增）

**适用范围**：
- 仅非鱼王；鱼王小游戏完整豁免。
- “钓鱼难度”指小游戏实际难度（原版 difficulty × 难度等级倍数，即星标判定用的调整后 difficulty，与等级 -10~100 不是同一数值）。
- 调整后难度 < 150 不参与。

**触发间隔（按调整后难度分档）**：

| 调整后难度 | 触发间隔 |
|---|---|
| 150 ~ 250 | 每 8 秒 |
| 251 ~ 350 | 每 6 秒 |
| 351 ~ 450 | 每 5 秒 |
| 451 ~ 550 | 每 4 秒 |
| 551 及以上 | 每 3 秒 |

**触发行为**：
- 鱼位于鱼可移动范围**下 25% 区域**（底部四分之一）时：0.5 秒后立刻跳到**上 25% 区域**（顶部四分之一内随机位置）。
- 鱼位于**上 25% 区域**时：0.5 秒后立刻跳到**下 25% 区域**（底部四分之一内随机位置）。
- 计时到点但鱼不在上/下 25% 区域：等待触发，统一每 1 秒检测一次，直到鱼进入上或下 25% 区域后按上述 0.5 秒延迟跳。
- 0.5 秒延迟期间鱼自行游出 25% 区域仍照跳（跳向相对方向的 25% 区域）。
- **上一次触发（跳跃完成）后才能开始新的计时**；跳跃为瞬间位移，不改变 fishSize、数量、品质、难度或结算状态。

##### 高难度运动公式修正（调整后难度 >100，2026-08-06 用户新增）

**适用范围**：仅非鱼王；“难度”指小游戏实际难度（原版 difficulty × 难度等级倍数，即调整后 difficulty）。难度 ≤100 时原生运动公式完全不变；鱼王完整豁免。

**修正规则**：
- **初始目标**：难度 >100 时，鱼的初始目标位置固定为顶部（原生公式 `(100-难度)/100×548` 在 >100 时算出负数被钳成“无目标”，修正为顶部）。
- **换目标频率**：难度 >150 时，三处换目标概率一律按 150 计算（150 以内按实际值）：
  - 大范围换目标：`min(难度,150)/4000`（每帧概率）
  - 小偏移换目标（±50~100 像素）：`min(难度,150)/2000`（每帧概率）
  - dart 型鱼额外换目标：`min(难度,150)/1000`（每帧概率），且 dart 偏移量 `±(50~100+难度×2)` 同样按 150 封顶计算（2026-08-06 用户确认）
- **加速度**：难度 >100 时，加速度分母固定为 `随机(10,30)`；高于 100 的部分线性增幅到加速度：`加速度 = 距离/随机(10,30) × (1 + 档位 × (难度-100)/100)`，无上限；档位按难度等级锚点曲线（2026-08-12 用户定稿，BATCH-053，覆盖 BATCH-029/034 的 10%/98 阈值）：0 级 10%、50 级 20%、70 级 40%、80 级 70%、90 级 100%、100 级 100%，点间线性，≤0 钳 10%、≥90 钳 100%（示例：难度 190、等级 80 → ×1.63；等级 ≥90 → ×1.9）。
  - 难度 100~150 区间：换目标频率按实际值（未超 150）、分母为 `随机(10,30)`、增幅 1.0~1.5 线性，与难度 100 处原生公式无缝衔接（100 处原生分母恰为 `随机(10,30)+0`、初始目标恰为顶部）。

**原生代码对齐**（2026-08-06 核对 `_analysis/StardewValley.BobberBar.decompiled.cs`）：
- 原生 `Data/Fish` 行为字段 → motionType：`mixed`=0、`dart`=1、`smooth`=2、`sinker`=3、`floater`=4。
- 本文“dart 型鱼”即原生 `dart`（motionType 1）：`难度/1000` 概率块与偏移 `±(50~100+难度×2)` 是该类型专属；原生 `floater`（4）/`sinker`（3）不参与，只额外有恒速漂移。
- 大范围换目标块（`难度/4000`）对所有类型生效，但 `smooth`（motionType 2）频率 ×20 且仅当当前目标为 -1 时触发；×20 与本条封顶同属同一公式（封顶后为 150×20/4000），无需单独处理。
- 小偏移块（`难度/2000`，±50~100）对除 `smooth` 外的类型生效。
**与鱼跳机制的叠加**：公式修正接管鱼在两次跳跃之间的原生运动表现；鱼跳机制按各自分档间隔强制瞬移，两者互不冲突、互不修改对方状态。

##### 手感系统（玩家操作手感，2026-08-09 用户确认，BATCH-034）

**进度来源（α）**：鱼竿熟练度 α = 可计数皇冠数分段线性曲线（2026-08-10 用户确认，BATCH-039）：0 皇冠→0%、5→2%、10→7%、20→20%、30→35%、61→100%，点位之间线性，超过 61 钳制 100%（61 = 56 条原生普通真鱼 + 5 条原版传奇鱼）。其他 Mod 鱼获得的皇冠只显示、不计入 α；原版 5 条传奇鱼钓到一次直接给皇冠并计入 α。α=0 时小游戏行为与原生完全一致；α=1 为终点手感；中间按该曲线插值。

**终点手感（α=1）**：
- 按下：绿条速度直接变为 25 px/帧（向上）；松开：直接变为 25 px/帧（向下）；无加速度过程、无条内阻尼、无惯性。
- 撞边：完全钳制（贴边停住、速度归零、不反弹）。
- 条内阻尼（原生 ×0.6 / 陷阱浮标 ×0.3）与陷阱浮标牵引随 α 线性取消。

**线性分配**：
- 输入响应：`速度 = (1-α) × (原速度 + 原生加速度×帧缩放) + α × 终点速度`（α=0 恰为原生积分，α=1 恰为直接定速，中间连续过渡）。
- 撞边：反弹保留系数 `2/3 × (1-α)`（α=0 原生反弹，α=1 完全钳制）。

**帧率解耦（整个钓鱼小游戏）**：绿条运动、鱼运动、平滑、随机概率全部改为 delta-time 基准，60fps 下与原版行为完全一致（逐帧增量乘 `dt×60`；概率按 `1-(1-p)^(dt×60)` 换算；平滑按 `1-0.8^(dt×60)` 换算，60fps 时精确等于原生 0.2）。蓄力槽进度与宝箱进度保持原生逐帧（60fps 基准）不在此列。

**高难度鱼跳文案**：150+ 跳鱼机制保持；每次瞬移在鱼旁边显示文案——向上跳（目标区 0~133）随机显示 15 条“鱼跃”类文案，向下跳（目标区 399~532）随机显示 15 条“甩尾！”类文案；文案 5 秒内上移 30px 并线性淡出（2026-08-09 用户确认；2026-08-10 BATCH-039 绿条旁提示统一延长至 5 秒）。

**视觉尺寸**：
- fishSize数值 = 原值 × 等级尺寸倍率（2026-08-06 用户确认，BATCH-029）：正等级每级 +10%（100级=11×）、负等级每级 -5%（-10级=0.5×，防御钳制 ≥0.1）、0级=1×；与数量倍数解耦，数量倍数只用于鱼获数量
- 绘制缩放 = 原缩放 × 线性值：`缩放 = 1 + 难度等级 × 0.0270843`，即 0级=1.0、100级≈3.7084（= 原25级大小 51^(1/3)）（2026-08-06 用户确认 BATCH-026 端点；2026-08-15 用户确认 BATCH-060 与数量倍数解耦、改按难度等级线性）

**视觉锚点合同**：
- 手持鱼：以鱼图源矩形的底边中点为锚点。放大后底边中点保持在原生绘制的同一位置，鱼图向上和左右扩展。
- 钓鱼小游戏：鱼标保持原生大小和中心位置，不做视觉缩放（2026-08-06 用户确认）。
- 钓鱼结算面板：面板内展示鱼名与尺寸的示意图保持原生大小，不做视觉缩放（2026-08-06 用户确认）。
- 钓鱼结算真鱼：玩家身边从水里钓起并举起的真鱼（结算第二处鱼图及原生多鱼图，原生位置 `(0,-56)`、原点 `(8,8)`、`3f`，点击后 `doneHoldingFish` 收包）以每一张鱼图源矩形的底边中点为锚点放大；放大后鱼图底边中点保持在原生基准位置；原生多鱼机制产生的多张鱼图仍各自使用底边中点锚点。
- 从水里飞向玩家的飞行动画：放大，以每一张鱼图源矩形的正中央为锚点；放大后鱼图中心保持在原生飞行轨迹上。原生多鱼机制仍保持原生数量与飞行动画数量。
- 锚点只影响绘制位置和缩放，不改变fishSize、鱼获数量、动画数量、品质或结算状态。

### 2. HUD 提示系统

#### 2.1 成功钓起提示
- 普通鱼未封顶时：左下角显示"下一次你将向{鱼名}中的{称号}发起挑战"
- 当本次成功后的难度等级小于1（0 级和负数）时，仍显示上述提示，称号为“额...稍微强一点的个体”（2026-08-07 用户确认：零以下胜利应显示弱称号提示，取代旧设计“小于等于0不显示”；BATCH-030）。
- 普通鱼已达到100级，或100级封顶后再次成功时：左下角显示"你已经成为{鱼名}中的神明，这一刻你是鱼，也是人，更是王。"
- 非鱼类已达到8级，或8级封顶后再次成功时：左下角显示"对于{鱼名}而言，你已是帝王"
- 鱼王类：随机显示10种独特文案之一，例如"嗷！孤傲的王！"

**称号对应表**：
| 难度等级 | 称号 |
|---|---|
| <1 | 额...稍微强一点的个体 |
| 1-3 | 精英 |
| 4-6 | 骑士 |
| 7-8 | 领主 |
| 9-15 | 伯爵 |
| 16-22 | 大公 |
| 23-33 | 亲王 |
| 34-45 | 帝王 |
| 46-66 | 神皇 |
| 67-88 | 神王 |
| 89-99 | 龙神王 |
| 100 | 太一（2026-08-10 由“混沌”改名，BATCH-038） |

#### 2.2 失败提示
- 等级<0（非触底）："你对{鱼名}的了解加深了"，**括号中显示难度绝对值**（如等级-5显示"（5）"）
- 等级≥0："还是挑战{鱼名}中更平庸的个体吧"
- 连续失败史诗提示（2026-08-06 用户确认，BATCH-029）：同一鱼种连续失败 ≥2 次（成功清零计数）且本次小游戏调整后难度 ≥150 时，失败提示替换为："收集更多徽章（鱼类收集品页的皇冠），获得更多加成，再挑战史诗级强者吧"；第 1 次失败、调整后难度 <150 或鱼王仍显示普通失败提示。
- 满皇冠史诗替换（2026-08-10 用户确认，BATCH-038）：史诗条件成立且玩家已获得全部可计数皇冠（α≥1）时，不再提示收集更多皇冠，改为："你需要进入人鱼合一的状态，你必须是鱼，才能赢下这场挑战。"（i18n `hud.fail.union`）；未满皇冠仍显示旧史诗文案（`hud.fail.epic`）。
- 等级=-10（触底）："最平庸的{鱼名}**（10）**依然太难了，请升级鱼竿，钓鱼等级，使用料理增加钓鱼等级，使用陷阱/软木塞浮标等"
- **提示优先级**（多条件同时满足时按此顺序显示，BATCH-031 设计核对确认与代码 ShowFailureNotification 一致）：史诗提示 > 触底提示 > 负等级提示 > 普通失败提示。


#### 2.3 HUD 提示排队（2026-08-08 用户确认）
- 所有本 Mod 左下角 HUD 提示统一经 FIFO 队列显示：同一屏幕同一时间只显示一条本 Mod 提示。
- 后续提示排队等待：前一条从原生 `Game1.hudMessages` 消失后自动显示下一条（每条保持原生默认显示时长，约 3.5 秒淡出）。
- **覆盖范围**：成功提示、失败提示、等级建议、星标挑战宣言、星之果茶掉落消息，全部排队。
- **队列上限**：5 条；满时丢弃最旧一条（保留最新）。
- 只排队本 Mod 自己的提示；原生或其他 Mod 的 HUD 消息不受影响、不参与排队。
- **例外（BATCH-038）**：钓鱼小游戏内的浮动提示（鱼动作提示/鱼其他提示）不经 FIFO 队列，在钓鱼条两侧独立显示（见第 7 节）；成功/失败/等级建议/星之果茶等左下角消息仍排队。
- 双人同屏：各屏幕按玩家独立排队（BATCH-027 模式）；在线联机每位本地玩家各自排队。
- 返回标题或切换存档时清空队列。
### 3. 超大鱼展示系统

#### 3.1 触发条件（三个条件同时满足）
1. 鱼在工具栏被选中且举起（手柄仅选中不举起不算）
2. 难度等级 ≥ 8（2026-08-15 用户确认，BATCH-060：巨型鱼与数量倍数解耦、直接按难度等级判定；原“数量倍数 >15”等价于等级 ≥8）
3. 玩家自钓到该鱼后尚未进入 FarmHouse 室内

#### 3.2 NPC 反应规则
**冒泡反应**：
- 触发范围：玩家5格内的NPC
- 显示方式：头顶冒泡，随机100种文案
- 文案要求：提及鱼种类 + 具体尺寸，显示尺寸（英文模式=原生英寸数字 + in.；其他语言=厘米 fishSize×2.54 取整；BATCH-033 厘米、BATCH-073 英文英寸）
- 频率限制：每个NPC在每个超大鱼会话内最多冒泡1次（进入 FarmHouse 室内或换日后开启新会话，BATCH-074）
- **动物 NPC（BATCH-058/073）**：NPC 类型为宠物（`StardewValley.Characters.Pet`，按 `petType` 区分狗/猫）或马（`Horse`）时，先显示动物叫声，再把赞美内容放在括号里；中文示例“汪汪！！！（这条狗鱼竟然有388cm简直是奇迹）”，英文模式用英文拟声词（如 Neigh!!!）；中/英各有多种随机叫声。只作用于头顶冒泡，动物无主动对话。

**主动对话**：
- 首次对话：替换为赞美文案（与冒泡使用相同文案池）
- 二次对话：恢复原对话
- 频率限制：每个NPC在每个超大鱼会话内最多替换对话1次（进入 FarmHouse 室内或换日后开启新会话，BATCH-074）
- 计数独立：冒泡和对话分别计数

**重复触发**：
- 同一NPC在同一超大鱼会话内对不同种类超大鱼可分别触发
- 同一种类鱼在同一超大鱼会话内只触发一次；进入 FarmHouse 室内或换日后重置，可再次触发（BATCH-074）

#### 3.3 尺寸重置规则
- 玩家进入 FarmHouse 室内后，当前持有的所有超大鱼永久失去放大效果，同时重置 NPC 冒泡/对话触发记录（BATCH-074）
- 下次钓到新的超大鱼时，重新计算倍数
- 换日（DayStarted）同样重置超大鱼展示与 NPC 赞美记录，等价于进入 FarmHouse（BATCH-074）

### 4. 收藏与永久加成

#### 4.1 触发条件
- 普通鱼（含扩展传奇 898–902 与 Mod 鱼）：成功钓起且该鱼的**调整后difficulty ≥ 120**
- 原版 5 条传奇鱼（159/160/163/682/775）：钓到一次直接获得（无难度门槛，2026-08-09 用户确认）

示例：
- 原始difficulty = 3
- 难度等级 = 100（倍数50）
- 调整后difficulty = 3 × 50 = 150 ≥ 120 ✓

#### 4.2 收藏星标
- 位置：Collections 菜单中该鱼图标的左上角
- 样式（2026-08-15 用户指令，BATCH-064-2/3）：
  - 可计数 61 原生鱼：金色皇冠标记（原生 `Characters\Farmer\hats` 的 Infinity Crown 贴图，源矩形 (20,800,20,20)，左上角绘制 24×24；2026-08-06 用户确认星标改皇冠，BATCH-029）
  - 非鱼类与其他 Mod 鱼（61 池外）：早期金星——`Game1.mouseCursors` 源矩形 (346,392,8,8)，图标左上角 +3/+3 处 20×20 `Color.Gold`（原 BATCH-016 画法，git 0020e30）
- 持久化：保存到存档数据

#### 4.3 皇冠与鱼竿熟练度（2026-08-09 用户确认，BATCH-034；2026-08-10 改名与曲线调整，BATCH-039）
- 皇冠来源：① 可计数 61 原生鱼（56 普通 + 扩展传奇 898–902）成功钓起且本次调整后 difficulty ≥120；② 原版 5 条传奇鱼钓到一次直接给皇冠。
- 鱼竿熟练度 α = 可计数皇冠数分段线性（0→0%、5→2%、10→7%、20→20%、30→35%、61→100%，点间线性，超 61 钳 100%，BATCH-039）；可计数 = 56 条原生普通真鱼 + 5 条原版传奇鱼；非鱼类与其他 Mod 鱼获得星星，不计入 α（2026-08-15 用户指令）。
- 隐藏钓鱼等级加成已整体删除（原每皇冠 +0.2，BATCH-034）。
- 流动金色皇冠（2026-08-10 用户确认，BATCH-038；BATCH-058 掉星后仍照常）：难度等级 ≥95 的鱼在挑战鱼饵生效的情况下成功钓起（无论是否超过 5 分钟时限，超时只按掉星扣数量），该鱼皇冠升级为流动效果的金色（Collections 页金/白呼吸 + 1±0.05 缩放脉冲）；独立存档字段 `ChallengeCrowns`，旧存档缺失默认空。

#### 4.4 鱼图鉴显示增强
- 位置：Collections 菜单中每种鱼的描述文本
- 新增一行显示：
  - "挑战等级：{称号}（{X}级）"
- 示例："挑战等级：伯爵（12级）"
- 当前难度等级小于等于0时，不显示“挑战等级”一项。
- 有皇冠的可计数 61 原生鱼追加显示其提供的加成："鱼竿手感增强"（i18n `collections.crownControl`，中英双语；2026-08-09 用户确认，BATCH-036；2026-08-10 改名，BATCH-039；2026-08-14 文案改“鱼竿手感增强/Improved rod handling”，BATCH-058T——机制名“鱼竿熟练度 α”保留，图鉴展示文案改为玩家可感知的手感描述，不再使用不可见数值“+1”）。纯展示，不新增状态；鱼王豁免挑战等级但皇冠行照常显示（原版 5 传奇一次钓获直接给皇冠，BATCH-034）。
- 非鱼类与其他 Mod 鱼（星星）追加显示："无手感增强"（i18n `collections.noCrownControl`；2026-08-15 用户指令，BATCH-064-3）。

#### 4.5 星标鱼开局挑战宣言
- 触发条件：钓鱼小游戏出现时，当前鱼种已经拥有图鉴星标；鱼王不可能获得星标，因此不触发本规则。
- 显示位置：左下角 HUD，且每个小游戏实例只显示一次。
- 文案结构：`{尊敬词}{鱼名}{鱼的等级}{毅然决然词}{应战说法}。`（等级 <1 的弱称号除外，见下）
- 等级 <1 的鱼：宣言不包含“额...稍微强一点的个体”称号文本，只显示 `{尊敬词}{鱼名}{毅然决然词}{应战说法}。`（该称号只用于零以下胜利提示；2026-08-07 用户确认，BATCH-030）。
- `{尊敬词}` 根据当前难度等级对应的称号选择，每个称号提供3种随机替换。
- `{毅然决然词}` 从50种随机替换中选择；`{应战说法}` 从20种随机替换中选择，三部分独立随机。
- 该提示只展示当前权威难度事实，不修改难度、星标、经验或其他持久数据。

#### 4.6 小游戏出现时的钓鱼等级建议
- 触发时机：钓鱼小游戏出现后，在小游戏构造边界读取当前玩家的钓鱼等级（BATCH-034：隐藏加成已整体删除，使用原生钓鱼等级）。
- 鱼王类完全豁免本规则，不显示建议或“不可能”提示。
- 当 `难度等级 / 10 > 玩家钓鱼等级` 时，左下角显示：`建议达到{难度等级/10}以上钓鱼等级再挑战，同时使用食物增加钓鱼等级，升级鱼竿并使用陷阱浮标/软木塞浮标。`
- 当 `难度等级 / 10 > 玩家钓鱼等级 × 2` 时，在上述建议文案开头额外加上：`这是不可能的高难度挑战！`
- `{难度等级/10}` 使用当前难度等级除以10后的数值显示，保留必要的小数位，不把比较值向下取整。
- 该规则只显示提示，不修改钓鱼等级、难度、鱼获、经验或蓄力槽。

#### 4.7 皇冠助战（2026-08-09 用户确认，BATCH-035）
- **触发**：非鱼王钓鱼小游戏构造时判定一次（一次小游戏最多一条助战鱼）；鱼王完整豁免。
- **概率**：总助战概率 = 10% × (可计数皇冠数 ÷ 61)（无皇冠=0%、满 61=10%）+ 每条可计数流动金冠鱼 +0.1% + 每条可计数增大流动金冠鱼（100 级 1.2 倍；困难模式 1.3 倍，BATCH-074）再 +0.05%；Mod 鱼皇冠只显示、不计入；不设 100% 上限，由可计数 61 条皇冠上限自然限制（BATCH-073，2026-08-18 用户确认）。
- **选鱼**：命中后从玩家可计数皇冠鱼中按权重选一条——普通皇冠鱼权重 1，流动金冠鱼权重 +0.1%，增大流动金冠鱼权重再 +0.05%（即 1 / 1.001 / 1.0015，归一化选择；BATCH-073 用户确认金冠鱼触发后也更容易被选中，替代 BATCH-035 的均匀随机）。
- **效果**：临时钓鱼等级 = 0~40，**随被选中助战鱼的难度排位 r∈[0,1] 线性倾斜**（权重 w(L)=1+s·(L-20)/20，s=-29/31+58r/31；2026-08-09 用户澄清：所有皇冠鱼被选中概率相同，但等级随机且最高难度鱼更可能给高级助战）：最高难度鱼 P(40)≈4.72%、P(0)≈0.16%、平均 26.55；最低难度鱼镜像 P(0)≈4.72%、P(40)≈0.16%、平均 13.45；40 级概率最高鱼=最低鱼 30 倍；最低+最高两条鱼平均助战等级恰为 20；中位鱼=均匀；最低鱼也可能抽到 40 级。只把本次小游戏绿条高度 +临时等级×8px（原生公式 `bobberBarHeight = 96 + 钓鱼等级×8` 的增量，保留训练鱼竿与浮标原生计算），**不修改玩家钓鱼等级字段、不入存档、不影响经验/数量/品质/难度**。
- **文案**：触发时在“鱼其他提示”通道（钓鱼条左侧，见第 7 节）显示 20 条随机文案（i18n `hud.assist.1~20`，中英双语，含鱼名+鱼职阶称号，如“荣耀的{{fishName}}{{rankName}}前来护驾！”），**助战文案显示 15 秒**（15 秒内上移 30px 并线性淡出；2026-08-15 用户确认，BATCH-060，仅助战文案延长，其余提示仍 5 秒），纯显示不改变任何状态（BATCH-038：由“1 秒停留+0.5 秒淡出”改为浮动提示通道）。
- **尺寸口径不变**：显示的鱼尺寸保持原生口径（英语英寸、其他语言厘米，BATCH-033），助战不参与尺寸计算。
- **测试入口**：`fish_bonus` 显示当前助战概率；`fish_assist` 强制下一次小游戏触发助战（薄控制，不写存档）。

#### 4.8 自定义钓鱼称号（BATCH-073，2026-08-18 用户确认）
- `ModConfig.CustomFishingTitle`：只在 `config.json` 手动编辑，不注册到 GMCM。
- 空/纯空白 = 继续使用 i18n 称号；非空 = 替代所有玩家可见的鱼职阶称号（成功提示、挑战宣言、图鉴挑战等级、助战/持久战奖励/巅峰/力竭提示、控制台日志）。
- 内部等级、越级限制、尊敬词仍按原 rankKey 逻辑，只替换可见称号文本。


### 5. 物品处理

#### 5.1 钓鱼动画与数量转化
- **视觉表现**：钓鱼时始终显示为钓到1条鱼的动画
- **数量转化时机**：鱼进入背包或溢出界面时才应用数量倍数
- **实现边界**：数量倍数在原生 `FishingRod.CreateFish()` 创建最终物品后应用，因此背包和原生 `ItemGrabMenu` 共用同一个物品，不建立第二套溢出逻辑。
- **原生机制兼容**：
  - 游戏原生鱼饵可能钓到多条鱼（如海泡鱼饵）
  - 原生多条鱼显示多个飞行动画
  - 本Mod的倍数机制不影响原生多条鱼功能
  - 特殊情况：原生机制钓到2条 × Mod倍数10 = 最终20条

#### 5.2 多条鱼堆叠
- 按原版堆叠规则处理（同种类同品质可堆叠）
- 堆叠上限：参考原版逻辑（通常为999）

#### 5.3 背包溢出
- **原生边界**：背包满时由原生 `ItemGrabMenu` 接管溢出鱼，让玩家选择如何处理
- **数量时机**：数量倍数在进入背包或进入溢出界面前完成转换；不得因数量倍数而丢失鱼获
- **本Mod继承原生行为**：当数量倍数很高（如100倍）导致背包装不下时，继续使用原生菜单，不建立第二套溢出界面


#### 5.4 星之果茶掉落（2026-08-08 用户新增）
- **触发**：普通鱼（非鱼王）成功钓起，且该鱼本次小游戏进入时的难度等级 ≥ 50。
- **概率**：`难度等级 / 4`%（即 等级 / 400）；示例：50 级 → 12.5%、80 级 → 20%、100 级 → 25%。
- **掉落**：1 瓶星之果茶（`(O)StardropTea`），走成功结算唯一边界（`Farmer.caughtFish` Postfix，与等级/星标同一防重 `SuccessRecorded`）；背包有空位直接入包，背包满时由原生 `addItemByMenuIfNecessary` 溢出菜单处理。
- **提示**：掉落时额外显示一条左下角消息，15 条文案随机（中英文 i18n，`hud.starfruitTea.1` ~ `.15`），文风幽默、符合游戏场景；该消息与其他提示一起排队。
- **豁免**：鱼王完整豁免；非鱼类上限 8 达不到 50 级，天然不参与。
- **多人**：掉落判定与发放只对本地玩家（`IsLocalPlayer` 边界）。
## 实现优先级

### Phase 1：核心机制（必须）
1. 难度等级统计与存档持久化
2. Difficulty 数值动态调整
3. 获得数量/经验/品质计算
4. HUD 提示系统

### Phase 2：视觉与交互（重要）
1. 超大鱼举起动画缩放
2. NPC 冒泡反应（100种文案）
3. NPC 对话替换
4. Collections 星标显示

### Phase 3：高级功能（可选）
1. ~~钓鱼等级隐藏加成~~（BATCH-034 已整体删除）
2. 蓄力槽保护机制（负数难度）
3. 尺寸重置判定（进入FarmHouse）

## 已确认的原生行为（反编译审计结果）

### ✅ 多条鱼处理逻辑
**位置**：`FishingRod.pullFishFromWater()` → `doPullFishFromWater()`

**机制**：
- 通过 `numberOfFishCaught` 参数控制数量
- 如果 `numberOfFishCaught > 1`，创建多个 `TemporaryAnimatedSprite` 飞行动画
- 每个额外的鱼延迟 `(i-1) × 100ms` 播放动画
- 最终调用 `Farmer.caughtFish(fishId, fishSize, fromFishPond, numberOfFishCaught)` 创建物品

**溢出处理**：
- 以用户确认的设计合同为准：背包满时由原生 `ItemGrabMenu` 处理溢出鱼
- 不得把“直接丢失”作为已确认行为；若运行证据显示丢失，必须重新开启独立 Case

### ✅ NPC 气泡系统
**API 入口**：`NPC.doEmote(int emoteIndex)` 和 `NPC.showTextAboveHead(string text)`

**重要发现**：
- `doEmote()` 只能显示预定义的表情图标（12=爱心, 28=惊讶等）
- **自定义文本必须使用 `showTextAboveHead()`**
- 或者修改 `NPC.CurrentDialogue` 实现对话替换

### ✅ 蓄力槽减速机制
**位置**：`BobberBar.update()` 中的蓄力槽计算

**关键字段**：
- `distanceFromCatching`：蓄力槽进度 [0, 1]
- `distanceFromCatchPenaltyModifier`：减速倍率（默认1.0）
- `bobberInBar`：鱼是否在绿条内

**减速公式**：
```csharp
distanceFromCatching -= 0.003f * distanceFromCatchPenaltyModifier;
```

## 状态边界

本文件只定义用户确认的游戏设计，不再维护逐批次的实现状态。当前构建、部署、实测和待验收状态统一读取 `BUG-LEDGER.md` 与 `MAINTENANCE-INDEX.md`；旧版本状态保留在历史报告中作为证据，不作为当前结论。

---

## 6. 特殊鱼类规则

### 6.1 鱼王类（传奇鱼）
- **适用鱼种**：五大传奇鱼（不含扩展传奇鱼）
  - 传奇鱼 (Legend) - ID: (o)163
  - 突变鲤鱼 (Mutant Carp) - ID: (o)682
  - 鮟鱇鱼 (Angler) - ID: (o)160
  - 冰川鱼 (Glacierfish) - ID: (o)775
  - 赤红鱼 (Crimsonfish) - ID: (o)159
  
- **规则豁免**：
  - 不使用难度等级系统（不记录成功/失败）
  - 不应用数量/品质/难度倍数
  - 其他鱼类的钓鱼等级加成对鱼王无效
  - 不显示挑战称号或等级提示
  - 不触发视觉缩放、NPC反应

- **全局保护边界**：鱼王不使用20%/1%蓄力槽保护。当前代码让鱼王在 `BobberBar` 构造边界完整退出自定义实例状态，继续保持这条完整豁免路径可以避免新增状态、清理和特殊更新分支；全局保护的“任意难度等级”指所有非鱼王难度等级。
  
- **独特提示**：钓到鱼王时，随机显示10种独特文案之一
  1. "嗷！孤傲的王！"
  2. "传说中的巨兽，终于现身！"
  3. "这是属于你的荣耀时刻！"
  4. "世间仅此一只，已入你囊中！"
  5. "山巅之王，俯首称臣！"
  6. "传说落幕，新的传奇诞生！"
  7. "你已成为渔夫中的传奇！"
  8. "王者归来，唯你独尊！"
  9. "这一刻，你是鱼，也是王！"
  10. "传说终结于此，荣耀属于你！"
- **皇冠**：钓到一次直接给皇冠并计入鱼竿熟练度 α（2026-08-09 用户确认；2026-08-10 改名，BATCH-039）。

### 6.2 非鱼类（垃圾/藻类）
- **适用物品**：Category不等于-4的可钓物品
  - 垃圾：破报纸、破CD、垃圾、烂靴子、浸水报纸等
  - 藻类：海草、绿藻、白藻等
  - 其他非鱼类可钓物品
  
- **等级上限**：8级（封顶）
  
- **规则应用**：
  - 等级范围：[-10, 8]
  - 达到8级后不再增长（成功时等级保持8）
- **封顶后的成功**：达到8级后后续成功的实际难度收益为 `+0`，不得继续增加底层难度统计。
  - 难度/数量/品质倍数正常计算（基于等级8的倍数）
  - 视觉缩放、NPC反应、星标系统正常工作
  
- **封顶提示**：达到8级后再钓到时显示"对于{物品名}而言，你已是帝王"；达到上限后的成功不再增加底层难度统计。

### 6.3 蟹笼物品（2026-08-15 用户确认，BATCH-065）

- **适用物品**：蟹笼（CrabPot）里拿到的所有物品——蟹笼鱼（Data/Fish 带 `trap` 标签、原生 Category=-4：龙虾 715、淡水龙虾 716、螃蟹 717、蛤 372、鸟蛤 718、蚌 719、虾 720、蜗牛 721、玉黍螺 722、牡蛎 723；其他 Mod 的蟹笼鱼同样按 trap 标签覆盖）与蟹笼垃圾（168-172）等。
- **规则口径**：一律按 §6.2 水藻类（非鱼）规则处理——等级范围 [-10, 8]、上限 8 级、8 级给图鉴星星（金星 + "无手感增强"）、封顶显示"对于{物品名}而言，你已是帝王"。
- **等级增长**：每次收获 = 一次成功，等级 +1（蟹笼没有小游戏/脱杆/失败概念，只有成功收获一种结果；背包满收获取消不算成功）。
- **数量倍数**：收获时应用数量倍数（BATCH-062 公式 = 难度等级×1，0/负=1；8 级=×8），与原生书《Crabbing》25%×2 叠加（8 级 + 书双倍 = ×16）。倍数在物品入包前应用，背包放不下时按原生规则收获取消（红字提示，等级不结算）。
- **提示**：左下角提示只在等级实际提升时显示（升到 1 级"下一次你将向{物品名}中的{称号}发起挑战"、首次满 8 级"你已是帝王"），平时收获不刷屏。
- **不应用**：小游戏难度倍数（蟹笼无小游戏）、品质提升（非鱼上限 8 达不到 10 级门槛，天然无）、经验倍数（蟹笼经验为原生固定 5 点，不属于钓鱼结算经验流）、每日收获限额（BATCH-061 的 333/天：蟹笼每笼每天 1 次，全图笼子数量有自然上限，远达不到 333）。
- **多人/分屏**：只结算操作玩家（`who.IsLocalPlayer`），难度/星标按玩家隔离；与钓鱼共用同一存档边界（`FishDifficultyData`）。

### 7. 力竭机制、提示双通道与挑战鱼饵（2026-08-10 用户确认，BATCH-038）

#### 7.1 力竭机制（调整后难度 ≥100 的非鱼王）
- **参与条件**：小游戏进入时调整后难度 ≥100 且非鱼王。难度 <100 的鱼不参与，全程保持原生难度。
- **节点与百分比**：1/3/5/7/9/12/15 分钟 → 1%/3%/10%/20%/35%/50%/100%；节点之间线性插值（含 0→1 分钟段）。
- **公式**：`有效难度 = 调整后难度 + (80 - 调整后难度) × 百分比`；15 分钟（100%）时难度降到 80，超过 15 分钟保持 80 不再下降。
- **所有难度同时下降**：难度随节点下降时，鱼的难度档位（跳鱼/星标等判定）整体按有效难度执行；**奖励不下降**——数量/等级/皇冠结算以开局调整后难度快照为准（`GetAdjustedDifficulty` 保持开局值）；经验基数按 BATCH-068 钳制规则处理（难度输入 ∈ [原生难度, 120]，有效难度低于原生难度时按原生难度重算，经验不低于未装模组）。
- **计时**：只在小游戏进行中累计（update 驱动，暂停/菜单不计时）。
- **节点提示**：每个节点到达时在“鱼其他提示”通道显示一条随机文案，模板 `[职阶]...`（i18n `hud.exhaust.{1|3|5|7|9|12|15}.{1..10}`，每个节点 10 套诙谐文案，中英双语）；示例：1 分钟“[职阶]势头没那么猛了”、15 分钟“[职阶]已经极度疲惫”。
- **挑战鱼饵共存**：挑战鱼饵生效时难度不衰减（有效难度恒等于开局值），但节点提示仍按时间显示，并在文案后追加一句随机文案（i18n `hud.exhaust.append.{1..10}`，如“但这场对决，它还想继续”）。

#### 7.2 提示双通道（钓鱼小游戏内浮动提示）
- **鱼动作提示**（跳鱼鱼跃/甩尾类，BATCH-034 文案池沿用）：显示 5 秒，起点固定在触发时位置（鱼位置上方 30px），不再随鱼上下移动；5 秒内慢慢上移 30px 并线性淡出；同通道可同时显示多条。锚点写死为距绿条的绝对像素——左缘距绿条右缘 24px、左对齐（BATCH-058K/058N 用户定稿）。
- **鱼其他提示**：助战、力竭节点、30 秒巅峰提示等非动作提示右缘距绿条左缘 50px、右对齐（绿条中部高度），同样上移 30px 线性淡出、可同时多条（BATCH-039 统一 5 秒；BATCH-058N 用户定稿 50px）。**例外（BATCH-060，2026-08-15 用户确认）**：助战文案显示 15 秒（15 秒内上移 30px 并线性淡出），其余其他提示仍 5 秒。
- **字体（BATCH-058C/058G/058H/058K/058L/058M/058N/070）**：两通道提示用星露谷 `dialogueFont`（原尺寸），白字 + 右下 2px 黑色软阴影（α0.65）+ 非常淡的细描边（行动=淡蓝、其他=淡红，4 向，向白色混合 85%）；长文本按 UI 视口宽度的 1/3 折行（英文按词、中文按字符；BATCH-070 用户定稿，替换原 420px 写死值），每段最多 3 行，超长内容拆成“续集提示”排队显示（前一条结束后才出现，不丢内容）；距绿条距离写死：动作 24px、其他 50px。
- **屏幕边缘侧翻（BATCH-058O）**：绿条可停靠屏幕任意横向位置；当原定一侧放不下文字时，提示自动翻到绿条另一侧（动作提示翻到左侧=右缘距绿条左缘 50px；其他提示翻到右侧=左缘距绿条右缘 24px），与原生鱼图标翻转同思路；仍保留 8px 屏幕边距兜底，避免提示被推到屏幕最右缘。
- **上限**：每通道最多 8 条同时显示，超限丢最旧一条。
- **纯显示**：浮动提示不写任何游戏状态、不入 FIFO 队列、不影响结算。

#### 7.3 脱杆惩罚取消（难度等级 >0）
- **尺寸**：难度等级 >0 时脱杆不再缩小鱼尺寸（原生 800ms 缩水计时器在 Update 边界持续重置）；尺寸数字按结算边界统一应用等级倍率（正等级每级 +10%、负等级每级 -5%、0 级不乘）。
- **品质**：难度等级 >0 时脱杆不再降低品质，按原生完美规则提升（鱼品质 ≥2 → 铱、≥1 → 金，原生已完美时幂等）。
- **等级收益照旧**：脱杆 10/5/2/1 + 额外 `round(调整后难度/50)` 的等级收益规则不变。
- **尺寸修复背景**：100 级狗鱼只有 30 多厘米的根因是原生 `BobberBar` 脱杆 800ms 缩水（`fishSizeReductionTimer`）累计作用 + 尺寸数字未乘等级倍率；难度等级 >0 禁用缩水并在结算边界统一乘倍率，收藏记录尺寸同步（`Farmer.caughtFish` Prefix）。

#### 7.4 万能鱼饵加成（难度等级 >0）
- 难度等级 >0 且原生本应给两条鱼（`numCaught >= 2`，非挑战鱼饵）时，最终数量在原基础上多给 10 条（原生 2 条 → 12 条），再乘等级数量倍数。

#### 7.5 挑战鱼饵改版（调整后难度 >100）
- **5 分钟时限**：5 分钟（300 秒）内成功钓到 → 最终数量 ×1.5（向上取整）再乘等级数量倍数。
- **超时掉星（BATCH-058）**：5:00 起每分钟掉 1 颗原生挑战星（5:00→2、6:00→1、7:00→0，不可恢复；难度等级 ≥95 豁免），每掉 1 颗最终鱼获 −20%（0.8/0.6/0.4，作用于乘完等级倍数后的最终数量，四舍五入并兜底 ≥1 条——BATCH-067 回归设计口径；原生挑战鱼饵必给 3 条，恒 ≥1 条不归零）；5 分钟内成功仍 ×1.5。等级收益、普通皇冠、流动金色皇冠、品质、尺寸都不受影响（例：30 分钟钓到 98 级鱼仍给流动金色皇冠）。
- **掉星提示（BATCH-060，2026-08-15 用户确认）**：掉星瞬间在左下角 FIFO 队列显示一条提示（小游戏期间可见），告知剩余星数与本次鱼获减少百分比（i18n `hud.starLoss`，中英双语）；≥95 豁免不掉星，天然不触发。
- **难度不衰减**：挑战鱼饵生效时力竭难度衰减不适用（有效难度恒等于开局值）。
- **无其他鱼助战**：挑战鱼饵生效时不触发皇冠助战。
- **原生脱杆失败禁用**：挑战难度 >100 时原生“3 次脱杆失败”规则禁用，可继续挑战到成功。
- **流动金色皇冠**：难度等级 ≥95 且挑战鱼饵生效时成功（不区分是否超时）→ 该鱼皇冠升级为流动金色（见 4.3）。
- **100 级流动皇冠（BATCH-048）**：挑战开始时难度等级 ≥100（100 级鱼）且挑战鱼饵生效时成功 → 该鱼流动皇冠按基础尺寸 ×1.2 绘制（新增存档字段 `Level100FlowCrowns`，旧存档默认空）。皇冠图层跟随鱼图标同批绘制（layer=图标+0.01），位于收藏页悬停详情 UI 与鼠标之下。**困难模式（随机无背板，`EnableRandomFishBehavior=true`）下基础尺寸改为 ×1.3（BATCH-074，仅图鉴绘制）。**

#### 7.6 手感优化（BATCH-038 叠加 BATCH-034 α 手感）
- α=1 时定速最大值 = 25（`30 − 5α`，随 α 线性；BATCH-056 由 BATCH-038 的 21 回调）；仍无加速度/条内阻尼/惯性、撞边完全钳制。
- 叠加相对速度系数：绿条中间朝鱼中间移动 ×(1+0.2α)（α=1 → ×1.2），相背离 ×(1-0.2α)（α=1 → ×0.8），随 α 线性（BATCH-056 由 BATCH-054 的 0.5 回调到 0.2）。
- α=0 保持原生积分手感不变。

#### 7.6.1 钓鱼小游戏期间食物 buff 暂停（BATCH-056）

- 钓鱼小游戏挂载在 `Game1.activeClickableMenu`（原生 `FishingRod.startMinigame` 路径）；小游戏期间 `id="food"` 的食物 buff 不走时（`Buff.update` Prefix 跳过），退出小游戏后恢复走时。
- 饮料（`id="drink"`）与其他 buff 照常走时（本次范围仅食物）。

#### 7.7 称号与满皇冠史诗提示
- 100 级称号由“混沌”改为“太一”（i18n `rank.taiyi`，中英双语；99 级仍为龙神王）。
- 满皇冠（α≥1）且史诗失败条件成立（同一鱼种连续失败 ≥2 次且调整后难度 ≥150）时，失败提示改为：“你需要进入人鱼合一的状态，你必须是鱼，才能赢下这场挑战。”（不再提示收集更多皇冠）。

#### 7.8 持久战奖励与巅峰提示（2026-08-10 用户确认，BATCH-039）

- **适用范围**：所有非鱼王小游戏实例（含非鱼类）；鱼王无实例数据天然豁免；对难度 <100 的鱼同样生效。
- **计时**：战斗秒数对小游戏进行中累计（update 驱动，暂停/菜单不计时），全部非鱼王实例统一累计（BATCH-038 力竭计时原仅 ≥100，BATCH-039 扩为全量；力竭难度衰减仍只对 ≥100 生效）。
- **30 秒巅峰提示**：战斗到 30 秒整时，在“鱼其他提示”通道追加一条随机文案（i18n `hud.peak.{1..10}`，10 套，模板“[职阶]的力气达到巅峰”）；单实例只触发一次；纯显示，不影响结算。
- **失败奖励**：在 BobberBar 失败单发边界（`FailureRecorded` 同分支）发放一次——
  - 战斗 ≥60 秒：60% 概率获得海泡布丁 1 个（(O)265，钓鱼 +4）；40% 不发任何持久战奖励（BATCH-073，2026-08-18 用户确认，60 秒不叠加 30 秒抽奖）。
  - 30 秒 ≤ 战斗 < 60 秒：50% 概率随机获得 +3 钓鱼料理之一（海之菜肴 (O)242 / 烩鱼汤 (O)728 / 龙虾浓汤 (O)730；BATCH-052 修正：228 实为生鱼寿司，海之菜肴=242）。
  - 60 秒不叠加 30 秒的 50% 抽奖（用户确认）；奖励进背包，背包满走原生溢出菜单；每位玩家按实例所有者发放一次。
- **奖励文案**：发放时左下角 FIFO 队列追加一条（i18n `hud.battleReward.{1..20}`，20 套随机，含鱼名+职阶，诙谐幽默；与失败提示排队）。
- **测试入口**：`fish_persisttest <30|60>` 强制下一次小游戏按指定秒数判定（薄控制，构造边界消费，不写存档；鱼王豁免时标志丢弃）。

#### 7.9 停战休息与前摇（BATCH-058，2026-08-13 用户确认）

- **停战休息**：小游戏内只按绿条判定操作——绿条连续 3 秒不动 → 鱼进入待命；鱼在“下一次出现在绿条外超过 5 像素”时停下，做缓慢摇头摆尾动画；期间蓄力槽不掉、战斗计时暂停；重新操作绿条立即恢复。触发时在“鱼其他提示”通道显示 10 套随机休战文案（i18n `hud.idle.1~10`）。
- **鱼跃/甩尾前摇**：高难跳鱼在 0.5 秒原有延迟结束后，追加 0.88 秒前摇（原地停留；鱼图标 0.77 秒旋转 70°、0.11 秒快速转回），随后瞬移；上跳逆时针、下跳顺时针，前摇期间鱼图标发红光呼吸脉动。仅鱼跃/甩尾瞬移有前摇，普通 dart 没有。鱼行动提示（鱼跃/甩尾）在跳鱼判定、0.5 秒延迟开始时即显示（比前摇再早 0.5 秒）。

#### 7.10 挑战鱼饵背板与不掉等级（BATCH-058，2026-08-13 用户确认）

- **失败不掉等级**：挑战鱼饵下失败只累计连续失败（史诗提示/逃逸减速照常），难度等级不下降。
- **背板**：挑战鱼饵下同一种鱼、同一难度等级的行为（目标序列、dart、跳鱼时机等全部随机源）由固定种子决定，钓起前每次一致；种子存玩家存档（键=鱼ID|等级），成功钓起后删除，下次重新随机。

#### 7.11 高难鱼招式短语（BATCH-059，2026-08-14 用户确认）

- **适用范围**：所有调整后难度 ≥150 的非鱼王鱼；开局到第一次瞬移不计入短语。
- **短语定义**：一次瞬移发生后，鱼图标第一次到达中线（266，±10 像素带，只判到达不判穿越、不设超时）为起点；下一次瞬移发生后再次第一次到达中线为终点。
- **循环播放**：起点→终点这段轨迹（含中途那次瞬移）按录制的位置轨迹逐帧回放，冻结原生运动；跳鱼前摇（0.88 秒旋转 70°+红光+行动提示）随轨迹原样重放。反复循环。
- **力竭节点打断（BATCH-059A，2026-08-15 用户确认）**：非挑战鱼饵下每到达力竭节点（1/3/5/7/9/12/15 分钟），短语立即退出并按当前已降低的难度重新录制（蓄力换段进度 1/3 只前进不重置）；挑战鱼饵（难度不衰减）不打断。
- **跳鱼间隔随力竭降档（BATCH-059A，2026-08-15 用户确认）**：跳鱼触发间隔按当前有效难度动态分档（8/6/5/4/3 秒，每次瞬移结算冷却时重算），力竭降档后下一跳变慢；有效难度 <150 时跳鱼与短语整体停止。
- **换段**：蓄力进度每净涨 1/3（绝对值 1/3、2/3、1，只前进不后退）切换到下一短语；新短语同样在“瞬移后第一次到达中线”开始录制，录制完整一段后继续循环。
- **其他机制照常**：蓄力槽、逃逸减速、脱杆计数、挑战星、力竭照常；只接管鱼位置与跳鱼时机。

### 8. GMCM 清除按钮与数据重置（2026-08-12 用户确认，BATCH-042）

- **入口**：GMCM 菜单新增“重置挑战数据”章节（参考 GCE GMCM 方案：章节标题 + 三条警告段落 + 布尔开关“准备重置挑战数据（两步确认）”；未安装 GMCM 时无此入口，`fish_clear confirm` 仍可用）。
- **作用**：回到刚安装本模组的状态——清空当前玩家的难度统计（成功/失败/连续失败）、收藏星标、挑战皇冠，并删除旧存档级键 `FishDifficultyData`（仅主玩家）；立即写回空数据。
- **范围**：只作用于当前玩家（单人=唯一玩家；联机=本机玩家；分屏=当前活动屏幕）。不影响 config.json、背包/箱子里的物品、已获得的钓鱼等级与经验、原版图鉴记录、其他玩家、其他存档。
- **防误点**：两步确认。第一步在 GMCM 中主动开启开关（待命态，不执行）；第二步关闭菜单后游戏弹出原生问题对话框（确认重置/取消），选择“确认重置”才执行；选择“取消”则不执行；确认框保持打开直到明确选择（BATCH-044 移除 5 秒超时，并校验应答者=发起玩家）。
- **不可恢复**：执行后挑战进度无法恢复（除非玩家此前手动备份存档）。
- **命令等价**：`fish_clear confirm` 与按钮同一语义、同一写入者（`DifficultyManager.ClearAllData`）。
- **与传奇皇冠回填的交互（BATCH-043/046）**：重置会清空收藏星标/挑战皇冠；但原版 5 条传奇鱼的皇冠属于原版钓获记录，重置后立即按 `farmer.fishCaught` 回填（BATCH-046，同会话生效；普通星标与挑战皇冠不恢复）。

### 9. 传奇鱼皇冠回填（2026-08-12 用户确认，BATCH-043）

- **规则**：SaveLoaded 后扫描本地玩家 `farmer.fishCaught` 中 5 条原版传奇（`(O)159/160/163/682/775`），累计钓获数 `value[0] > 0` 即补发皇冠并写回——装模组前已钓到的玩家同样获得。
- **幂等**：已有皇冠跳过；无变化不写；每次加载最多 5 次字典查询。
- **鱼塘不计**：原生 `Farmer.caughtFish` 只在非鱼塘钓获时写入 `fishCaught`，与实时钓获规则一致。
- **作用域**：每个本地玩家只补自己（单人=唯一玩家；联机/分屏各客户端各自补并同步）。
- **与重置交互**：重置按钮清空后立即按上述规则回填传奇皇冠（BATCH-046，同会话生效；普通星标不恢复）。

### 10. 连续失败逃跑减速（2026-08-12 用户确认，BATCH-051；BATCH-057 修订：挑战鱼饵同样参与）

- **适用**：调整后难度 >100 的鱼（鱼王天然豁免；挑战鱼饵同样参与，95 级以上也吃这个减速）。
- **按鱼种累计**：连续失败次数按“玩家 + 鱼 ID”分别统计（复用现有 `ConsecutiveFailCount`），成功钓起同一种鱼后清零。
- **效果**：鱼快逃跑（蓄力槽低区，复用 -10 级/普通鱼的蓄力槽保护口径）时，逃跑减速倍率从原倍率向“-10 级等效倍率”线性插值——连续失败 0→5 次对应 0→100% 加成；第 5 次失败时完全等效 -10 级（≤20%→×0.60、≤1%→×0.20）。
- **天然只在低条区生效**：-10 级等效倍率在蓄力进度 >40% 时≈1.0，因此高条区无效果。
- **运行时纯手感**：不改存档、不改难度等级；与力竭/手感 α 共存（只作用于距离惩罚倍率）。

### 11. 节日钓鱼开关（2026-08-16 用户确认，BATCH-066）

- **开关**：`config.festivalFishing`（GMCM“节日钓鱼应用模组规则”，默认**关闭**）。
- **覆盖范围**：三个钓鱼节日——鱿鱼节（SquidFest，冬 12-13 沙滩）、鳟鱼大赛（TroutDerby，夏 20-21 森林）、冰雪节（Festival of Ice，冬 8 森林结冰湖钓鱼比赛）。
- **关闭（默认）= 节日钓鱼完全原生**：无难度等级注入、无数量倍数、无经验倍数、无品质加成、无模组结算/HUD/提示；鱿鱼节分数=原生（每次成功 +原生数量 分）、冰雪节分数=原生（每次成功 +1 分）、鳟鱼大赛 tag 概率原生（33%×原生数量）。
- **开启 = 模组规则照常生效**，且节日分数按数量倍数翻倍：
  - 鱿鱼节：分数 = 原生数量 × 数量倍数（原生已 +数量，模组补 (倍数−1)×数量；例：难度 10 → 一次给 10 个鱿鱼的分）。
  - 冰雪节：每次成功 `festivalScore` +数量倍数 分（原生 +1，模组补 倍数−1；例：难度 10 → 一次 +10 分）。冰雪节为事件型节日，鱼不进背包、无经验，结算走 `Event.caughtFish`。
  - 鳟鱼大赛：模组规则照常（数量倍数等）；tag 概率保持原生（tag 不走 CreateFish，不受倍数影响）。
- **不在覆盖范围**：夜市潜艇钓鱼（冬 15-17）为 FishingGame 小游戏，不走 FishingRod/BobberBar/Farmer.caughtFish 链，模组天然不介入。
- **实现边界**：统一判定服务 `FestivalFishingService`（IsSquidFest/IsTroutDerby/IsIceFestival/IsVanillaFestivalMode）；原生门挂 BobberBar 构造、pullFishFromWater 记录、CreateFish 数量转换、caughtFish 结算、gainExperience 经验五处边界；冰雪节补分走新增 `Event.caughtFish` Postfix 并消费式清理 pending（顺带修复冰雪节 pending 残留缺陷）。
