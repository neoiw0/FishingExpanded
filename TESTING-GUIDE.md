# FishingExpanded 控制台测试指南

本指南提供快速测试各难度等级和功能的控制台命令，无需反复钓鱼即可验证系统行为。

---

## 一、控制台命令总览

### 1.1 核心命令

| 命令 | 功能 | 用法示例 |
|------|------|----------|
| `fish_setlevel` | 直接设置鱼的难度等级 | `fish_setlevel 128 50` |
| `fish_addsuccess` | 增加成功次数 | `fish_addsuccess 128 10` / `fish_addsuccess 2 128 10` |
| `fish_addfail` | 增加失败次数 | `fish_addfail 128 5` / `fish_addfail 2 128 5` |
| `fish_info` | 查看鱼的详细信息 | `fish_info 128` / `fish_info 2 128` |
| `fish_list` | 列出所有已记录的鱼 | `fish_list` / `fish_list 2` |
| `fish_clear` | 清空所有数据 | `fish_clear confirm` / `fish_clear 2 confirm` |
| `fish_addstar` | 添加收藏星标 | `fish_addstar 128` / `fish_addstar 2 128` |
| `fish_giant` | 模拟巨型鱼（触发NPC反应；参数为难度等级，≥8 生效，BATCH-060）| `fish_giant 128 20` / `fish_giant 2 128 20` |
| `fish_bonus` | 查看收藏皇冠与鱼竿熟练度 α | `fish_bonus` / `fish_bonus 2` |
| `fish_addstars` | 批量添加收藏皇冠（完整可计数 61 鱼池：56 普通 + 5 原版传奇，提升鱼竿熟练度） | `fish_addstars 20` / `fish_addstars 2 20` |
| `fish_challengecrown` | 设置挑战鱼饵流动金色皇冠标记（测试） | `fish_challengecrown 144 1` / `fish_challengecrown 2 144 1` |
| `fish_persisttest` | 强制下一次小游戏失败时按指定秒数发放持久战奖励（测试） | `fish_persisttest 60` |

> **玩家序号（BATCH-045）**：上述数据/查询命令可把第一个参数写成玩家序号——`1`=主机、`2`=第一个农场客/副机，以此类推（顺序=`Game1.getAllFarmers()`，含离线农场客）；省略序号=当前玩家。示例：`fish_addstars 2 20` 给副机加 20 颗皇冠，`fish_bonus 2` 查看副机，`fish_clear 2 confirm` 只清副机。`fish_list`/`fish_bonus` 支持单独序号；`fish_assist`/`fish_persisttest`/`fish_selftest`/`fish_assiststats` 仍只作用于当前玩家。

---

## 二、常用鱼ID参考表

| 鱼ID | 中文名 | 稀有度 | 推荐测试用途 |
|------|--------|--------|--------------|
| 128  | 河豚 | 困难 | 基础测试 |
| 129  | 凤尾鱼 | 简单 | 负等级测试 |
| 130  | 金枪鱼 | 困难 | 高难度测试 |
| 131  | 沙丁鱼 | 简单 | 快速测试 |
| 132  | 鲈鱼 | 中等 | 中等难度测试 |
| 136  | 幼鱼 | 简单 | 巨型鱼视觉对比 |
| 138  | 彩虹鳟鱼 | 困难 | 星标测试 |
| 140  | 核桃鱼 | 困难 | 多倍数测试 |
| 143  | 鲶鱼 | 中等 | 品质测试 |
| 146  | 红鲻鱼 | 困难 | 高倍数测试 |
| 149  | 章鱼 | 困难 | NPC对话测试 |
| 151  | 鱿鱼 | 困难 | 星标收集测试 |

---

## 三、完整测试流程

### 3.1 测试前准备

1. **启动游戏**并加载存档
2. **按 `~` 键**打开SMAPI控制台
3. **清空旧数据**（可选）：
   ```
   fish_clear confirm
   ```

### 3.2 场景1：负等级测试（-10 到 0）

**目的**：验证难度降低和接住保护

```bash
# 设置凤尾鱼为 -10 级
fish_setlevel 129 -10
fish_info 129

# 预期结果：
# - 难度倍数: 0.50x
# - 接住保护: 在1%/20%/40%进度时大幅减少惩罚
# - 数量倍数: 1x (负等级不加成)
```

**游戏内验证**：
1. 去海边钓凤尾鱼
2. 观察小游戏难度明显降低
3. 故意让进度条降到1%，观察能否轻易接住

---

### 3.3 场景2：低等级测试（0 到 10）

**目的**：验证难度倍数分段曲线（4级×1.2、20级×5、100级×50，越往后加得越多）

```bash
# 设置沙丁鱼为 5 级
fish_setlevel 131 5
fish_info 131

# 预期结果：
# - 难度倍数: 1.44x (分段线性：4级1.2起，5级=1.2+3.8×1/16；20级5、100级50)
# - 数量倍数: 11x (四舍五入 1 + 199 × 5/100)
# - 品质加成: +1 (每5级一次)
```

**游戏内验证**：
1. 钓一条沙丁鱼
2. **HUD通知**显示"骑士"等级
3. **背包**收到约11条鱼
4. **品质**为银星（+1）

---

### 3.4 场景3：中等级测试（10 到 50）

**目的**：验证多倍数和品质提升

```bash
# 设置河豚为 30 级
fish_setlevel 128 30
fish_info 128

# 预期结果：
# - 难度倍数: 15.70x
# - 数量倍数: 61x
# - 品质加成: +6 (30/5 = 6)
```

**游戏内验证**：
1. 钓一条河豚
2. **HUD通知**显示"大公"等级
3. **背包**收到约61条河豚
4. **品质**为铱星（+6 → 4级）
5. 如果背包满，应弹出**ItemGrabMenu选择界面**

**钓鱼等级建议验证**：
1. 记录当前玩家钓鱼等级 `L`。
2. 用 `fish_setlevel 128 50` 重新进入小游戏；当 `5 > L` 时应在左下角显示“建议达到5以上钓鱼等级再挑战……”完整建议。
3. 用 `fish_setlevel 128 1` 测试小数阈值；当 `0.1 > L` 时才显示建议，比较不得先把 `0.1` 向下取成 `0`。
4. 当 `5 > L × 2` 时，建议开头还应显示“这是不可能的高难度挑战！”。
5. 钓鱼五大鱼王时不应显示上述建议或“不可能”提示。

---

### 3.5 场景4：高等级测试（50 到 100）

**目的**：验证星标和鱼竿熟练度 α

```bash
# 设置金枪鱼为 80 级
fish_setlevel 130 80
fish_info 130

# 预期结果：
# - 难度倍数: 40.20x
# - 数量倍数: 160x (四舍五入 1 + 199 × 80/100)
# - 品质加成: +16 → 铱星（已封顶）
```

**游戏内验证**：
1. 钓一条金枪鱼
2. **HUD通知**显示"神皇"等级
3. **背包**收到约160条金枪鱼
4. **成功钓起且调整后难度 ≥ 120**时：自动添加**收藏星标** ★（隐藏等级加成已整体删除，BATCH-034）

**验证星标**：
```bash
fish_info 130
# 应显示: 收藏星标: ★ 已获得

fish_bonus
# 应显示: 可计数皇冠: x/61 | 鱼竿熟练度: 0.xx（按已收集皇冠）
```

**验证图标标记（BATCH-064-2/3）**：
- 打开**收藏品页面**（按 `E` → Collections → Fish标签）
- 金枪鱼等可计数原生鱼图标**左上角**应有**金色皇冠**
- 非鱼类（如绿藻）与其他 Mod 鱼达到条件后图标**左上角**应有**早期小金星**（`Game1.mouseCursors (346,392,8,8)`，20×20 金色）

**验证皇冠加成显示（BATCH-036；BATCH-039 改名；BATCH-058T 文案）**：
- 点击有皇冠的可计数鱼（如金枪鱼）查看描述：应显示 `挑战等级：{称号}（{X}级） | 鱼竿手感增强`
- 点击有星星的非鱼类/Mod 鱼：显示 `挑战等级：{称号}（{X}级） | 无手感增强`
- 点击无皇冠/星星的鱼：只显示挑战等级，**不**出现“鱼竿手感增强”或“无手感增强”
- 鱼王（如传奇鱼）有皇冠时：只显示“鱼竿手感增强”，不显示挑战等级

---

### 3.6 场景5：巨型鱼与NPC反应测试

**目的**：验证NPC气泡和对话

```bash
# 模拟20倍章鱼
fish_giant 149 20
```

**游戏内验证**：
1. **将章鱼拿在手上**（拖到工具栏并选中）
2. **靠近任意NPC**（5格半径内）
3. **观察**：
   - NPC 头顶出现**赞美文本气泡**（100 种文案之一，含鱼名和具体尺寸；每 NPC 每天每种鱼仅一次）
   - 主动与该 NPC 对话：首次为替换的赞美文案，二次恢复原对话（每 NPC 每天每种鱼仅一次，与冒泡计数独立）
4. **进入FarmHouse**再出来：巨型鱼标记清除，可重新触发

**重置测试**：
- 每天开始时，NPC反应次数重置
- 同一天同一NPC对同一鱼种只触发一次

---

### 3.7 场景6：边界值测试

**最小负等级**：
```bash
fish_setlevel 129 -10
fish_info 129
# 难度倍数应为 0.50x（最低）
```

**最大正等级**：
```bash
fish_setlevel 146 100
fish_info 146
# 难度倍数应为 50.00x（最高）
# 数量倍数应为 200x（最高）
```

**品质封顶测试**：
```bash
fish_setlevel 140 100
fish_info 140
# 品质加成: +20，但游戏内只能到铱星（4级）
```

---

### 3.8 场景7：多鱼种星标收集

**目的**：验证钓鱼等级累加

```bash
# 添加5种鱼的星标
fish_addstar 128
fish_addstar 130
fish_addstar 138
fish_addstar 149
fish_addstar 151

fish_bonus
# 预期: 可计数皇冠: 5/61 | 鱼竿熟练度: 2%（BATCH-039 曲线锚点）
```

**游戏内验证**：
- 打开**玩家菜单** → 技能页面
- 钓鱼等级应增加**2级**（floor(2.5) = 2）
- 例如：原本5级 → 显示7级

---

### 3.9 多人隔离与安装

在线联机时，主机和每一位农场客都必须安装相同的 FishingExpanded 版本和依赖。每个玩家的难度、星标保存在自己的玩家数据中，不会互相共享；鱼竿熟练度 α 由各自皇冠数派生。

**验收步骤**：
1. 主机和农场客分别用不同鱼 ID 设置不同等级。
2. 两人分别钓起同一种鱼，观察自己的等级和 HUD，不应互相增加。
3. 一方使用 `fish_addstar`，另一方的 `fish_bonus`（皇冠/α）不应变化。
4. 两人同时或连续钓鱼，不应出现重复数量转换、重复等级结算或对方玩家的待处理鱼获串线。

---

### 3.10 场景8：BATCH-034 手感/皇冠/跳鱼文案实测

**目的**：验证隐藏等级删除、皇冠/鱼竿熟练度 α、跳鱼文案、传奇一次皇冠（BATCH-034）。

```bash
# 1) 查看初始鱼竿熟练度
fish_bonus
# 应显示: 收藏皇冠总数 / 可计数皇冠: 0/61 | 鱼竿熟练度: 0%

# 2) 添加 20 颗可计数皇冠后查看熟练度
fish_addstars 20
fish_bonus
# 应显示: 可计数皇冠: 20/61 | 鱼竿熟练度: 20%（BATCH-039 锚点）

# 3) 加满 56 颗普通鱼皇冠（差 5 颗传奇由钓获获得）
fish_addstars 56
fish_bonus
# 应显示: 可计数皇冠: 56/61 | 鱼竿熟练度: 90%（35%+65%×26/31≈89.5%）
```

**游戏内验证（逐项独立标记）**：
1. **隐藏加成已删**：图鉴不再显示“钓鱼条长度额外加成”行；任何星标鱼都不增加钓鱼等级。
2. **手感 α=0（未加皇冠时）**：小游戏手感和之前版本一致（渐进加速、有惯性反弹）。
3. **手感随 α 增强**：`fish_addstars 20` 后钓鱼，按下/松开绿条的响应更接近“直接定速”；加满 56 颗后再试，应接近终点手感——按下瞬间直接 30px/帧、松手直接下落、撞边停住不反弹。
4. **绿条外钳制已取消**：鱼在绿条外时，其随机运动会像原版一样自由游动（BATCH-033 的“禁止进条”不再生效）；玩家移动绿条照常可抓。
5. **传奇一次皇冠**：钓起任意原版传奇鱼（如 163 传奇鱼）成功后 `fish_bonus` 可计数皇冠 +1；图鉴出现皇冠；不影响难度/数量/品质/称号豁免。
6. **Mod 鱼星星（BATCH-064-3）**：Mod 鱼获得星标后图鉴显示早期金星、描述“无手感增强”，`fish_bonus` 可计数皇冠不变。
7. **跳鱼文案**：调整后难度 ≥150 的鱼瞬移到上（0~133）或下（399~532）25% 区域时，鱼旁显示“鱼跃！”/“甩尾！”等随机文案（各 15 条），约 1 秒后淡出；跳鱼本身位置保持不动（速度归零）。
8. **帧率解耦**：60fps 下手感/概率/运动与原版一致；低帧率下不出现明显漂移或加速差异。

**预期禁止现象**：α=0 时手感与原版出现明显差异；鱼跳文案不出现或乱出现；图鉴出现 +0.2 加成行；撞边仍反弹。

**失败证据**：记录游戏时间、地点、鱼 ID、皇冠数、α、操作步骤、观察现象、截图和 SMAPI 日志。

### 3.10 场景8 补充：BATCH-035 皇冠助战实测

**目的**：验证助战概率/选鱼、临时钓鱼等级绿条加长、助战文案（BATCH-035）。

```bash
# 查看当前助战概率（随可计数皇冠数线性增长，满 61 = 10%）
fish_bonus
# 应显示: 助战概率: X%

# 强制下一次小游戏触发助战（验证文案与绿条）
fish_assist
# 应提示: 下一次钓鱼小游戏将强制触发助战；无皇冠时给出警告

# 自动自测（概率/可计数/排位/分布统计/i18n/强制标志，逐项 PASS/FAIL）
fish_selftest
# 应显示: 全部 [PASS] + "自测结果: X 通过 / 0 失败"

# 助战观测统计（累积真实触发样本；clear 清空）
fish_assiststats
# 应显示: 平均等级、低/中/高排位组均值（期望 13.5/20/26.5）、40级比例高低组比值（期望≈30）
```

**游戏内验证（逐项独立标记）**：
1. **概率显示**：0 皇冠时 `fish_bonus` 显示 0%；`fish_addstars 20` 后约 3.3%；加满 56 后约 9.2%。
2. **助战触发**：`fish_assist` 后开始钓鱼小游戏，绿条旁边显示 20 条文案之一（如“荣耀的XX伯爵前来护驾！”），5 秒内上移 30px 线性淡出（BATCH-039 统一 5 秒）；日志出现 `[BobberBar] 助战触发 | 助战鱼: ... | 难度: ... | 排位: ... | 临时钓鱼等级: +N | 绿条高度: X → Y`。
3. **临时等级绿条**：触发助战时绿条明显变长（+临时等级×8px，最高 40 级=+320px）；助战等级 0~40 随机且随助战鱼难度排位倾斜（最高难度鱼更常出高级、平均 26.55；最低难度鱼镜像、平均 13.45；两条极端鱼平均恰 20）；日志 `绿条高度: 104 → 424` 之类（随玩家钓鱼等级与助战等级变化）；玩家钓鱼等级本身不变（图鉴/存档无变化）。
4. **鱼王豁免**：钓传奇鱼（如 163）时即使有皇冠也不触发助战、不显示文案。
5. **Mod 鱼皇冠不计**：给 Mod 鱼加皇冠后 `fish_bonus` 助战概率不变。
6. **最多一条**：一次小游戏最多出现一条助战鱼/一次文案（构造时只判定一次）。
7. **统计核验（fish_assiststats）**：累积 200+ 次触发后核对——高排位组（>0.67）平均≈26.5、低排位组（<0.33）平均≈13.5、40 级比例高组/低组≈30、低组+高组平均和≈40；`fish_assiststats clear` 清空后重计。
8. **自动自测（fish_selftest）**：无皇冠时"排位检查"显示 [SKIP]，其余项应全 [PASS]；加满皇冠后重跑全部 [PASS]。

**预期禁止现象**：无皇冠时触发助战；助战修改玩家钓鱼等级或存档；绿条加长覆盖训练鱼竿/浮标原生计算；文案刷屏或常驻不消失；钓鱼王触发助战。

**失败证据**：记录游戏时间、地点、鱼 ID、皇冠数、助战概率、操作步骤、观察现象、截图和 SMAPI 日志。

### 3.9 场景：持久战奖励与巅峰提示（BATCH-039）

**目的**：验证 30/60 秒失败奖励、30 秒巅峰提示、浮动提示 5 秒淡出与 α 曲线。

```bash
# 确定性入口：强制下一次小游戏失败时按指定秒数发放奖励（不写存档）
fish_persisttest 60
# 应提示: 下一次钓鱼小游戏失败时将按 60 秒发放持久战奖励

# α 曲线自测（纯函数锚点 0/5/10/20/30/61 → 0/2/7/20/35/100%，点间线性）
fish_selftest
# 应显示: α曲线 各项 [PASS]；i18n: hud.peak.1~10、hud.battleReward.1~20 各 10/10、20/20；持久战标志 3 项全过
```

**游戏内验证（逐项独立标记）**：
1. **30 秒巅峰提示**：`fish_setlevel 144 80` 后钓狗鱼等高难鱼，战斗满 30 秒时绿条左侧出现一条 `[职阶]...` 巅峰文案（10 套随机之一，单次只出现一次）；日志出现 `[BobberBar] 持久战巅峰提示`。
2. **30 秒失败奖励**：战斗满 30 秒后失败（不按测试命令）：50% 概率背包获得 海之菜肴/烩鱼汤/龙虾浓汤 之一；失败提示后排队显示 20 条奖励文案之一（含鱼名+职阶）；日志出现 `[BobberBar] 持久战奖励`。
3. **60 秒失败奖励**：`fish_persisttest 60` 后任意非鱼王小游戏失败：必得海泡布丁 1 个，且不额外获得 30 秒料理；日志带“测试强制”。
4. **30 秒内失败**：快速失败无奖励、无巅峰提示。
5. **成功不发放**：战斗超过 30 秒后成功钓起：无奖励。
6. **鱼王豁免**：`fish_persisttest 60` 后钓传奇鱼失败：无奖励，日志出现“持久战测试标志被鱼王豁免消耗”。
7. **5 秒淡出**：触发跳鱼/助战/巅峰任一浮动提示，观察提示约 5 秒内缓慢上移 30px 并淡出（不再是 2 秒）。
8. **α 曲线**：`fish_addstars 5` → `fish_bonus` 鱼竿熟练度约 2%；10 → 7%；20 → 20%；30 → 35%；61 → 100%。

**预期禁止现象**：同一失败重复发奖励；60 秒同时拿到料理+布丁；30 秒内失败也给奖励；成功给奖励；鱼王给奖励；提示 2 秒快速消失；α 显示旧线性值。

**失败证据**：记录游戏时间、地点、鱼 ID、战斗秒数、背包奖励、提示文案、截图和 SMAPI 日志。

## 四、快速调试命令组合

### 4.1 快速跳到高难度

```bash
fish_setlevel 128 80
fish_addstar 128
fish_giant 128 25
fish_info 128
```

### 4.2 测试所有等级档位

```bash
fish_setlevel 128 -10   # 额...稍微强一点的个体
fish_setlevel 129 -5    # 额...稍微强一点的个体
fish_setlevel 130 0     # 额...稍微强一点的个体
fish_setlevel 131 20    # 大公
fish_setlevel 132 40    # 亲王
fish_setlevel 138 60    # 帝王
fish_setlevel 140 80    # 神皇
fish_setlevel 146 100   # 神王

fish_list
```

### 4.3 重置并开始新测试

```bash
fish_clear confirm
fish_list   # 应为空
fish_bonus  # 应为 可计数皇冠: 0/61 | 鱼竿熟练度: 0%
```

---

## 五、日志观察要点

### 5.1 关键日志标记

打开SMAPI控制台，筛选以下关键词：

| 日志标记 | 含义 | 示例 |
|----------|------|------|
| `[DifficultyManager]` | 难度数据变化 | 成功/失败记录 |
| `[BobberBar]` | 小游戏初始化 | 难度/品质修改 |
| `[FishingRod]` | 钓鱼成功 | 数量倍数应用 |
| `[GiantFishManager]` | 巨型鱼记录 | NPC反应触发 |
| `[HUDNotifier]` | HUD通知 | 等级称号显示 |
| `[CollectionsPage]` | 星标绘制 | 收藏品页面 |
| `★ 达成高难度里程碑 ★` | 星标获得 | 皇冠/鱼竿熟练度 |

### 5.2 正常日志示例

**钓鱼成功**（30级河豚，完美脱杆0次，区间截断到亲王上限33）：
```
[FishingRod] 钓鱼成功（动画阶段）| 玩家: 123456 | 鱼ID: (O)128 | 难度等级: 30 | 数量倍数: 61 | 脱杆次数: 0 | 动画显示: 1条
[FishingRod] 数量转化完成 | 玩家: 123456 | 鱼ID: (O)128 | 原生堆叠: 1 → 最终: 61 (×61)
[DifficultyManager] 钓鱼成功记录 | 玩家: 123456 | 鱼ID: (O)128 | 请求增长: +10 | 实际增长: +3 | 成功次数: 33 | 失败次数: 0 | 等级变化: 30 → 33
[HUDNotifier] 成功提示显示 | 鱼: 河豚 ((O)128) | 等级: 33 | 封顶: False | 非鱼类: False
```

**星标获得**：
```
[DifficultyManager] ★ 达成高难度里程碑 ★ | 玩家: 123456 | 鱼ID: (O)130 | 调整后难度: 125.6 | 可计数皇冠: x/61
```

**NPC反应**：
```
[GiantFishManager] 超大鱼记录 | 玩家: 123456 | 鱼ID: (O)149 | 倍数: 20 | fishSize: 400 | 视觉缩放: ×1.259
[GiantFishManager] NPC冒泡触发 | NPC: Abigail | 鱼ID: (O)149 | fishSize: 400 | 文案: 天哪！这条章鱼足足有400厘米长！...
```

---

## 六、常见问题排查

### 6.1 控制台命令无效

**症状**：输入命令后无反应

**排查**：
```bash
help
# 检查是否列出 fish_* 系列命令
```

**解决**：
- 确认MOD已正确加载（SMAPI启动日志中有"FishingExpanded 初始化完成"）
- 确认已进入游戏并加载存档（命令需要存档上下文）

---

### 6.2 数量倍数未生效

**症状**：钓到的鱼数量仍是1

**排查**：
```bash
fish_info 128
# 检查"数量倍数"是否 >1
```

**可能原因**：
- 等级 ≤ 0 时数量倍数固定为1
- 查看日志中是否有`[FishingRod] 钓鱼成功`记录

---

### 6.3 星标未显示

**症状**：收藏品页面看不到星星

**排查**：
```bash
fish_info 130
# 检查"收藏星标"是否为"★ 已获得"
```

**验证步骤**：
1. 打开收藏品页面
2. 切换到**Fish标签**（第二个标签）
3. 找到对应鱼的图标
4. 星星在图标**左上角**（偏移-4像素）

**注意**：星标只在**Collections页面**显示，不在背包/钓鱼界面显示

---

### 6.4 NPC无反应

**症状**：拿着巨型鱼靠近NPC无气泡

**排查**：
```bash
fish_list
# 检查是否有巨型鱼记录（倍数 >15）
```

**验证条件**：
1. 鱼倍数 >15
2. 鱼在**工具栏**且**被选中**（高亮）
3. NPC在**5格半径**内
4. 今天这个NPC对这条鱼**未触发过**
5. **未进入FarmHouse**清除标记

---

## 七、推荐测试顺序

### 第一阶段：基础功能（15分钟）

1. ✅ **负等级保护**：`fish_setlevel 129 -10` → 钓鱼验证
2. ✅ **正等级倍数**：`fish_setlevel 128 30` → 验证61倍数量
3. ✅ **HUD通知**：观察等级称号是否正确
4. ✅ **品质提升**：验证铱星品质

### 第二阶段：高级功能（15分钟）

5. ✅ **星标系统**：`fish_setlevel 130 80` → 验证星标和加成
6. ✅ **钓鱼等级**：`fish_bonus` 查看加成，打开技能页面验证
7. ✅ **收藏品页面**：验证星标图标显示

### 第三阶段：NPC交互（10分钟）

8. ✅ **巨型鱼**：`fish_giant 149 20` → 靠近NPC验证气泡
9. ✅ **对话触发**：等待30秒验证随机对话
10. ✅ **重置测试**：进入FarmHouse再出来验证清除

### 第四阶段：边界测试（10分钟）

11. ✅ **最小/最大值**：`fish_setlevel 129 -10` 和 `fish_setlevel 146 100`
12. ✅ **多鱼种累加**：添加5个星标验证等级叠加
13. ✅ **数据持久化**：保存退出再进入，验证数据保留

---

## 八、性能检查

### 8.1 日志频率检查

**正常情况**：
- 每次钓鱼：3-5条日志
- 每30帧（半秒）：NPC检查仅在**有巨型鱼**时触发
- 收藏品页面：每种鱼的星标**仅首次绘制时记录**

**异常情况**（需修复）：
- 每帧输出日志
- 重复输出相同内容
- 卡顿或延迟

### 8.2 内存检查

```bash
fish_list
# 鱼种类数应 < 100（正常玩家）

fish_bonus
# 星标数应 < 50（正常玩家）
```

---

## 九、测试完成检查清单

- [ ] 负等级难度降低生效
- [ ] 负等级接住保护生效
- [ ] 正等级数量倍数正确（线性插值）
- [ ] 品质加成正确（每5级+1）
- [ ] HUD通知显示正确等级称号
- [ ] 小游戏等级建议按 `难度等级/10` 小数比较生效
- [ ] 超过玩家等级两倍时显示“不可能”前缀，鱼王不显示该提示
- [ ] 成功钓起且调整后难度≥120才获得星标
- [ ] 隐藏等级加成已整体删除（星标不再增加任何加成，BATCH-034）
- [ ] 收藏品页面显示皇冠图标（金色 Infinity Crown）
- [ ] 巨型鱼触发NPC气泡
- [ ] 巨型鱼触发随机对话
- [ ] 进入FarmHouse清除巨型鱼标记
- [ ] 每日重置NPC触发次数
- [ ] 数据存档持久化
- [ ] 日志无每帧刷屏
- [ ] 无明显卡顿或崩溃

---

## 十、附录：等级称号对照表

| 等级范围 | 称号 | 难度倍数范围 | 数量倍数范围 |
|----------|------|--------------|--------------|
| -10 ~ 0 | 额...稍微强一点的个体 | 0.50-1.00x | 1x |
| 1 ~ 3 | 精英 | 1.49-2.47x | 3-7x |
| 4 ~ 6 | 骑士 | 2.96-3.94x | 9-13x |
| 7 ~ 8 | 领主 | 4.43-4.92x | 15-17x |
| 9 ~ 15 | 伯爵 | 5.41-8.35x | 19-31x |
| 16 ~ 22 | 大公 | 8.84-11.78x | 33-45x |
| 23 ~ 33 | 亲王 | 12.27-17.17x | 47-67x |
| 34 ~ 45 | 帝王 | 17.66-23.05x | 69-91x |
| 46 ~ 66 | 神皇 | 23.54-33.34x | 93-133x |
| 67 ~ 88 | 神王 | 33.83-44.12x | 135-177x |
| 89 ~ 100 | 神王 | 44.61-50.00x | 179-200x |

---

## 十一、手持鱼视觉缩放与诊断验收

视觉缩放已进入真实绘制验证，不再按“只记录日志的占位实现”验收。缩放为 BATCH-026 线性公式 `1 + (数量倍数-1) × 0.0136102`：数量倍数为 27 时预期绘制缩放约为 `1.354` 倍；100 级（倍数 200）约为 `3.7084` 倍（= 原 25 级大小）。放大只作用于真鱼：飞行动画中心锚点、落地真鱼与手持鱼底边中点锚点；小游戏鱼标与结算面板示意图保持原生大小（2026-08-06 用户确认）。

### 11.1 测试步骤

1. 完全重启游戏和 SMAPI，加载测试存档；如果该场景会写入难度或星标，使用当前批次规定的全量 `Saves` 沙箱。
2. 确保工具栏中有鲤鱼 ID `142`，并能在 FarmHouse 外将它真正举起；仅选中但未举起不算通过。
3. 在 SMAPI 控制台输入：
   ```text
   fish_giant 142 27
   ```
4. 将 ID `142` 的鱼选中并举起，保持在画面中观察；不要进入 FarmHouse，因为进入后会清除当前超大鱼展示事实。
5. 记录完整 SMAPI 日志和一张画面截图，再按下面的链路判定。

### 11.1.1 三种锚点的画面判定

- **手持**：鱼图底边中点应保持在原生鱼图底边中点的位置；放大只向上和左右扩展，不应整体向身体右下方漂移。
- **小游戏**：鱼标保持原生大小（原生 `scale=2f`）与原生中心，不做视觉缩放；移动轨迹与原生一致。
- **结算面板**：面板与面板内展示鱼名/尺寸的示意图（原生 `scale=4f`）保持原生大小，不做视觉缩放。
- **落地真鱼**：与结算面板同步立在玩家身边、点击后收包的真鱼（原生位置 `(0,-56)`、原点 `(8,8)`、`scale=3f`）以底边中点为锚点放大；放大后底边中点保持在原生位置，原生多鱼机制产生的每张鱼图各自使用底边中点锚点。
- **飞行动画**：从水里飞向玩家的鱼以每张鱼图中心为锚点放大，中心保持在原生飞行轨迹上；不新增动画数量。
- **数量边界**：Mod数量倍数不得新增结算或飞行动画数量；原生多鱼机制仍可产生原生数量的多张鱼图。

### 11.2 日志判定

必须按顺序观察到以下三类低频诊断；同一状态只应记录一次，不得每帧刷屏：

```text
[DIAG-FISH-VISUAL] stage=Game1.drawPlayerHeldObject | ... | item=(O)128 | ...
[DIAG-FISH-VISUAL] stage=Object.drawWhenHeld | ... | isFishCategory=True | isActiveObject=True | ...
[DIAG-FISH-VISUAL] stage=GetDrawScale | ... | visualScale=1.354 | stateHit=True
```

结算路径应出现一次 `stage=FishingRod.draw`、飞行动画路径出现一次 `stage=FishingRod.fly` 的低频视觉状态记录；小游戏路径不产生视觉诊断（无缩放接入），画面保持原生大小；这些记录不应逐帧刷屏。

- 没有 `Game1.drawPlayerHeldObject`：先判定原生手持绘制条件或实际操作不成立，不能直接修改缩放代码。
- 有 `Game1` 但没有 `Object.drawWhenHeld`：检查原生虚方法调用或目标物品类型。
- 有 `Object` 但没有 `GetDrawScale`：检查 Transpiler 的实际运行 IL 和其他 Mod 对目标方法的改写。
- `GetDrawScale` 为 `1.000`：检查鱼类类别、玩家 ID、规范化鱼 ID和 `GiantFishManager` 状态是否一致。
- 日志为 `1.354` 但画面不变：保存截图，继续核对 `SpriteBatch.Draw` 最终参数或其他绘制覆盖；不能把日志数值当作画面通过。

### 11.3 关闭与失败证据

测试结束返回标题并关闭游戏，封存完整 SMAPI 日志、日志 SHA-256、目标 DLL SHA-256、截图和沙箱恢复结果。成功必须同时满足：手持三层日志命中、`FishingRod.draw`/`FishingRod.fly` 命中、`visualScale > 1`、画面明显放大、无 FishingExpanded Error、诊断无每帧刷屏。任一项缺失都标记为未通过，并保留第一处分歧证据。

---

**测试愉快！遇到问题请查看SMAPI控制台日志或参考本指南第六章《常见问题排查》。**

---

## 十二、GMCM 清除按钮测试（BATCH-042）

1. 打开游戏内 GMCM → Fishing Expanded 菜单，找到“重置挑战数据”章节（三条警告段落 + “准备重置挑战数据（两步确认）”开关）。
2. 先用测试命令制造数据：`fish_setlevel 128 50`、`fish_addstar 128`、`fish_challengecrown 144 1`，再用 `fish_info 128` / `fish_bonus` 记录重置前状态。
3. **待命态**：打开开关（勾选），此时数据未清空；关闭 GMCM 菜单。
4. **取消路径**：弹出确认框后选择“取消”，预期数据未清空、开关恢复为关。
5. **无超时**：再次打开开关并关闭菜单，确认框保持打开；等待 10 秒以上仍不自动关闭，此时选择“确认重置”仍会执行。
6. **确认执行**：再次打开开关并关闭菜单，在确认框选择“确认重置”，预期 HUD 显示“已重置为刚安装状态”。
7. **核验**：`fish_info 128` / `fish_bonus` / `fish_challengecrown 144 0` 显示普通星标/挑战皇冠为空；原版 5 条传奇皇冠在重置后立即按图鉴记录恢复（无需重载）；保存（睡觉）后重载仍为空（传奇皇冠除外）。
8. **失败证据**：仅开启开关即清空（缺第二步）、误清其他玩家数据、config 被改动、存档损坏。

---

## 十三、传奇鱼皇冠回填测试（BATCH-043）

1. 准备一个沙箱存档：确保 `fishCaught` 中已有 5 条原版传奇记录（`(O)159/160/163/682/775`，可用原生已钓存档或存档工具构造），并先用 `fish_clear confirm` 清空 CollectionStars。
2. 重新加载该存档。
3. **预期**：加载后 5 条传奇鱼自动获得皇冠；`fish_bonus` 可计数皇冠 +5、α 增加；日志出现 `[DifficultyManager] ★ 传奇鱼回填皇冠 ★`（每个 ID 一条）。
4. **幂等**：再次重载，不出现重复回填日志、数据不变。
5. **重置交互**：用 GMCM 重置清空后，传奇皇冠立即按图鉴记录恢复（无需重载），普通星标不恢复。
6. **失败证据**：无记录玩家被补发、鱼塘记录被计入、每次加载反复写、串到其他玩家。

---

## 十四、100 级流动皇冠与图层测试（BATCH-048）

1. 用 `fish_setlevel 128 100` 把某条鱼设为 100 级，装备挑战鱼饵并成功钓起（挑战鱼饵生效）。
2. 打开图鉴鱼类页：预期该鱼皇冠为流动金色且明显比普通流动皇冠大（基础尺寸 ×1.2）。
3. 悬停到该鱼图标：预期皇冠在详情信息 UI 之下，鼠标光标在最上层，不被皇冠遮挡。
4. 对照：`fish_setlevel 128 99` 后挑战鱼饵成功，皇冠为普通流动尺寸（不记 1.2 倍）。
5. **失败证据**：100 级皇冠尺寸无变化、皇冠盖住详情 UI、99 级误记 1.2 倍。

---

## 十五、89 级以上成功增长上限测试（BATCH-049）

1. `fish_setlevel 128 89` 后真实成功钓起一次：预期只 +3（89 → 92），不会 +11。
2. `fish_setlevel 128 97` 后真实成功：预期 +3 到 100 封顶。
3. `fish_setlevel 128 99` 后真实成功：预期 +1 到 100（不越过 100）。
4. `fish_setlevel 128 88` 后真实成功：预期只 +1 到 89（不能跳到 99）；87 级成功预期 +2 到 89（不得跳进 89-99 区间内部）。
5. **失败证据**：89 级一次超过 +3、99 级越过 100、88 级跳到 89 以上。

---

## 十六、连续失败逃跑减速测试（BATCH-051）

1. 准备一条调整后难度 >100 的普通鱼（非挑战鱼饵），连续失败 1 次后再次挑战，把蓄力条压到 ≤20%：预期条下落速度比 0 次失败时慢（日志出现“逃逸减速加成生效”，倍率 ≈0.75）。
2. 连续失败 5 次后再挑战，蓄力 ≤20%：预期倍率 ≈0.60；≤1% 时 ≈0.20（完全等效 -10 级）。
3. 用挑战鱼饵重复：预期无“逃逸减速加成生效”日志。
4. 成功钓起该鱼后再次挑战：预期回到 0 次失败倍率（≈0.80/0.50）。
5. **失败证据**：5 次失败倍率仍 0.80/0.50、0 次失败也被减速、挑战鱼饵生效、跨鱼种串计数。

---

## 十七、节日钓鱼开关测试（BATCH-066）

> 开关：GMCM“节日钓鱼应用模组规则”或 `config.json` 的 `EnableFestivalFishingMods`（默认 **false**=节日完全原生）。

### 17.1 只读自测（随时可跑，不依赖节日）

```
fish_selftest
# 应显示: 节日: 非节日判定=false [PASS]；节日: 开关默认=关 [PASS]；节日: 非节日原生门=false [PASS]
```

### 17.2 鱿鱼节（冬 12-13，沙滩）

1. **开关关（默认）**：节日钓鱼 → 小游戏为原生难度（无“钓鱼小游戏开始”的难度等级/数量倍数日志、无模组 HUD）；钓到鱿鱼 (O)151 → 分数按原生（每次成功 +1 分，SquidFestScore 以原生数量计）；日志应出现 `[节日] 原生模式跳过 BobberBar 注入` / `[节日] 原生模式跳过 pending 记录`。
2. **开关开**：先 `fish_setlevel 151 10`（鱿鱼难度 10）再钓鱿鱼 → 每次成功数量 ×10 且分数 +10 分（日志 `[节日] 鱿鱼节分数补差` 补分 +9，原生已 +1）；小游戏难度按模组注入。
3. **失败证据**：关状态下出现难度注入/数量倍数/经验倍数；开状态下分数仍是 +1 或补差重复计算（总分 ≠ 原生数量×倍数）。

### 17.3 冰雪节（冬 8，Cindersap 森林结冰湖）

1. **开关关（默认）**：节日钓鱼 → 小游戏原生难度；每次成功比赛分数 +1；鱼不进背包（原生行为）；**下一杆（节后普通钓鱼）无错误结算**（无残留 pending 污染：正常钓鱼日志的难度等级更新只对应本次鱼）。
2. **开关开**：先 `fish_setlevel <该鱼ID> <N>` 再钓 → 每次成功 festivalScore +N 分（日志 `[节日] 冰雪节分数补差` 补分 N−1，原生已 +1）。
3. **失败证据**：关状态下出现模组注入；开状态下分数不加倍；节后第一杆普通钓鱼出现“难度等级更新”且鱼 ID 与冰雪节钓的鱼相同（pending 残留）。

### 17.4 鳟鱼大赛（夏 20-21，森林）

1. **开关关（默认）**：完全原生；tag 概率原生（0.33×数量）。
2. **开关开**：模组规则照常（难度/数量/经验）；tag 数量仍原生（33% 判定一次，不被倍数放大）。
3. **失败证据**：关状态下出现模组介入；开状态下 tag 被数量倍数放大。

---

## 十八、固定钓鱼等级系数测试（BATCH-067）

> 难度等级增长速度与玩家的**固定钓鱼等级**（`Farmer.fishingLevel` 字段，0~10，不含食物/饮料 buff）挂钩：系数 = max(等级,1)×0.1（1 级 ×0.1、10 级 ×1.0=现有速度）；成功钓起保底 +1 级；蟹笼固定 +1 不吃系数。

### 18.1 只读自测（随时可跑）

```
fish_selftest
# 应显示: 系数: 0级=0.1 / 1级=0.1 / 5级=0.5 / 10级=1.0 [PASS]；
# 保底: 1级完美=+1 / 1级2脱杆=+1 / 10级完美=+10 / 10级高难500=+20 [PASS]；
# 掉星: 0星×倍数10=12 / 0星×倍数1=1(兜底) / 无掉星不变 [PASS]
```

### 18.2 两档钓鱼等级对比（核心验收）

1. **低等级档**：新档或 `fish_setlevel 0`（钓鱼等级 1 级）→ 钓一条新鱼（难度等级 0）→ 完美钓起（0 脱杆）→ 日志 `难度等级更新` 应显示 `钓鱼等级系数: ×0.1 | 请求增长: +1`，难度等级 0 → 1（保底生效，未封顶）。
2. **高等级档**：钓鱼等级 10（原生满级）→ 同一规则 → 完美钓起应 `请求增长: +10`（未封顶时）。
3. **0 级新档**：钓鱼等级 0（开局）→ 2 次及以上脱杆成功 → 仍至少 +1（日志 `请求增长: +1`）。
4. **食物 buff 排除**：吃海泡布丁（钓鱼 +4）后钓鱼等级属性显示 5（实际 1+4），日志系数仍按 ×0.1 计算（固定字段 1 级），不得按 buff 后等级。
5. **失败证据**：低等级档成功 +0；日志系数用了 buff 后等级（如 ×0.5）；10 级档完美钓 ≠ +10。

### 18.3 挑战鱼饵掉星折扣（回归设计口径）

1. 准备：难度等级 ≥5 的鱼（等级倍数 ≥5），使用挑战鱼饵，拖过 7:00（0 颗星）后钓起。
2. **预期**：最终数量 = round(3 × 等级倍数 × 0.4)，且至少 1 条（例：倍数 10 → 12 条；倍数 5 → round(6)=6 条）。
3. 日志核对：`数量转化完成` 的 `最终` 数量与上述公式一致；`掉星提示` 显示"减少 60%"。
4. **失败证据**：0 星时 0 条鱼；0 星时数量 = 1×倍数（旧口径，如倍数 10 → 10 条而非 12）。

---

## 十九、经验基数钳制测试（BATCH-068）

> 原生经验公式 `max(1, (品质+1)×3 + 难度/3)` 的难度输入钳制在 **[原生难度, 120]**（用户确认 120）：低于原生难度（负数等级/力竭）按原生难度重算，高于 120 按 120 重算，中间不变；重算后仍乘经验倍数。

### 19.1 只读自测（随时可跑）

```
fish_selftest
# 应显示: 经验: 原生70普通=26 / 完美+140%=62 / 宝箱+120%=57 / Boss×5=195 / 品质4=38 [PASS]；
# 钳制: 低于原生→原生 / 力竭80→原生110 / 超上限→120 / 中间不变 / 边界120=120 [PASS]；
# 倍数: 0级=1 / 负数=1 / 5级=3(现状) / 10级=5(保持) / 13级=6 / 50级=12 / 100级=20 [PASS]
```

### 19.2 负数等级经验补偿（核心验收）

1. 准备：某鱼连续失败到难度等级 -10（`fish_info 128` 查看；或用 `fish_setlevel 128 -10`）。
2. 钓起该鱼（金枪鱼 130 原生难度 70 为例）：日志应出现 `经验基数重算`，`钳制后: 70`，经验 = (0+1)×3 + 70/3 = **26**（完美钓 = 62）；未装模组时同为 26。
3. **失败证据**：日志无 `经验基数重算` 且经验 = 14（调整后 35 的旧行为）；经验 < 原生水平。

### 19.3 上限钳制（120）

1. 准备：高等级鱼（调整后难度 >120，如难度等级 20+ 的 60+ 原生难度鱼）。
2. 钓起：日志 `经验基数重算`，`钳制后: 120`，普通品质经验基数 = 3 + 40 = **43**，再 × 经验倍数。
3. **失败证据**：经验基数 = 调整后难度/3（无上限爆炸，如调整后 500 → 166+）。

### 19.4 力竭后经验不下降

1. 准备：扩展传奇（如 (O)900 Legend II，原生难度 110），等级 0，非挑战鱼饵，拖过 15 分钟（有效难度降到 80）后钓起。
2. **预期**：日志 `经验基数重算`，`钳制后: 110`，经验 = 3 + 36 = **39**（= 原生水平，而非 80 难度的 29）。
3. **失败证据**：经验 = 29（按 80 难度）或日志缺失。

### 19.5 经验倍数曲线（BATCH-068 线性 ×5→×20）

1. 只读断言见 19.1；抽查公式：等级 50 经验倍数 = round(5 + 40×15/90) = **12**；等级 100 = **20**。
2. 真实钓鱼核对（如金枪鱼难度等级 50）：日志 `经验倍率` 显示 ×12；等级 100 显示 ×20。
3. **失败证据**：等级 50 显示 ×25 或等级 100 显示 ×50（旧公式）。










