# FishingExpanded 长期回归矩阵

本文件只保存跨批次稳定成立的回归维度，不保存候选哈希、当轮测试步骤、单次结果或历史时间线。具体批次的 R2 合同在 `Governance\Active` 对应活动卡，当前状态在 `BUG-LEDGER.md`，玩家操作步骤在 `TESTING-GUIDE.md`。

| 维度 | 长期不变量 |
|---|---|
| 存档与玩家边界 | 鱼难度统计和收藏星标按当前设计的存档/玩家粒度保存；保存、重载、换日和返回标题不丢失或串写 |
| 多人安装与隔离 | 在线联机主机和每位农场客都安装同一 Mod；每位玩家的鱼难度和星标互不读取或重复结算 |
| 钓鱼生命周期 | 抛竿、小游戏更新、成功/失败判定、回调、鱼获提交和难度结算顺序符合当前原生调用链；失败判定只结算一次（BATCH-030 恢复 `FailureRecorded` 防重，单次失败不得重复扣级） |
| 数量与动画 | 高倍鱼获在原生 `CreateFish` 返回物品时增加最终背包数量，动画阶段不重复显示或重复结算；原生多鱼机制仍保持可辨识边界 |
| 经验与皇冠 | 非鱼王成功钓鱼经验按当前小游戏难度倍率结算：经验倍数 1~10 级 max(1, round(level×0.5))、10~100 级线性 ×5→×20（BATCH-068）；经验公式难度输入钳制在 [原生难度, 120]（BATCH-068）：负数等级/力竭衰减使传入难度低于原生难度时按原生难度重算（不低于未装模组），高于 120 按 120 重算（基数不爆炸），中间不变；重算复刻原生公式（(品质+1)×3 + 难度/3 + 宝箱 120% + 完美 140% + Boss ×5）；隐藏等级加成（`+0.2` 钓鱼条长度）已整体删除（BATCH-034）；鱼王不参与自定义经验规则；Mod 鱼皇冠只显示、不计入鱼竿熟练度；原版 5 条传奇鱼钓到一次直接给皇冠（BATCH-034） |
| 星之果茶掉落 | 非鱼王难度等级 ≥50 成功钓起后按 等级/400 概率掉落 1 瓶 (O)StardropTea（50级 12.5%、80级 20%、100级 25%）；走成功结算唯一边界，背包满由原生溢出菜单处理；掉落消息 15 条随机并与其他提示一起排队；鱼王/非鱼类不参与（BATCH-032） |
| 品质与尺寸 | 品质封顶、鱼尺寸数字、堆叠数量和视觉缩放各自符合设计，不互相重复应用；尺寸数字按难度等级线性（正每级 +10%、负每级 -5%、与数量倍数解耦，BATCH-029）；手持与落地真鱼保持底边中点，飞行动画保持鱼图中心，小游戏鱼标与结算面板示意图不缩放；视觉缩放为线性公式：0级=1.0、100级≈3.7084（=原25级大小）、每级≈+0.0270843 |
| 特殊对象 | 传奇鱼完全遵守豁免合同；非鱼类遵守八级上限；特殊对象不会被普通鱼规则重写 |
| 难度等级曲线 | 非鱼王难度倍数按分段线性：0级×1.0、4级×1.2、20级×5、50级×20、100级×50，每段斜率递增（0~4:0.05/级、4~20:0.2375/级、20~50:0.5/级、50~100:0.6/级），越往后加得越多；负数区间 [-10,0] 保持 0.5~1.0 线性不变；鱼王不乘倍数（BATCH-037，2026-08-10 用户确认） |
| 89 级增长上限 | 89 级为超高难挑战区间入口：88 级及以下每次成功最多只能升到 89；89 级以上（含 89）每次成功最多 +6 级（BATCH-049/050 为 +3，BATCH-060 用户确认改为 +6），仍受 100 封顶（BATCH-049/050/060） |
| 固定钓鱼等级系数与保底 | 难度等级增长与玩家的固定（基础）钓鱼等级挂钩：请求增益 = max(1, round((脱杆基数 + round(调整后难度/50)) × max(等级,1)×0.1))——固定等级 = `Farmer.fishingLevel` 字段（不含食物/饮料 buff 与助战临时等级）；1 级 ×0.1、10 级 ×1.0=现有速度、0 级钳制 0.1；成功保底 +1（已封顶仍为 +0，不违反封顶）；蟹笼固定 +1 不吃系数；称号区间封顶、89+ 上限、鱼王豁免、挑战鱼饵失败不掉级均不受影响（BATCH-067） |
| 连续失败逃跑减速 | 调整后难度>100（挑战鱼饵同样参与，95 级以上也吃；BATCH-057 修订）：按鱼种连续失败 0→5 次，蓄力槽低区逃跑减速从原倍率线性插值到 -10 级等效（≤20%→0.60、≤1%→0.20）；成功清零；鱼王豁免；不改存档（BATCH-051）；验收：挑战鱼饵连续失败 5 次后低条减速生效 |
| 力竭机制 | 调整后难度 ≥100 的非鱼王参与：节点 1/3/5/7/9/12/15 分钟 → 1%/3%/10%/20%/35%/50%/100%，节点间线性（含 0→1 分钟段），15 分钟难度降到 80 后保持；有效难度 = 调整后难度 + (80-调整后难度)×百分比；难度随节点整体下降但奖励按开局快照不下降；挑战鱼饵下难度不衰减但节点提示照常并追加文案；每节点 10 套诙谐文案（hud.exhaust.*）、追加 10 条（hud.exhaust.append.*）；计时只在小游戏进行中累计（BATCH-038） |
| 提示双通道 | 鱼动作提示（鱼跃/甩尾）与鱼其他提示（助战/力竭/挑战追加/30 秒巅峰提示）各为独立浮动通道：起点固定、显示 5 秒、5 秒内上移 30px 并线性淡出、可同时多条、每通道上限 8 丢最旧；动作提示左缘距绿条右缘 24px（左对齐）、其他提示右缘距绿条左缘 50px（右对齐，绝对像素写死）；绿条靠屏幕边缘放不下时自动翻到另一侧（BATCH-058O），避免被推到屏幕最右缘；纯显示不入 FIFO 队列；字体=星露谷 dialogueFont+右下黑阴影+非常淡的蓝/红描边（向白色混合 85%）；长文本按写死最大 420px 折行、每段最多 3 行，超长内容拆成续集提示排队显示（BATCH-058C/058G/058H/058K/058L/058M/058N/058O） |
| 停战休息 | 绿条连续 3 秒不动 → 鱼下一次出绿条外超过 5 像素时停下，缓慢摇头摆尾；期间蓄力槽不掉、战斗计时暂停；重新操作绿条恢复；其他提示随机显示 10 套休战文案（hud.idle.1~10）（BATCH-058） |
| 跳鱼前摇 | 鱼跃/甩尾在原有 0.5 秒延迟后追加 0.88 秒前摇（鱼图标 0.77s 旋转 70°+0.11s 快速转回）再瞬移；上跳逆时针、下跳顺时针，前摇期间鱼图标发红光呼吸脉动；鱼行动提示在跳鱼判定、0.5 秒延迟开始时即显示（比前摇再早 0.5 秒）；普通 dart 无前摇（BATCH-058/058H/058I/058T） |
| 动物 NPC 赞美 | 动物 NPC（类型=宠物 Pet，按 petType 狗/猫；马 Horse）冒泡格式=叫声 + 赞美文案，如中文“汪汪！！！（…）”、英文“Neigh!!! (…)”；中/英各有多种叫声随机；只作用于头顶冒泡，动物无主动对话（BATCH-058/073） |
| 招式短语 | 调整后难度≥150 的非鱼王：瞬移后鱼第一次到中线（266±10，只判到达）为短语起点，下次瞬移后再到中线为终点；该段（含瞬移+前摇旋转+红光+行动提示）循环回放；蓄力进度每净涨 1/3（绝对值、只前进）换一段；只接管鱼位置与跳鱼时机，其余机制照常（BATCH-059）；力竭节点（非挑战鱼饵）到达时短语立即退出按当前难度重录（BATCH-059A）；跳鱼间隔按当前有效难度动态分档（BATCH-059A） |
| 挑战鱼饵背板/不掉等级 | 同鱼同等级挑战鱼饵行为固定（钓起前）；失败不掉等级但连续失败计数+1；成功钓起后清除种子重新随机（BATCH-058） |
| 持久战奖励 | 所有非鱼王小游戏战斗秒数统一累计；战斗到 30 秒整“鱼其他提示”单发一次巅峰提示（hud.peak.1~10，10 条随机）；失败且 ≥60 秒 60% 概率获得海泡布丁（(O)265），40% 无奖励；30~60 秒失败 50% 概率随机 +3 料理（(O)242/(O)728/(O)730；BATCH-052 修正 228→242）；60 秒不叠加 30 秒抽奖（BATCH-073 确认）；奖励走失败单发边界（FailureRecorded 同分支）+ 原生溢出菜单，只发一次；奖励提示 20 条随机含鱼名+职阶（hud.battleReward.1~20）入 FIFO 队列；鱼王天然豁免；测试命令 fish_persisttest <30|60>（BATCH-039） |
| 脱杆尺寸与品质 | 难度等级 >0 时脱杆不再缩小鱼尺寸（原生 800ms 缩水计时器持续重置）也不降低品质（按原生完美规则提升，≥2→铱、≥1→金）；等级收益照旧 10/5/2/1 + round(调整后难度/50)；难度等级=0 保持原生（BATCH-038） |
| 尺寸数字与收藏记录 | 尺寸数字在 pullFishFromWater 结算边界统一乘等级倍率（正 +10%/级、负 -5%/级、0 级不乘），doPullFishFromWater 写回 fishSize，Farmer.caughtFish Prefix 同步收藏记录；100 级狗鱼（O)144）本安装数据 min15/max60 应显示 15×11=165 起、不再因脱杆缩到 30 多厘米（BATCH-038） |
| 万能/挑战鱼饵 | 万能鱼饵：难度等级 >0 且原生两条时 +10 条（2→12）再乘等级倍数，挑战鱼饵不叠加；挑战鱼饵（难度>100）：5 分钟内成功数量 ×1.5（ceil）再乘等级倍数；超时掉星（5:00→2、6:00→1、7:00→0，≥95 豁免）：每掉 1 颗最终鱼获 −20%（0.8/0.6/0.4），折扣作用于乘完等级倍数后的最终数量并兜底 ≥1 条（BATCH-067 回归 GAME-DESIGN §7.5 口径；原生挑战鱼饵必给 3 条，round(3×0.4)=1 恒不归零）；等级/皇冠/流动皇冠/品质/尺寸照常；原生 3 次脱杆失败禁用，无其他鱼助战（BATCH-038/056/067） |
| 流动金色皇冠 | 难度等级 ≥95 且挑战鱼饵生效时成功（不区分超时）→ 该鱼皇冠升级流动金色（Collections 页金/白呼吸 + 1±0.05 缩放脉冲）；挑战开始时难度等级 ≥100 的鱼按 ×1.2 绘制（Level100FlowCrowns，BATCH-048）；皇冠图层位于悬停详情 UI 与鼠标之下；独立存档字段 ChallengeCrowns，旧存档缺失默认空；测试命令 fish_challengecrown <鱼ID> [0|1]（BATCH-038） |
| 助战概率与选鱼 | 总助战概率 = 10%×(可计数皇冠/61)（满 61=10%）+ 每条可计数流动金冠鱼 +0.1% + 每条增大流动金冠鱼（100 级 1.2 倍）再 +0.05%；不设上限由 61 条皇冠自然限制；触发后按权重选鱼（普通 1 / 流动 1.001 / 增大 1.0015），金冠鱼更容易被选中；助战等级仍随被选中鱼难度排位倾斜；无皇冠但有金冠时仍可能触发（BATCH-035/073） |
| 自定义钓鱼称号 | ModConfig.CustomFishingTitle（config.json 手动编辑，不注册 GMCM）：空/纯空白=继续 i18n 称号；非空=替代所有显示 rankName 的位置（成功提示/挑战宣言/图鉴挑战等级/助战/持久战奖励/巅峰/力竭/控制台日志）；内部等级与尊敬词逻辑不变（BATCH-073） |
| 称号与史诗失败 | 100 级称号=太一（rank.taiyi，99 级仍龙神王，混沌已删除；BATCH-058J 创世神改名龙神王）；满皇冠（α≥1）且史诗条件成立（同鱼种连续失败≥2 且调整后难度≥150）时失败提示=“人鱼合一”（hud.fail.union），未满皇冠仍为 hud.fail.epic（BATCH-038） |
| 高难度小游戏 | 非鱼王调整后难度 >100：初始目标=顶部、加速度线性增幅（档位锚点曲线：0→10%、50→20%、70→40%、80→70%、90→100%、100→100%，点间线性，≤0 钳 10%、≥90 钳 100%；BATCH-053 用户定稿覆盖 BATCH-029/034）；>150：换目标频率三处与 dart 偏移按 150 封顶；≥150：按难度分档 8/6/5/4/3 秒触发上下 25% 区域瞬移（0.5s 延迟、跳完才重新计时、1 秒检测）；d≤100 与鱼王保持原生不变（BATCH-028）；每次瞬移到上/下 25% 区域时在鱼旁显示贴鱼文案（上跳“鱼跃”类 15 条、下跳“甩尾”类 15 条随机，5 秒内上移 30px 线性淡出，BATCH-034；BATCH-039 统一 5 秒） |
| UI 与展示 | BobberBar、HUD、Collections、NPC 文案、手持鱼、小游戏鱼标和结算鱼图只展示权威事实，不建立第二份难度或奖励状态；可计数 61 原生鱼收藏页星标为金色皇冠（Infinity Crown 贴图，BATCH-029）；非鱼类与其他 Mod 鱼为早期金星（Game1.mouseCursors (346,392,8,8) 20×20 Color.Gold，BATCH-064-2/3）；已有星标鱼开局显示一次挑战宣言；超出玩家钓鱼等级的小游戏显示等级建议，超过两倍时带“不可能”前缀；同鱼种连续失败 ≥2 次且调整后难度 ≥150 时失败提示为史诗文案（成功清零，BATCH-029），且史诗提示优先于触底、负等级和普通失败提示（BATCH-031）；本 Mod 全部提示 FIFO 排队显示（同屏同时一条、前一条消失后出队、上限 5 丢最旧、按玩家隔离、原生消息不参与，BATCH-032）；难度等级 <1（0 和负数）的胜利显示弱称号“额...稍微强一点的个体”提示，该弱称号不嵌入星标挑战宣言（BATCH-030）；难度等级<1（0和负数）的鱼即使有星标也不显示挑战宣言（BATCH-033）；NPC 冒泡/对话尺寸：英文模式=原生英寸数字 + in.，其他语言=厘米（fishSize×2.54 取整）；中文与手持尺寸一致（BATCH-033/073）；收藏页可计数皇冠鱼描述追加“鱼竿手感增强”（i18n collections.crownControl，鱼王豁免挑战等级但皇冠行照常，BATCH-036；2026-08-10 改名，BATCH-039；BATCH-058T 文案），星星条目显示“无手感增强”（collections.noCrownControl，BATCH-064-3） |
| 手感系统 | 鱼竿熟练度 α = 可计数皇冠数分段线性曲线：0→0%、5→2%、10→7%、20→20%、30→35%、61→100%，点位间线性、超 61 钳 100%（BATCH-039 覆盖 BATCH-034 的皇冠÷61 全程线性）；56 普通 + 5 原版传奇可计数，Mod 鱼不计；0=原生手感、1=终点手感；终点手感：按下/松开瞬间直接定速、无加速度/条内阻尼/惯性、撞边完全钳制（不反弹、速度归零）；定速最大值 `30−5α`（α=1 → 25px/s，BATCH-056 由 21 回调），并叠加方向系数——绿条中间朝鱼中间移动 ×(1+0.2α)、相背离 ×(1-0.2α)（α=1 → ×1.2/×0.8，BATCH-056 由 0.5 回调）；帧率解耦覆盖整个小游戏（绿条、鱼、平滑、概率），60fps 行为与原生一致（BATCH-034）；绿条外钳制机制已整体删除（BATCH-034 覆盖 BATCH-033） |\n| 小游戏等级建议 | 非鱼王比较 `难度等级 / 10` 与当前有效整数钓鱼等级；超过当前等级显示建议，超过两倍在开头显示“不可能”提示；小数比较不能先向下取整 |
| 原生契约 | 当前安装游戏的 `BobberBar`、`FishingRod`、`Farmer.caughtFish`、`CollectionsPage` 和绘制入口保持真实签名、回调和生命周期边界 |
| 多人/屏幕 | 双人同屏与在线联机均按玩家 `UniqueMultiplayerID` 隔离：难度/星标写入各自 `Farmer.modData`（原生同步字段），展示按各自屏幕实例；每位本地玩家独立 `Game1` 实例；未执行的多人/同屏实测必须明确标记 |
| 控制台多玩家目标 | 11 个数据/查询命令支持可选玩家序号（1=主机，2=副机…，省略=当前玩家；顺序=`Game1.getAllFarmers()` 含离线农场客）；`fish_list`/`fish_bonus` 支持单独序号；`fish_next` 也支持玩家序号（BATCH-072）；写入者仍为 `DifficultyManager`，不影响结算所有权（BATCH-045） |
| 性能 | 高频路径有明确上限、频率和失效条件；诊断日志低频且可关闭，不出现每帧刷屏或无界集合增长；每帧/高频路径禁止直接日志（只能经 `FishingLog.LogRateLimited`，同键 30 秒 ≤1 条、上限 64 键超限清空），蓄力槽保护日志按“生效/解除”状态转换单发，手持/绘制链路无逐帧诊断输出（BATCH-024/026 验收诊断已退休，FE-040-4），全部日志受 `config.EnableLogging`（config.json + GMCM）门控（BATCH-040） |
| 卸载/重载 | Mod 卸载、重载、存档切换和标题返回不会残留静态缓存、事件注册或旧鱼获待处理状态 |
| GMCM 重置 | GMCM“重置挑战数据”章节（警告段落 + 开关待命）与原生问题对话框二次确认（无超时，确认框保持打开直到明确选择；BATCH-044 移除超时并校验应答者=发起玩家），同 `fish_clear confirm` 语义：只清当前玩家难度统计/星标/挑战皇冠并删除旧存档级键（仅主玩家）；不重置 config、不动背包/等级/原版图鉴/其他玩家；取消不执行；执行后普通星标/挑战皇冠为空，原版传奇皇冠立即按图鉴恢复（BATCH-042/044/046） |
| 节日钓鱼开关 | 开关 `config.festivalFishing`（默认关）：关闭=鱿鱼节（冬 12-13）/鳟鱼大赛（夏 20-21）/冰雪节（冬 8 事件型）钓鱼完全原生（无难度/数量/经验/品质/模组结算）；开启=模组全链照常 + 鱿鱼节分数=原生数量×数量倍数（难度 10 → 一次 +10 分）+ 冰雪节每次成功 festivalScore +数量倍数 分；鳟鱼大赛 tag 概率保持原生（不走 CreateFish）；夜市潜艇钓鱼（FishingGame 小游戏）不涉及（BATCH-066） |
| 传奇皇冠回填 | 每次加载存档按 `farmer.fishCaught` 补发 5 条原版传奇皇冠（幂等、无变化不写、鱼塘不计）；装模组前已钓到的玩家同样补发；重置后立即补回传奇皇冠、普通星标不恢复（BATCH-043/046） |
| 证据边界 | 静态代码证据、构建、部署、运行观察和真实存档验收分别记录，不能互相替代 |
| 独立判定 | 一次游戏可以覆盖多个批次，但每个玩家可见现象必须分别标记通过、失败或未执行 |

失败证据按批次决定，通常包括游戏时间、地点、鱼 ID/品质/数量、操作步骤、观察屏幕、截图和完整 SMAPI 日志。日志先封存，再判断根因。

