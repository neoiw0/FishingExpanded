# FishingExpanded 长期回归矩阵

本文件只保存跨批次稳定成立的回归维度，不保存候选哈希、当轮测试步骤、单次结果或历史时间线。具体批次的 R2 合同在 `Governance` 对应活动卡，当前状态在 `BUG-LEDGER.md`，玩家操作步骤在 `TESTING-GUIDE.md`。

| 维度 | 长期不变量 |
|---|---|
| 存档与玩家边界 | 鱼难度统计和收藏星标按当前设计的存档/玩家粒度保存；保存、重载、换日和返回标题不丢失或串写 |
| 多人安装与隔离 | 在线联机主机和每位农场客都安装同一 Mod；每位玩家的鱼难度和星标互不读取或重复结算 |
| 钓鱼生命周期 | 抛竿、小游戏更新、成功/失败判定、回调、鱼获提交和难度结算顺序符合当前原生调用链；失败判定只结算一次（BATCH-030 恢复 `FailureRecorded` 防重，单次失败不得重复扣级） |
| 数量与动画 | 高倍鱼获在原生 `CreateFish` 返回物品时增加最终背包数量，动画阶段不重复显示或重复结算；原生多鱼机制仍保持可辨识边界 |
| 经验与皇冠 | 非鱼王成功钓鱼经验按当前小游戏难度倍率结算；隐藏等级加成（`+0.2` 钓鱼条长度）已整体删除（BATCH-034）；鱼王不参与自定义经验规则；Mod 鱼皇冠只显示、不计入手感进度；原版 5 条传奇鱼钓到一次直接给皇冠（BATCH-034） |
| 星之果茶掉落 | 非鱼王难度等级 ≥50 成功钓起后按 等级/400 概率掉落 1 瓶 (O)StardropTea（50级 12.5%、80级 20%、100级 25%）；走成功结算唯一边界，背包满由原生溢出菜单处理；掉落消息 15 条随机并与其他提示一起排队；鱼王/非鱼类不参与（BATCH-032） |
| 品质与尺寸 | 品质封顶、鱼尺寸数字、堆叠数量和视觉缩放各自符合设计，不互相重复应用；尺寸数字按难度等级线性（正每级 +10%、负每级 -5%、与数量倍数解耦，BATCH-029）；手持与落地真鱼保持底边中点，飞行动画保持鱼图中心，小游戏鱼标与结算面板示意图不缩放；视觉缩放为线性公式：0级=1.0、100级≈3.7084（=原25级大小）、每级≈+0.0270843 |
| 特殊对象 | 传奇鱼完全遵守豁免合同；非鱼类遵守八级上限；特殊对象不会被普通鱼规则重写 |
| 难度等级曲线 | 非鱼王难度倍数按分段线性：0级×1.0、4级×1.2、20级×5、50级×20、100级×50，每段斜率递增（0~4:0.05/级、4~20:0.2375/级、20~50:0.5/级、50~100:0.6/级），越往后加得越多；负数区间 [-10,0] 保持 0.5~1.0 线性不变；鱼王不乘倍数（BATCH-037，2026-08-10 用户确认） |
| 力竭机制 | 调整后难度 ≥100 的非鱼王参与：节点 1/3/5/7/9/12/15 分钟 → 1%/3%/10%/20%/35%/50%/100%，节点间线性（含 0→1 分钟段），15 分钟难度降到 80 后保持；有效难度 = 调整后难度 + (80-调整后难度)×百分比；难度随节点整体下降但奖励按开局快照不下降；挑战鱼饵下难度不衰减但节点提示照常并追加文案；每节点 10 套诙谐文案（hud.exhaust.*）、追加 10 条（hud.exhaust.append.*）；计时只在小游戏进行中累计（BATCH-038） |
| 提示双通道 | 鱼动作提示（鱼跃/甩尾）与鱼其他提示（助战/力竭/挑战追加）各为独立浮动通道：起点固定、显示 2 秒、2 秒内上移 30px 并线性淡出、可同时多条、每通道上限 8 丢最旧；其他提示锚定钓鱼条左侧（x+44）；纯显示不入 FIFO 队列（BATCH-038） |
| 脱杆尺寸与品质 | 难度等级 >0 时脱杆不再缩小鱼尺寸（原生 800ms 缩水计时器持续重置）也不降低品质（按原生完美规则提升，≥2→铱、≥1→金）；等级收益照旧 10/5/2/1 + round(调整后难度/50)；难度等级=0 保持原生（BATCH-038） |
| 尺寸数字与收藏记录 | 尺寸数字在 pullFishFromWater 结算边界统一乘等级倍率（正 +10%/级、负 -5%/级、0 级不乘），doPullFishFromWater 写回 fishSize，Farmer.caughtFish Prefix 同步收藏记录；100 级狗鱼（O)144）本安装数据 min15/max60 应显示 15×11=165 起、不再因脱杆缩到 30 多厘米（BATCH-038） |
| 万能/挑战鱼饵 | 万能鱼饵：难度等级 >0 且原生两条时 +10 条（2→12）再乘等级倍数，挑战鱼饵不叠加；挑战鱼饵（难度>100）：5 分钟内成功数量 ×1.5（ceil）再乘等级倍数，超时只取消数量加成（等级/皇冠/流动皇冠/品质/尺寸照常），原生 3 次脱杆失败禁用，无其他鱼助战（BATCH-038） |
| 流动金色皇冠 | 难度等级 ≥95 且挑战鱼饵生效时成功（不区分超时）→ 该鱼皇冠升级流动金色（Collections 页金/白呼吸 + 1±0.05 缩放脉冲）；独立存档字段 ChallengeCrowns，旧存档缺失默认空；测试命令 fish_challengecrown <鱼ID> [0|1]（BATCH-038） |
| 称号与史诗失败 | 100 级称号=太一（rank.taiyi，99 级仍创世神，混沌已删除）；满皇冠（α≥1）且史诗条件成立（同鱼种连续失败≥2 且调整后难度≥150）时失败提示=“人鱼合一”（hud.fail.union），未满皇冠仍为 hud.fail.epic（BATCH-038） |
| 高难度小游戏 | 非鱼王调整后难度 >100：初始目标=顶部、加速度线性增幅（等级 <98 按现增幅 10%、等级 ≥98 保持 100%，BATCH-034 覆盖 BATCH-029 阈值；2026-08-09 晚用户确认后 BATCH-036 临时统一改动已还原）；>150：换目标频率三处与 dart 偏移按 150 封顶；≥150：按难度分档 8/6/5/4/3 秒触发上下 25% 区域瞬移（0.5s 延迟、跳完才重新计时、1 秒检测）；d≤100 与鱼王保持原生不变（BATCH-028）；每次瞬移到上/下 25% 区域时在鱼旁显示贴鱼文案（上跳“鱼跃”类 15 条、下跳“甩尾”类 15 条随机，停留 1 秒后 0.5 秒淡出，BATCH-034） |
| UI 与展示 | BobberBar、HUD、Collections、NPC 文案、手持鱼、小游戏鱼标和结算鱼图只展示权威事实，不建立第二份难度或奖励状态；收藏页星标为金色皇冠（Infinity Crown 贴图，BATCH-029）；已有星标鱼开局显示一次挑战宣言；超出玩家钓鱼等级的小游戏显示等级建议，超过两倍时带“不可能”前缀；同鱼种连续失败 ≥2 次且调整后难度 ≥150 时失败提示为史诗文案（成功清零，BATCH-029），且史诗提示优先于触底、负等级和普通失败提示（BATCH-031）；本 Mod 全部提示 FIFO 排队显示（同屏同时一条、前一条消失后出队、上限 5 丢最旧、按玩家隔离、原生消息不参与，BATCH-032）；难度等级 <1（0 和负数）的胜利显示弱称号“额...稍微强一点的个体”提示，该弱称号不嵌入星标挑战宣言（BATCH-030）；难度等级<1（0和负数）的鱼即使有星标也不显示挑战宣言（BATCH-033）；NPC 冒泡/对话尺寸统一显示厘米（fishSize×2.54 取整），与手持尺寸一致（BATCH-033）；收藏页有皇冠的鱼描述追加“钓鱼控制力+1”（i18n collections.crownControl，鱼王豁免挑战等级但皇冠行照常，BATCH-036） |
| 手感系统 | 手感进度 α = 玩家已收集可计数皇冠数 ÷ 61（56 普通 + 5 原版传奇，Mod 鱼不计），0=原生手感、1=终点手感，全程线性分配（含撞边钳制）；终点手感：按下/松开瞬间直接定速、无加速度/条内阻尼/惯性、撞边完全钳制（不反弹、速度归零）；定速最大值随 α 线性减 30%（α=1 → 21px/s，30×(1-0.3α)），并叠加方向系数——绿条中间朝鱼中间移动 ×(1+0.2α)、相背离 ×(1-0.2α)（α=1 → ×1.2/×0.8，BATCH-038 用户确认）；帧率解耦覆盖整个小游戏（绿条、鱼、平滑、概率），60fps 行为与原生一致（BATCH-034）；绿条外钳制机制已整体删除（BATCH-034 覆盖 BATCH-033） |\n| 小游戏等级建议 | 非鱼王比较 `难度等级 / 10` 与当前有效整数钓鱼等级；超过当前等级显示建议，超过两倍在开头显示“不可能”提示；小数比较不能先向下取整 |
| 原生契约 | 当前安装游戏的 `BobberBar`、`FishingRod`、`Farmer.caughtFish`、`CollectionsPage` 和绘制入口保持真实签名、回调和生命周期边界 |
| 多人/屏幕 | 双人同屏与在线联机均按玩家 `UniqueMultiplayerID` 隔离：难度/星标写入各自 `Farmer.modData`（原生同步字段），展示按各自屏幕实例；每位本地玩家独立 `Game1` 实例；未执行的多人/同屏实测必须明确标记 |
| 性能 | 高频路径有明确上限、频率和失效条件；诊断日志低频且可关闭，不出现每帧刷屏或无界集合增长 |
| 卸载/重载 | Mod 卸载、重载、存档切换和标题返回不会残留静态缓存、事件注册或旧鱼获待处理状态 |
| 证据边界 | 静态代码证据、构建、部署、运行观察和真实存档验收分别记录，不能互相替代 |
| 独立判定 | 一次游戏可以覆盖多个批次，但每个玩家可见现象必须分别标记通过、失败或未执行 |

失败证据按批次决定，通常包括游戏时间、地点、鱼 ID/品质/数量、操作步骤、观察屏幕、截图和完整 SMAPI 日志。日志先封存，再判断根因。

