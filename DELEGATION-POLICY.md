# FishingExpanded 委派与执行器政策

本文件只在准备、复核或收尾外部模型委派时读取。历史上由 Claude Code/Opus 5 生成的项目材料不证明当前轮实际路由；只有当前 provider、stream 和 receipt 证据能证明实际模型。

## 主控和资格

- Codex 是本项目规划、根因裁决、治理状态和最终验证的唯一主控；执行器不会因被调用而取得主控权。
- 委派必须通过已加载且验证可用的 `codex-with-cc`。只有预计能减少主线程工作量时才委派。
- `opus5` 是有界委派的默认 profile；`deepseek-1m` 只在输入确实超过 Opus 有效上下文，或已有稳定 1M 缓存收益证据时承担机械取证。
- 主线程已经掌握足够源码、日志和调用链证据并完成根因/所有权裁决后，直接实施和验证，不重复委派同范围审计。

## 合同与证据

- 每次委派写明 Case、权威证据和精确入口、能在旧代码上失败的合同、停止条件、允许读写范围、禁止事项、验证命令和返回格式。
- 可写委派前记录允许范围的当前变更文件与验证基线；执行器未提供对应证据时不得把失败归为既有问题。
- 可委派限定搜索、日志提取、写入者枚举、已裁决方案的白名单实现、静态合同、构建和 diff 初审。
- 根因裁决、真实验收、治理规则修改、正式部署和外部状态修改不得委派。
- 任务名、TaskId、`-Model` 文本、sidecar 或旧聊天都不证明实际模型路由。缺少当前 provider receipt 时，路由和成本状态必须写为“未验证/等待 receipt”。
- 需要主动使用工具搜索源码、反编译证据或核对真实文件与行号的任务，必须使用具备工具能力的 Claude Code CLI researcher/reviewer；不能用 report-only 结果替代现场证据。
- 每次委派前只读确认 profile 的当前模型列表，并显式锁定主模型、默认模型和子代理模型；列表支持不等于本次调用实际路由。
- 实际路由只有在本地 v2 配置、Claude stream 模型证据和 provider receipt 三者一致时才可确认；任一缺失都只能报告“未验证/等待 receipt”。
- 不得把任务名、模型别名、sidecar 或本地 `-Model` 文本当作成本或供应商证明；成本归因必须单独列出无法计量的编排开销。
- 执行器未找到某项内容时只能报告“未找到”，不能推断为“不存在”；同一 Case 或文件同时只能有一个修改者。
- 可写实施委派前必须记录允许范围的当前 Git diff 与验证基线；缺少基线时不得把失败归因于既有问题。每次委派合同必须写明 Case、权威入口、停止条件、允许读写范围、禁止事项和验证命令。
- 同一 Case 或文件的委派结果始终只是候选成果；Codex 必须复核实际 diff、静态合同、构建和行为边界后，才能推进阶段或宣布完成。根因、所有权、协议、存档或生命周期仍不清楚时不得委派实现。
- 若 `.codex/codex_with_cc/cost-summary.local.json` 要求成本比较，收尾时必须报告费用、首次通过率、返工次数和耗时；缺少 receipt 时只能保留未验证状态，不能归因供应商或低价模型。

## 返工边界

- “未找到”只能报告未找到，不能推成不存在。
- 同一 Case 或同一文件同时只能有一个修改者。
- 执行器报告始终经过 Codex 的源码、diff、构建和行为契约复核后才能接受。
