# FishingExpanded 委派与执行器政策

本文件只在准备、复核或收尾外部模型委派时读取。历史上由 Claude Code/Opus 5 生成的项目材料不证明当前轮实际路由；只有当前 provider、stream 和 receipt 证据能证明实际模型。

## 主控和资格

- Codex 是本项目规划、根因裁决、治理状态和最终验证的唯一主控；执行器不会因被调用而取得主控权。
- 委派必须通过已加载且验证可用的 `codex-with-cc`。只有预计能减少主线程工作量时才委派。
- `opus5` 是有界委派的默认 profile；`deepseek-1m` 只在输入确实超过 Opus 有效上下文，或已有稳定 1M 缓存收益证据时承担机械取证。
- 主线程已经掌握足够源码、日志和调用链证据并完成根因/所有权裁决后，直接实施和验证，不重复委派同范围审计。

## 合同与证据

- 每次委派写明 Case、权威证据和精确入口、能在旧代码上失败的合同、停止条件、允许读写范围、禁止事项、验证命令和返回格式。
- 可写委派前记录允许范围的当前变更文件与验证基线；执行器未提供对应证据时不得把失败归为既有问题。
- 可委派限定搜索、日志提取、写入者枚举、已裁决方案的白名单实现、静态合同、构建和 diff 初审。
- 根因裁决、真实验收、治理规则修改、正式部署和外部状态修改不得委派。
- 任务名、TaskId、`-Model` 文本、sidecar 或旧聊天都不证明实际模型路由。缺少当前 provider receipt 时，路由和成本状态必须写为“未验证/等待 receipt”。

## 返工边界

- “未找到”只能报告未找到，不能推成不存在。
- 同一 Case 或同一文件同时只能有一个修改者。
- 执行器报告始终经过 Codex 的源码、diff、构建和行为契约复核后才能接受。
