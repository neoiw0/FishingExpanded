# FishingExpanded - Codex 开发契约

<!-- CODEX_EFFORT_WATCHDOG_V1 enabled default=high -->

本文件是 `D:\GGGGG\FishingExpanded` 下工作的唯一根规则。嵌套 `AGENTS.md` 只能补充目录特有约束，不得复制、削弱或改写本文件。用户当轮的明确指令优先于默认范围，但不能把构建、部署或静态证据冒充真实游戏验收。

## 读取边界

| 文件 | 唯一职责 | 读取时机 |
|---|---|---|
| `AGENTS.md` | 核心约束、停止条件和阶段规则 | 每次任务 |
| `CLAUDE.md` | FishingExpanded 专属增量规则和入口说明 | 每次任务后按需 |
| `GAME-DESIGN.md` | 用户确认的钓鱼机制 | 玩法含义不明确或准备修改玩法时 |
| `FISHING-BATCH-REPAIR-SOP.md` | 取证、R0/R1/R2、构建和部署边界 | 诊断或修改 Bug 时 |
| `FISHING-BATCH-TEMPLATE.md` | 活动根因批次表单 | 建立新批次时 |
| `BUG-LEDGER.md` | 当前 Case 索引、反证次数和门禁 | 开始或续接 Bug 工作时 |
| `MAINTENANCE-INDEX.md` | 维护入口、台账分层和当前接管门禁 | 接手项目或整理状态时 |
| `TESTING.md` | 稳定的长期回归矩阵 | 设计 R2 或检查回归覆盖时 |
| `TESTING-GUIDE.md` | Fishing 控制台命令和场景步骤 | 需要执行玩家测试时 |
| `DELEGATION-POLICY.md` | 委派、执行器复核和成本证据 | 准备或收尾委派时 |
| `LOCAL-ENVIRONMENT.md` | 当前电脑的有效路径、工具和命令 | 执行本机命令、构建或部署前 |
| `LOCAL-PITFALLS.md` | 仍可复现的本机工具踩坑 | 命令失败或遇到相似操作时 |
| `DECOMPILATION-RESEARCH-INDEX.md` | 已保存的原生契约和反编译证据索引 | 准备反编译或审计原生契约时 |

- `Governance` 保存 Fishing 专属批次和证据卡。先由 `BUG-LEDGER.md` 定位当前 Case，再读取所需卡片，不批量加载无关历史材料。
- 现有批次卡可能保留历史阶段文字；若批次卡与总账状态冲突，保留卡内证据不改写，当前状态以 `BUG-LEDGER.md` 和新建的活动卡为准。
- `_analysis` 只保存本项目当前已获取的反编译/IL证据；旧证据只能作线索，不能替代当前安装 DLL 的重新核对。
- `CURRENT-WORK-HANDOFF.md` 是项目交接材料；除非用户明确要求维护交接文件，否则只读、不追加时间线。
- 治理文件只记录当前有效规则和状态，不把旧聊天摘要当作当前事实。除本次用户明确授权的治理接管外，不得擅自修改治理规则。

## 核心开发原则

### 证据、停止与升级

1. **先证实第一处分歧。** 没有能区分主要竞争解释的证据时，只能增加低频、有界诊断，不能改变行为。尚未锁定时，活动卡和对话必须明确写“根因尚未证实”。
2. **完成调用链再进入 R0。** 接管 `BobberBar`、`FishingRod`、`Farmer.caughtFish`、`CollectionsPage`、NPC 对话或玩家手持绘制时，必须记录 Expanded 入口、原生上游调用与状态、提交方法、后续回调/生命周期以及第一处分歧。只搜索方法名、读取单个方法或从日志终点反推都不算完整调用链。
3. **核对当前安装契约。** 修改原生钓鱼小游戏、鱼获数量/品质、动画、图鉴或绘制前，读取当前 `D:\GGGGG\K1515` 安装中的真实游戏 DLL；旧版反编译和旧日志只作线索。
4. **未证实不等于暂停。** 只有进入“等待玩家日志”“等待真实验收”，或存在需用户授权的外部阻塞时才能停止；仍有不依赖用户的下一步就必须继续。
5. **持续累计反证。** 同一玩家现象的已部署修复被推翻次数跨会话、批次、版本和名称累计，不能通过重新分组清零。

| 已部署修复被推翻次数 | 强制处理 |
|---:|---|
| 1 | 重开原 Case，保存反证，增加低频诊断并重新定位第一处分歧 |
| 2 | 停止局部行为补丁，只做诊断取证、所有权审计和方案设计；登记后申请 `xhigh` |
| 3+ | 禁止继续补丁式修复；先给出旧/新行为链、失败尝试、写入者、删除计划和系统重构方案 |

### Fishing 所有权不变量

每个行为和持久状态只能有一个权威写入者；新路径生效前必须退休旧路径。以下是当前实现的边界，若源码或原生契约证明不同，先更新活动卡再修改代码：

| 状态/行为 | 当前权威边界 | 非权威端 |
|---|---|---|
| 鱼难度、成功/失败统计、收藏星标和技能加成 | `DifficultyManager` 与 `FishDifficultyData` 存档数据 | HUD、图鉴和命令只读取或请求其操作 |
| 钓鱼小游戏参数 | `BobberBarPatches` 在原生构造/更新边界调整 | 结果展示不得重新计算第二份难度 |
| 钓鱼动画阶段 | 原生 `FishingRod` 流程；`FishingRodPatches` 只记录待处理事实 | 不得通过动画数量伪造最终背包数量 |
| 最终鱼获数量与等级结算 | `Farmer.caughtFish` 的当前 Postfix 边界 | 其他 Patch 不得重复发放、重复升级或重放结算 |
| 巨型鱼展示事实 | `GiantFishManager` | `ObjectPatches`、HUD、NPC和图鉴只消费展示事实 |
| HUD、图鉴、手持鱼视觉和 NPC 文案 | 各自展示 Patch/Notifier | 不得反向写入难度、存档或世界奖励 |

- 修改已验收行为的所有者或相邻系统时，重新打开对应回归项并安排聚焦反向测试。
- 新增字典、队列、缓存、超时或消息协议前，先证明现有所有者不能承载；独立生命周期的状态不得继续堆入 `ModEntry.cs`。
- 纯 Bug 修复若运行时代码明显净增长，必须说明为何不能复用现有所有者；目标是减少写入者、状态和生命周期，而不是机械减少行数。

### 阶段、状态与变更边界

- 工作单位是根因批次。R0 消除第一处错误决策；R1 删除被替代的字段、方法、消息、缓存、注册和清理；R2 推演场景、性能、存档、多人和部署对应性。仍存在两个写入者或两套同状态机制时，不得进入 R2。
- 新 Case、根因/第一处分歧、R0/R1/R2、构建、部署或实测状态变化时，必须在同一阶段覆盖更新活动卡顶部和 `BUG-LEDGER.md`。长期回归维度变化才更新 `TESTING.md`；历史材料不回填当前状态。
- 对话同步必须用白话说明结论、已证实、未验证、玩家可见变化和下一步；需要实测时给出步骤、预期、禁止现象和失败证据。内部状态或对话同步缺失，都不能切换阶段、部署、等待用户或结束本轮。
- 连续两个已完成的有界动作都没有产出能缩小竞争解释的证据时，停止扩展当前假设，选择下一个最短区分动作或明确报告阻塞；不能用重复状态说明代替工作。
- 用户一次提交多个问题时，先做有界全量分诊；同一时间只允许一个活动根因批次。只有证据证明多个现象共享同一第一处分歧且属于同一权威所有权链时才能合并。
- 增加反证次数、重开 Case 或修改行为前，必须绑定新的实测/日志/截图、用户明确改变玩法，或当前源码与安装契约证实的未覆盖缺口。重复描述、提醒、计划和旧证据摘要不算新证据。
- 首次编辑前在活动卡顶部写明本轮准入证据、现有实现复核、允许修改的文件/符号和冻结 Case；只有当前唯一执行批次可以改变生产行为。
- 每个玩家现象独立记录通过、失败或未执行，不能用同批其他现象通过来代替。
- 构建、静态合同、部署、运行观察和真实存档验收是不同状态，不能互相替代。
- Fishing 使用本目录自己的 `.git` 基线；不得把兄弟 `codex working space` 的 Git 状态当成 Fishing 的提交、分支或基线。暂存前按明确路径核对实际 diff，禁止用 `git add -A` 吸收来源不明文件。
- 未经用户明确授权，不启动游戏、不修改受保护安装、不覆盖部署目录 `config.json`，也不执行正式部署。部署前保留目标文件备份并核对源/目标哈希。

## 主控与委派

- Codex 是本项目规划、根因裁决、架构/所有权判断、治理状态、Git边界、正式部署和最终验证的唯一主控；执行器结果始终只是候选成果。
- 只有预计能减少主线程工作量时才委派。委派前读取 `DELEGATION-POLICY.md` 和适用的 `codex-with-cc` 技能。
- `opus5` 是有界委派默认 profile；`deepseek-1m` 只用于确实超过 Opus 有效上下文或已证实能稳定复用 1M 缓存的机械取证。委派历史、任务名、侧车配置或 `-Model` 文本都不证明本次实际路由。
- 根因裁决、真实验收、治理规则修改、部署和外部状态修改不得转交；同一 Case 或文件同时只能有一个修改者。
- 没有 provider receipt、stream 模型证据和当前配置三者闭环时，不得宣称路由、成本或供应商模型已验证。

## 自动推理强度

主线程使用 `low / medium / high / xhigh`，默认 `high`，选择当前阶段的最小充分档位：

| 档位 | 适用范围 |
|---|---|
| `low` | 确定、短小的机械操作或有界只读诊断 |
| `medium` | 证据目标明确的取证、局部实施、构建、打包、哈希和部署核对 |
| `high` | 普通诊断、完整调用链、R0/R1/R2 和跨文件审查 |
| `xhigh` | 第二次实测推翻后的所有权审计，或高代价的存档、多人、原生契约和架构决策 |

档位只适用于触发它的 Case 和阶段。第一处分歧证实、阶段/Case 切换或中断后续接时，必须重新评估。高档位不能替代运行证据；已证实方案进入实施、静态验证、构建、打包或部署时应降档。

切换档位时先记录证据、未决问题和准确下一步，再在回复末尾使用对应 gate：

`[EFFORT_GATE] desired=low action=pause`

`[EFFORT_GATE] desired=medium action=pause`

`[EFFORT_GATE] desired=high action=pause`

`[EFFORT_GATE] desired=xhigh action=pause`

不得修改 JSONL、数据库或全局状态伪造档位切换。
