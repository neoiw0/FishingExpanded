# FishingExpanded - Codex 开发契约

<!-- CODEX_EFFORT_WATCHDOG_V1 enabled default=high -->

本文件是 `D:\GGGGG\FishingExpanded\` 下工作的唯一根规则。

## 读取边界

| 文件 | 唯一职责 | 读取时机 |
|---|---|---|
| `AGENTS.md` | 核心约束、停止条件与门禁 | 每次任务 |
| `GAME-DESIGN.md` | 用户确认的游戏机制 | 玩法含义不明确或准备修改玩法时 |
| `FISHING-BATCH-REPAIR-SOP.md` | 取证、R0/R1/R2、构建和部署边界 | 诊断或修改 Bug 时 |
| `FISHING-BATCH-TEMPLATE.md` | 活动根因批次表单 | 建立新批次时 |
| `BUG-LEDGER.md` | 当前 Case 索引、反证次数和用户门禁 | 开始或续接 Bug 工作时 |
| `TESTING.md` | 稳定的长期回归矩阵 | 设计 R2 或检查回归覆盖时 |
| `DELEGATION-POLICY.md` | 委派、执行器复核和成本决策 | 准备或收尾委派时 |
| `LOCAL-ENVIRONMENT.md` | 当前电脑的有效路径、工具和版本 | 执行本机命令、构建或部署前 |
| `LOCAL-PITFALLS.md` | 仍可复现的本机工具踩坑 | 命令失败或遇到相似操作时 |
| `DECOMPILATION-RESEARCH-INDEX.md` | 已证实且可复用的原生/基础契约 | 准备反编译或审计原生契约时 |
| `CLAUDE.md` | Claude Desktop 轻量独立工作入口 | 用户直接在 Claude Desktop 工作时 |
| `MAINTENANCE-INDEX.md` | 维护入口、台账分层和当前接管门禁 | 接手项目或整理状态时 |
| `TESTING-GUIDE.md` | Fishing 控制台命令和场景步骤 | 需要执行玩家测试时 |
| `CURRENT-WORK-HANDOFF.md` | 项目交接材料；默认只读 | 仅用户明确要求维护交接文件时 |

- `Governance/Active` 只保存：当前轮活动卡、已部署且仍有玩家门禁的冻结轮次卡、用户授权扩展的独立项目跟踪卡；其余一律移入 `Archive-ReadOnly`。先由 `BUG-LEDGER.md` 定位 Case，再读取其唯一活动卡，不批量加载其他卡。
- `Governance/Archive-ReadOnly` 和 Wiki 只作历史证据，默认不读取、不维护、不参与普通构建、部署或状态判断；仅在用户明确要求或当前证据指向具体档案时读取所需文件。
- 当前治理文件只覆盖当前事实，不追加历史时间线；普通续接使用 Git、Ledger 和活动卡。
- 没有用户明确授权时，不得修改治理规则。
- `_analysis` 只保存本项目当前已获取的反编译/IL 证据；旧证据只能作线索，不能替代当前安装 DLL 的重新核对。
- `AGENTS.md` 只保留原则、门禁与停止条件；流程步骤以 `FISHING-BATCH-REPAIR-SOP.md` 为准，活动卡字段与表格定义以 `FISHING-BATCH-TEMPLATE.md` 为准。

## 核心开发原则

### 证据、停止与升级

1. **先证实第一处分歧。** 没有能区分主要竞争解释的证据时，只能增加低频、有界诊断，不能改变行为。尚未锁定时，活动卡和聊天必须明确写“根因尚未证实”并按 SOP 继续诊断；只有满足下述停止条件时才能转入等待。进入 R0 前，活动卡必须完成竞争解释表（至少 2 个互斥假设及排除判据；完整定义与豁免规则见 `FISHING-BATCH-TEMPLATE.md`）。
2. **完成调用链再进入 R0。** 涉及基础 Mod、原生游戏、地图入口或逐屏实例时，活动卡必须记录 Expanded 接管入口、基础 Mod 上游调用与状态、原生提交方法、后续回调/同步/生命周期以及第一处分歧。只搜索方法名、读取单个方法或从日志终点反推不算完成；证据不完整时不得进入 R0、升版或部署。
3. **核对当前安装契约。** 接管原生行为前，读取当前安装 DLL 的真实实现；旧版反编译和旧日志只作线索。
4. **未证实不等于暂停。** 只有 Case 已进入“等待玩家日志”“等待真实验收”，或存在需用户授权的外部阻塞时才能停止；仍有不依赖用户的下一步就必须继续。
5. **持续累计反证。** 同一玩家现象的已部署修复被推翻次数跨会话、批次、版本和名称累计，不能通过重新分组清零。

| 已部署修复被推翻次数 | 强制处理 |
|---:|---|
| 1 | 重开原 Case，保存反证，增加低频诊断并重新定位第一处分歧（当轮完成，禁止仅冻结跨轮） |
| 2 | 停止局部行为补丁，只做诊断取证、所有权审计和方案设计；先过高级复核门禁再实施 |
| 3+ | 禁止继续补丁式修复；先给出旧/新行为链、失败尝试、写入者、删除计划和系统重构方案；在高级复核完成 |

高级复核=至少完成诊断取证、所有权审计和方案设计，绑定新证据并经主控复核；不自动切换模型或供应商。

### 修改与可观测性

- 未完成真实验收的运行时行为修改，在首次生产编辑前必须记录可观测性决定：复用现有诊断、新增低频诊断，或符合免除条件。
- 现有证据不能区分主要竞争解释时，不得部署没有对应诊断的候选。
- 诊断只记录第一处分歧所需的输入、唯一所有者、状态转换、结果和原因；必须绑定 Case/事务标识，并具有限频、上限和清理条件，禁止逐 Tick、逐格或逐节点输出。
- 纯文案、注释、机械重命名以及静态合同可完整证明且不改变运行行为的修改可以免除，但活动卡必须记录免除理由。
- 强制工件允许豁免，豁免必须写理由并给出依据，由门禁核验格式；运行时行为修改的竞争解释表不豁免。
- 真实证据闭环后，临时诊断必须删除、默认关闭，或经证明转为长期低频健康信号。

### 自动化验收

- 未完成真实验收的运行时行为修改，在进入 R2 前必须记录自动化验收决定：复用现有 SMAPI 指令、稳定 UI 路线、生产入口，或必要的薄控制入口。
- 决定必须写明场景、存档影响、触发、成功、失败和关闭条件；`存档影响=有` 时必须使用统一全量 `Saves` 沙箱。
- 只有不存在确定性现有入口时，才允许新增调用真实生产所有者的薄控制指令；不得复制行为、增加第二个状态所有者或写测试专用分支。
- Build/Deploy 门禁检查决定和可执行路线；Runtime 门禁必须检查实际结果、日志哈希、配置哈希、全量 `Saves` 恢复和目标进程清零。

### 所有权不变量

每个行为和持久状态只能有一个权威写入者；新路径生效前必须退休旧路径。

| 状态 | 权威所有者 | 非权威端 |
|---|---|---|
| 鱼难度、成功/失败统计、收藏星标和技能加成 | `DifficultyManager` 与 `FishDifficultyData` 存档数据 | HUD、图鉴和命令只读取或请求其操作 |
| 钓鱼小游戏参数 | `BobberBarPatches` 在原生构造/更新边界调整 | 结果展示不得重新计算第二份难度 |
| 钓鱼动画阶段 | 原生 `FishingRod` 流程；`FishingRodPatches` 只记录待处理事实 | 不得通过动画数量伪造最终背包数量 |
| 最终鱼获数量与等级结算 | `Farmer.caughtFish` 的当前 Postfix 边界 | 其他 Patch 不得重复发放、重复升级或重放结算 |
| 巨型鱼展示事实 | `GiantFishManager` | `ObjectPatches`、HUD、NPC 和图鉴只消费展示事实 |
| HUD、图鉴、手持鱼视觉和 NPC 文案 | 各自展示 Patch/Notifier | 不得反向写入难度、存档或世界奖励 |

- 修改已验收行为的所有者或相邻系统时，重新打开对应回归项并安排聚焦反向测试。
- 新增字典、队列、缓存、lease、超时或消息协议前，先证明现有所有者不能承载；有独立生命周期的系统不得继续堆入 `ModEntry.cs`。
- 纯 Bug 修复若运行时代码明显净增长，必须说明为何不能复用现有所有者；判断目标是减少写入者、状态和生命周期，而不是机械减少行数。

### 阶段、状态与 Git

- 外层工作单位是一个整合轮次，内层实施单位是按可能共享第一处分歧和唯一所有者粗分的问题类别。R0 消除第一处错误决策；R1 删除被替代的字段、方法、消息、缓存、注册和清理；R2 推演场景、性能、多人和部署对应性。仍存在两个写入者或两套同状态机制时，不得进入 R2。
- 完整影响矩阵（竞争解释表 + 多人/分屏影响矩阵）和生命周期检查写入对应活动卡，不在根规则中展开第二份明细。
- **内部状态同步。** 新 Case、根因/第一处分歧、R0/R1/R2、构建、部署或实测状态变化时，必须在同一阶段覆盖更新活动卡顶部和 `BUG-LEDGER.md`。`TESTING.md` 只在长期回归维度变化时更新；只读档案不回填当前状态。
- **对话同步。** 上述状态变化时，同时用白话说明证据、未决项、玩家可见变化和下一步；需要实测时直接给出步骤、预期、禁止现象和失败证据。内部或对话同步缺失都视为阶段未完成，不得切换 Case、部署、输出 effort gate、等待用户或结束本轮。
- 聊天状态更新必须先用完整白话说明“结论、已证实、还未验证、下一步”。阶段、Case、构建、部署或实测状态发生变化时发送一次即可；完成该同步后可以连续执行同一阶段的工具调用。没有新增证据、文件变化、命令结果或决策变化时，不得重复完整同步。连续两个已完成的有界动作都没有产出能缩小竞争解释的证据时，必须停止扩展当前假设，选择下一个最短区分动作或明确报告阻塞；长命令运行中的进度更新不计入该次数，不得以重复状态说明代替工作。Case ID、R0/R1/R2、构建名和门禁名只能在末尾括号补充，不能作为标题或句子主语。任何只含内部标签的状态更新都视为对话同步未完成；必须先改写并补发，之后才能切换阶段或 Case、构建、部署、等待或结束本轮。
- **多来源收集与粗分。** 每轮先统一收集玩家汇报、用户修改要求、源码分析发现和遗留未完成问题，再按可能共享的第一处分歧、唯一所有者和生命周期事务粗分为暂定类别，只获取足以确定类别边界的证据（步骤见 SOP §1.1）；不能因为同一页面、文件或用户消息合并，后续证据证明根因不同就拆分。
- **单类别实施与结束条件。** 同一时间只允许一个当前活动类别改变生产行为；其余类别排队或冻结。当前类别必须持续处理到代码侧结束后才能切换，代码侧结束只允许两条路径：一是根因已证实，行为修改、旧路径退休、R2/静态验证和测试设计完成；二是根因仍未证实，停止行为修改，低频有界侦测代码、日志判据、SMAPI 命令或稳定 UI 触发、清理条件均已完成。第二条路径仍是等待证据，不得写成 Bug 已修复或已验收。
- **统一候选。** 类别结束只冻结该类别源码，不单独生成发布 DLL 或部署。只有本轮所有纳入类别均为 `代码侧结束`、`当前活动类别=无`，整合候选状态才能进入 `待统一构建`；之后只进行一次统一 Release 构建、一次精确部署和按测试计划顺序执行的集中测试（流程见 SOP §7/§8）。测试结束必须关闭进程、冻结日志并逐现象记录通过、失败或未执行；新问题进入下一轮收集与粗分，不在当前测试会话中临时改码重部署。
- **证据准入与重复工作检查。** 增加反证次数、重开 Case 或修改行为前，必须绑定新的实测/日志/截图、用户明确改变玩法，或当前源码与安装契约证实的未覆盖缺口；重复描述、提醒、计划和旧证据摘要不算新证据。先搜索现有源码与静态合同；若已经覆盖且没有新证据，只能冻结并等待真实验收，不得重复修改或审计。
- **单类别修改锁。** 首次编辑前在活动卡顶部写明当前轮次、当前活动类别、本轮准入证据、现有实现复核、允许修改的文件/符号和冻结类别/Case；只有当前活动类别可以改变生产行为。静态检查可以逐类执行，但不得生成发布候选或借全仓结果给排队类别归因、改状态或宣称完成。
- 每个玩家现象独立记录证据与验收结果，不能用同批其他通过项代替。
- 静态契约验证行为不变量。生产修改与旧断言冲突时，先记录原生或调用链证据，再修改断言，不能只为通过构建改测试。
- 中断、压缩或工具异常后，先核对进程、HEAD、工作树、部分修改和最近构建/部署事实，再继续编辑。
- **压缩续接与指令防重放。** 压缩摘要、旧聊天计划和历史用户指令只作来源证据，不构成待执行队列。用户指令首次纳入活动卡后，只由当前轮次、类别队列、整合候选状态和 `唯一下一步` 代表尚未完成的工作；续接时只依据最新用户消息、`BUG-LEDGER.md` 指向的权威活动卡和当前 Git/进程/构建/部署事实，不得从旧聊天恢复动作。
- 活动卡和总账必须同步维护 `当前轮次`、`当前活动类别`、`整合候选状态`、`唯一下一步`、`已消费动作` 和 `重复执行授权`。类别处理中，`唯一下一步` 只描述当前类别的最短动作；所有类别代码侧结束后才可改为统一 Build、Deploy 或集中测试。`重复执行授权` 默认为 `无`。
- Build、Deploy 和 Runtime 是轮次级动作，不属于单个 Case。消费标识分别使用 `Build:<轮次>:<DLL SHA-256>`、`Deploy:<轮次>:<DLL SHA-256>` 和 `Runtime:<轮次>:<场景>:<DLL SHA-256>`。已经具有当前证据的轮次动作必须移入 `已消费动作`；重做前先把 `唯一下一步` 改为该重复动作，并把授权写成 `用户明确要求；原因=<具体原因>` 或 `自动授权重做；原因=<具体原因>；证据=<证据位置或哈希>`，之后才能执行。无人值守时允许自授权：只要重做附有具体、可核验的理由（例如已部署候选缺陷被日志/IL/契约证据证实、安装契约与源码不一致、新静态契约反证），可自行登记 `自动授权重做；原因=<具体原因>；证据=<证据位置或哈希>` 并继续，无需逐次等待用户批准；理由为空、证据缺失或只重复旧摘要的仍必须拒绝。涉及用户可见新行为或需要玩家实测确认的变更，仍以 `用户明确要求` 为优先。
- 外部状态所有者必须硬拒绝越级和重放：统一构建器只在所有纳入类别代码侧结束、当前活动类别为 `无`、整合候选状态为 `待统一构建` 时允许 Release Build；部署脚本只在状态为 `已统一构建` 时允许 Deploy；统一运行器只在状态为 `已部署待集中测试` 或 `集中测试中` 且活动卡 DLL 哈希与目标安装一致时允许 Run。只读 Preflight、Analyze 和 Recover 不因消费记录被禁止。
- R0/R1/R2 关闭门禁核对当前类别和对应 Case；Build/Deploy/Runtime 关闭门禁核对轮次、类别队列、整合候选状态、轮次级消费标识和重复授权。仍有未结束类别、当前活动类别不为 `无`、阶段倒退、相同候选重复操作或未同步最新用户授权时，门禁必须失败。
- 暂存或提交前核对基线和实际 diff，区分用户修改、既有成果和本轮修改；混合来源只能按明确路径暂存，禁止 `git add -A`。
- 无法安全拆分的同文件修改必须保留未暂存，并记录原因和拆分计划。
- 完成 R2、构建和部署核验后，必须决定建立逻辑检查点或记录不能安全提交的原因与拆分计划。检查点不代表真实游戏验收。
- Git 不可用时不得声称创建提交或分支，改用变更文件清单与哈希。

## 主控与委派

- Codex 是项目唯一主控，保留规划、根因/所有权/架构裁决、治理状态、Git、正式部署和最终验证；执行器结果始终只是候选成果。
- 除保留事项外，可委派任务默认委派；只有附对比证据（预计主线程耗时 vs 委派耗时）才能记录 `NotBeneficial`。委派前读取 `DELEGATION-POLICY.md` 和适用的 `codex-with-cc` 技能。
- 同一轮存在 ≥2 个独立可委派任务时，必须批量并行派发；主线程等待期间继续非冲突工作，禁止逐个阻塞等待。
- 每轮收集/粗分结束后，向用户报告本批可委派任务、已派发任务 ID 与未派发理由；缺失视为对话同步未完成。
- 第一处分歧证实、阶段或 Case 切换，以及中断/压缩后续接时，独立复核委派资格与 provider；根据工具需求、上下文规模、近期首次接受率、返工、耗时和本地价格选择，不用任务名或模型别名代替实际 profile 证据。
- 根因裁决、真实验收和外部状态修改不得转交；同一 Case 或文件同时只能有一个修改者。

## 自动推理强度

本系统已废除思考强度评估与切换（2026-08-14 采用对方治理原则）：主线程固定最高可用档，不输出 `EFFORT_GATE`/`EFFORT_RELEASE`，不存在 `xhigh` 申请或禁令。已部署修复被推翻的 2 档处理不再包含 `xhigh` 登记步骤：停止局部行为补丁，先完成诊断取证、所有权审计和方案设计（先过高级复核门禁），再实施；仍需绑定新证据并同步治理。
