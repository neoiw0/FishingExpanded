# FishingExpanded 根因批次 SOP

本文件只定义工作流程；核心规则、门禁与复核协议以根 `AGENTS.md` 为准（冲突时以 `AGENTS.md` 为准），活动卡字段与表格定义以 `FISHING-BATCH-TEMPLATE.md` 为准，游戏机制以 `GAME-DESIGN.md` 为准。`Governance\Active\*.md` 与 `Governance\Archive-ReadOnly\*.md` 保存 Fishing 专属证据，不被本 SOP 覆盖。

## 适用范围与轻量机制修改

- Bug 诊断与修复使用本文件的根因批次流程。
- 非 Bug 类的普通文案、概率、数值和局部游戏机制调整，不机械建立完整根因批次；先写入 `GAME-DESIGN.md`，确认唯一所有者、受影响入口和回归范围，再实现和验证。
- 只要改动涉及游戏原生逻辑、多人同步、逐屏实例或替换旧系统，仍必须执行完整调用链门禁以及 R0/R1/R2。
- 复核深度按实际风险选择，不因“玩法修改”或“Bug”标签机械升降。

## 治理文件维护触发

治理按事件更新，不按时间、轮次或“顺手整理”更新：

| 文件 | 唯一更新触发 | 不应更新的情况 |
|---|---|---|
| `AGENTS.md` | 用户明确授权治理修改 | 普通实现、构建、部署或收尾 |
| `DELEGATION-POLICY.md` | 用户改变委派策略、供应商边界或成本决策 | 没有委派的 Case |
| `GAME-DESIGN.md` | 用户确认新的长期玩法规则，或明确推翻旧规则；进入实现前更新 | 临时诊断、构建结果、源码方法名变化 |
| 本 SOP / 批次模板 | 流程、门禁或必填字段变化 | 单个 Case 的特殊情况 |
| `BUG-LEDGER.md` | Case 创建/重开、根因状态、R0/R1/R2、构建、部署、实测或累计次数变化 | 重复记录日志细节和实现过程 |
| 当前活动卡 | 新 Case/反证需要本地工作时创建；证据、第一处分歧、阶段或下一步变化时覆盖顶部状态 | 逐命令流水、已归档历史 |
| `TESTING.md` | 长期回归维度或跨 Case 不变量变化 | 候选哈希、当轮测试步骤、单次结果或历史时间线 |
| `LOCAL-ENVIRONMENT.md` | 路径、工具版本或可重复命令变化 | 每次构建、部署、Git 提交 |
| `LOCAL-PITFALLS.md` | 已证实且会复现的本机工具规律新增、变化或失效 | 单次错误、Case 根因、插件临时故障 |
| `DECOMPILATION-RESEARCH-INDEX.md` | 新的可复用原生/基础契约被证实，或依赖 DLL 版本/哈希变化使旧结论需复核 | Case 专用调用链和长反编译输出 |
| `CLAUDE.md` | 用户改变 Claude Desktop 的独立工作范围 | Codex 委派或普通源码变更 |
| `MAINTENANCE-INDEX.md` | 维护入口、台账分层和当前接管门禁 | 接手项目或整理状态时 |
| `TESTING-GUIDE.md` | Fishing 控制台命令和玩家场景步骤 | 需要执行玩家测试时 |
| `CURRENT-WORK-HANDOFF.md` | 交接背景材料；默认只读 | 仅用户明确要求维护交接文件时 |
| Wiki / `Archive-ReadOnly` | 仅用户明确要求 | 所有普通测试、修复和部署 |

归档 Case 返回新证据时不得修改旧档案：失败或仍需分析时，用模板新建一份简洁活动卡并链接所需档案；直接验收通过时只更新总账和当前测试结果。

## 0. 恢复与续接

中断或压缩后先读取根 `BUG-LEDGER.md` 和对应当前活动卡；同时执行根 `AGENTS.md` 的“压缩续接与指令防重放”规则，旧聊天和压缩摘要不得恢复为待执行动作。然后：

1. 核对游戏/SMAPI、构建或辅助进程是否仍在运行。
2. 获取 Git diff；Git 不可用则记录修改时间、变更文件和哈希。
3. 区分完整修改、部分修改、未执行计划和生成产物。
4. 运行最近一级可用的静态门禁或构建。
5. 把真实状态覆盖写回总账和当前活动卡，再继续编辑。

恢复检查前不得根据上一轮计划推断补丁、构建或部署已经完成。

## 1. 收集与现象卡

每个玩家可见现象拥有稳定 Case ID，记录：

- 用户原话、版本/DLL、存档、孩子、地点、时间和屏幕；
- 最短复现与预期；
- 已保存日志/截图/存档证据；
- 过去修复次数和失败方案。

按第一处分歧和所有者合并根因，不按拟采用的补丁合并。多个现象可共享根因，但不能共享验收结论。

### 1.1 问题轮次、粗分与类别队列

每轮先统一收集玩家汇报、用户修改要求、源码分析发现和遗留未完成问题，再建立有界粗分表，至少记录：

- Case 与玩家可见现象；
- 风险和可能的唯一所有者；
- 第一处分歧候选及能区分竞争解释的最短证据；
- 依赖关系和独立验收条件；
- 暂定类别、类别顺序、本轮纳入或下一轮处理的决定。

粗分阶段的目标是建立本轮类别队列，不是为所有问题完成完整调用链。粗分可以读取、搜索和做不改变行为的有界检查，但不得并行修改多个类别。完成粗分后，同一时间只允许一个当前活动类别；只有证据证明多个现象共享同一第一处分歧且属于同一唯一所有权链时，才允许放入同一类别。不可拆分的生命周期事务可以作为例外，但必须记录其不能独立提交或回滚的证据。即使合并，仍须为每个 Case 保留独立场景和验收结果。

未选中的问题必须登记到下一轮或明确排队，不得因为同一条用户消息、同一文件或同一功能页面而自动合并。新证据若改变根因判断，可以在进入生产编辑前重定义类别边界，并记录合并或拆分依据。

### 1.2 类别代码侧结束

当前类别必须持续处理到以下任一结束路径，之后才能激活下一个类别：

1. `根因修复`：第一处分歧和唯一所有者已证实，行为修改、旧路径退休、R2/静态验证完成，并写清侦测标签、SMAPI 命令或稳定 UI 入口、成功/失败和清理条件。
2. `诊断候选`：根因仍未证实，停止行为修复；只加入能区分竞争解释的低频有界侦测代码，并写清日志字段、限频、上限、SMAPI 命令或稳定 UI 入口、预期分支和移除条件。

两条路径都把类别标记为 `代码侧结束`。该状态只表示本轮无需继续改该类别源码，不表示 Bug 已修复、部署或真实验收。类别结束时冻结其允许范围；后续类别若必须改动同一所有者，先记录依赖并重新打开受影响类别。

### 1.3 准入与修改锁

重开 Case、增加累计反证或开始生产行为编辑前，活动卡必须记录四项：本轮准入证据、现有实现复核、允许修改范围、冻结 Case/禁止范围。准入证据只能是明确的新候选实测及其时间/日志/截图/哈希、用户明确改变玩法，或当前源码与安装契约共同证明的未覆盖缺口；重复描述、提醒、测试计划和旧证据摘要不得增加反证次数。

先搜索当前源码和静态合同并写明“已覆盖”或“未覆盖及第一处分歧”。已覆盖且没有新证据时，转为等待真实验收，不进入 R0，也不重复审计。未覆盖时，生产修改仅限活动卡声明的文件和符号；排队及冻结 Case 即使位于同一文件也不得顺手修改、重新归因或宣称完成。

## 2. 取证

生产行为修改和部署前必须完成根 `AGENTS.md` 的“修改与可观测性”门禁，并在活动卡记录决定。

运行时修改进入 R2、Build、Deploy 或 Runtime 收尾前，按根规则记录并执行自动化验收决定；具体路线复用统一运行器，不在本 SOP 复制场景实现。

根因卡必须区分：

1. 第一处错误决策或写入；
2. 决定性证据；
3. 仍成立与已排除的竞争解释；
4. 唯一所有者；
5. 最短的成功/失败区分动作。

证据不足时只增加有界、低频、不会改变行为的诊断。日志记录事务/路线/事件 ID、状态转换和原因，不逐帧、逐格或逐节点输出。

根因状态只允许以下三种，不得从候选直接跳到修复：

1. `未证实`：明确写“目前没有分析到根因”，列出竞争解释和能区分它们的最短日志。
2. `等待玩家日志`：只读诊断已构建/部署，记录诊断标签、触发窗口、最短复现、预期区分结果和日志位置；立即停止行为修改、升版和正式部署，等待玩家返回日志。
3. `已证实`：返回日志同时证明错误前状态、实际写入入口和写入后错误结果，并排除主要竞争解释；此时才允许进入设计/R0。根因卡必须填写竞争解释表（定义与豁免规则见 `FISHING-BATCH-TEMPLATE.md`）；无法区分时保持 `未证实`，只走诊断候选路径。

诊断日志不得修正、重载、排队或重新提交目标状态。玩家返回日志后先封存原文件与哈希，再更新根因状态；日志未命中时只能调整观测边界，不能把“没有记录到”写成“该路径不存在”。

怀疑存档损坏时，必须指出具体字段、非法值、写入来源和因果链；否则只登记为待排除假设。

## 3. 设计与原生契约

设计顺序：行为所有权 → 世界/物品事务 → 生命周期 → 逐屏展示与文案。

接管原生行为前记录当前安装 DLL 的版本、哈希、类型、方法及相关帧/方向/碰撞/Warp/mutex 契约。旧版 Expanded 用于比较玩家现象和调用边界，不直接复制旧架构。

每个新增运行时状态必须写明：

| 项目 | 内容 |
|---|---|
| 唯一所有者 | 谁创建、修改、提交 |
| 消费者 | 谁只读或展示 |
| 容量/频率 | 上限、调用频率、最坏对象量 |
| 失效 | 完成、超时、换日、地图变化等 |
| 生命周期 | SaveLoaded、DayStarted、Saving、ReturnedToTitle、分屏和远程断线 |

若需要第二套字典、超时、重试或消息协议处理同一状态，停止实现并重新审计所有权。

## 4. R0：首次完整实现

- 修改第一处错误决策，不在终点增加周期修补。
- 主机提交共享事实；本地/远程屏幕只投影展示。原生提交方法继续拥有鱼获和背包事务；HUD、图鉴、NPC 和绘制只投影权威事实。
- 事务失败保留来源状态，不猜目标、不半提交、不重放。
- 玩家文案进入翻译资源；代码只保存语义键和 token。
- 新的有状态子系统使用职责单一的服务；不得继续把独立生命周期堆进 `ModEntry.cs`。

R0 可以在编辑过程中短暂保留旧路径，但必须明确列入 R1 删除清单。

## 5. R1：删除优先结构审计

搜索所有调用者、Harmony 顺序和写入者，逐项填写：

| 类别 | 必须证明 |
|---|---|
| 新权威入口 | 唯一可提交路径 |
| 旧入口 | 已删除或被稳定边界完整截断 |
| 旧状态 | 字段、字典、队列、lease、消息、超时和缓存已退休 |
| 生命周期 | 旧注册和清理也已删除 |
| 兼容代码 | 实际调用者、支持版本、退出条件 |
| 复杂度 | 运行时代码增删及净增长理由 |

不可达代码不能以“以后可能有用”为由保留。R1 后仍存在双写入者、双状态机或两套展示协议时，不得进入 R2。

## 6. R2：场景、多人、性能与验证脚本

至少推演本次改动相关的边界：

- 主机单人、本地主屏、本地副屏、远程请求者和远程观察者；
- 四人混合在线、两对真人婚姻、玩家/NPC 婚姻；
- 父母/孩子不在同图、只有副屏或远程玩家观察；
- 换日、保存重载、返回标题、断线重连；
- 菜单/容器占用、动态角色阻挡、来源状态变化；
- 每 tick/高频路径的对象上限、缓存 TTL 和失效条件。
- 普通鱼、非鱼类、五只鱼王、负/零/正等级和原生特殊鱼饵；
- 满背包 `ItemGrabMenu`、已有同类堆叠、数量转换和动画数量；
- 逐屏表现热路径必须与当前屏幕可见目标数量线性相关；不得逐帧创建绘制适配器、重载或复制纹理、执行反射查找、全量扫描世界角色，或为离屏目标提交绘制。共享资源必须缓存，并记录明确的失效条件。

多人推演必须落到活动卡 RC 根因卡的“多人/分屏影响矩阵”；运行器无联机/分屏能力时，该矩阵就是多人验证的替代证据。静态审计至少覆盖已知陷阱：`Game1.ticks` 逐屏实例静态、`Context.IsMainPlayer` 主屏限制、进程级静态字典、NetField/NetBool 订阅、消息广播接收者、客户端本地写入。

R2 门禁核验竞争解释表与多人/分屏影响矩阵非空；Fishing 无统一 closeout 脚本时，在活动卡逐项记录同等检查（治理同步、Git 基线、聚焦 diff、委派评估、构建结果和部署哈希）；不满足时不得宣布 R2 完成。

静态脚本应验证行为不变量。若旧断言固化了错误实现，先在活动批次记录原生或调用链证据，再分别修改生产代码与测试。

R2 后冻结当前类别实现并标记代码侧结束；只有本轮全部纳入类别代码侧结束后才准备统一候选和集中测试。新证据推翻根因时回到取证，不继续第三层补丁。

R2、构建与部署收尾前复核 Git 基线：记录 HEAD、相关路径状态、聚焦 diff 和验证结果；只允许将来源明确且已完成当前阶段验证的修改纳入逻辑检查点。未接受的执行器候选、来源不明修改和其他活动 Case 不得混入；无法安全拆分提交时记录原因和拆分计划。检查点只证明已核验变更的版本边界，不代表真实游戏验收通过。

阶段开始、切换、续接时按根 `AGENTS.md` 的委派状态处理：委派禁止期间跳过评估，门禁固定记录 `Prohibited`（理由=委派禁止）；解除禁止后恢复评估（先列出本阶段可委派任务，清单为空才允许直接 `NotBeneficial`；≥2 个独立可委派任务时批量并行派发，主线程等待期间继续非冲突工作），并在该阶段最终门禁中记录最新决定。

R0、R1、R2、统一构建或部署阶段结束前必须运行项目可用的 closeout/static gate；Fishing 尚未提供统一脚本时，在活动卡中逐项记录同等检查（治理同步、Git 基线、聚焦 diff、委派评估、构建结果和部署哈希）。R0/R1/R2 门禁属于当前类别；Build/Deploy/Runtime 门禁属于当前轮次并核对全部类别已代码侧结束。门禁审计 Git 范围、治理同步、检查点决定、委派评估决定及部署哈希；每次调用记录 `Delegated`、`NotBeneficial` 或 `Prohibited` 及具体理由（委派禁止期间固定 `Prohibited`/委派禁止，见前一条），`NotBeneficial` 必须附对比证据，本阶段声明存在可委派任务但未记录 `Delegated` 时门禁失败，实际委派还必须记录任务 ID。门禁不自动暂存、提交或修改治理文件；退出码非零时不得宣布阶段完成、切换类别、结束任务或输出 effort gate。

活动卡顶部保存当前轮次、当前活动类别、整合候选状态、类别队列、证据链接和唯一下一步，不追加版本时间线。只有当前活动类别可以进入 R0/R1/R2 和改变生产行为；代码侧结束的类别冻结等待统一候选。Case 关闭、被新卡取代，或旧卡不再代表当前轮次时，在把有效状态提取到总账/测试路线后，将旧卡移入 `Governance/Archive-ReadOnly`。

## 7. 构建与部署

1. 确认本轮所有纳入类别均为 `代码侧结束`、当前活动类别为 `无`，把整合候选状态改为 `待统一构建`。
2. 使用 `LOCAL-ENVIRONMENT.md` 的当前项目和命令完成一次 Release Rebuild；不得为单个类别生成发布候选（Fishing 无统一构建脚本时，以本机构建命令、0 警告 0 错误和 DLL 哈希核对作为同等检查）。
3. 核对版本字段、manifest、翻译、正式输出、旧路径搜索和运行契约，把整合候选状态改为 `已统一构建` 并记录轮次级 Build 标识。
4. 部署前核对当前轮次、唯一下一步、目标进程和文件锁。FishingExpanded 安装目录具有用户长期部署授权（2026-08-14 起，与对方治理一致）；通过统一构建和事务预检后自动继续，其他目标仍须精确授权，并保持部署目录 `config.json` 不变。
5. 一轮只部署一次整合候选；只允许 DLL、PDB、manifest 和实际变化的 i18n，禁止 Wiki、README、日志、测试文件和 `.deps.json`。
6. 核对源/目标哈希，把状态改为 `已部署待集中测试` 并记录轮次级 Deploy 标识；重新执行 Git 基线与检查点复核，绝不整体暂存。

操作步骤成功只表示该步骤完成；玩家验收结果分别覆盖更新对应活动卡与 `BUG-LEDGER.md`，不写入长期回归矩阵。
## 8. 对话检查点与集中测试

测试按游戏内时间、地点和操作编排，不按源码文件编排。活动卡在统一构建前为每个类别写清侦测标签、SMAPI 命令或稳定 UI 入口，并形成有顺序的集中测试计划。部署后按顺序触发，完成后统一退出游戏、确认进程清零、冻结并哈希日志，再逐现象标记通过、失败或未执行。日志产生的新问题进入下一轮收集和粗分，不在当前测试会话中修改后重部署。

判断形成时逐步完成以下聊天检查点，不等全部结束后一次性回顾：

| 阶段 | 必须同步给用户 |
|---|---|
| 新 Case | 现象如何拆分，哪些仍只是猜测 |
| 取证开始 | 正在区分哪些竞争解释 |
| 取证进展 | 新证据证明、排除了什么，还缺什么 |
| 根因证实 | 决定性证据、因果链、第一处分歧和唯一所有者 |
| 进入 R0 | 准备修改的入口，以及玩家之后应看到的变化 |
| 完成 R1 | 退休的旧路径，以及为何不再双写 |
| 完成 R2 | 已覆盖边界和仍需真实游戏验证的项目 |
| 构建 | 构建证明什么、不能证明什么 |
| 部署 | 实际部署内容、玩家可见变化和未验收项目 |
| 等待玩家 | 完整步骤、预期、禁止现象和失败证据 |
| 玩家返回 | 每个现象分别标记通过、失败或未执行 |

使用简单中文；技术名称放在括号中补充。不得把搜索命令、原始构建输出或文件填写流水整段倾倒到聊天，也不得要求用户打开治理文件才能知道下一步。

用户离开后可继续取证、R0/R1/R2、构建和准备测试；除非当轮授权，不自行启动游戏或扩大到其他安装目录。

进入 `等待玩家日志` 后是例外：除保存证据、准备日志读取命令和回答玩家问题外，不继续修改该 Case；其他独立 Case 只有在不会改变同一写入者、日志窗口或部署 DLL 时才能继续。
