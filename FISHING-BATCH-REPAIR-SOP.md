# FishingExpanded 根因批次 SOP

本文件只定义工作流程；核心规则与档位协议以根 `AGENTS.md` 为准，玩法机制以 `GAME-DESIGN.md` 为准。`Governance\*.md` 保存 Fishing 专属证据，不被本 SOP 覆盖。

## 适用范围与轻量机制修改

- Bug 诊断与修复使用本文件的根因批次流程。
- 非 Bug 的普通文案、概率、数值或局部玩法调整，不机械建立完整批次；先在 `GAME-DESIGN.md` 确认唯一所有者、入口和回归范围。
- 只要改动涉及 Stardew Valley 原生逻辑、多人同步、逐屏实例、鱼获提交或替换旧系统，仍必须执行完整调用链门禁以及 R0/R1/R2。
- 思考强度按实际风险选择，不因“玩法修改”或“Bug”标签机械升降。

## 治理文件维护触发

治理按事件更新，不按时间、轮次或“顺手整理”更新：

| 文件 | 唯一更新触发 | 不应更新的情况 |
|---|---|---|
| `AGENTS.md` | 用户明确授权治理修改 | 普通实现、构建、部署或收尾 |
| `DELEGATION-POLICY.md` | 用户改变委派策略、供应商边界或成本决策 | 没有委派的 Case |
| `GAME-DESIGN.md` | 用户确认新的长期玩法规则，或明确推翻旧规则；进入实现前更新 | 临时诊断、构建结果、源码方法名变化 |
| 本 SOP / 批次模板 | 流程、门禁或必填字段变化 | 单个 Case 的特殊情况 |
| `BUG-LEDGER.md` | Case 创建/重开、根因状态、R0/R1/R2、构建、部署、实测或累计次数变化 | 重复记录日志细节和实现过程 |
| 当前活动卡 | 新 Case/反证需要本地工作时创建；证据、第一处分歧、阶段或下一步变化时覆盖顶部 | 逐命令流水、已归档历史 |
| `TESTING.md` | 长期回归维度或跨 Case 不变量变化 | 候选哈希、当轮测试步骤、单次结果或历史时间线 |
| `LOCAL-ENVIRONMENT.md` | 路径、工具版本或可重复命令变化 | 每次构建、部署、Git 提交 |
| `LOCAL-PITFALLS.md` | 已证实且会复现的本机工具规律新增、变化或失效 | 单次错误、Case 根因、临时故障 |
| `DECOMPILATION-RESEARCH-INDEX.md` | 新的可复用原生契约被证实，或依赖 DLL 版本/哈希变化使旧结论需复核 | Case 专用调用链和长反编译输出 |
| `CLAUDE.md` | 用户改变 Claude Desktop 的独立工作范围 | Codex 委派或普通源码变更 |
| Wiki / `Archive-ReadOnly` | 仅用户明确要求 | 所有普通测试、修复和部署 |

归档 Case 返回新证据时不得修改旧档案：失败或仍需分析时，用模板新建当前活动卡并链接所需档案；直接验收通过时只更新总账和当前测试结果。

## 0. 恢复与续接

中断或压缩后先读取根 `BUG-LEDGER.md` 和对应当前活动卡；只有用户明确要求过交接时才读取 `CURRENT-WORK-HANDOFF.md`。然后：

1. 核对游戏、SMAPI、构建和辅助进程是否仍在运行。
2. 获取 Git diff；Git 不可用则记录修改时间、变更文件和哈希。
3. 区分完整修改、部分修改、未执行计划和生成产物。
4. 运行最近一级可用的静态门禁或构建。
5. 把真实状态覆盖写回总账和当前活动卡，再继续编辑。

恢复检查前不得根据上一轮计划推断补丁、构建或部署已经完成。

## 1. 收集与现象卡

每个玩家可见现象拥有稳定 Case ID，记录用户原话、版本/DLL、存档、游戏位置和时间、最短复现与预期、已保存日志/截图/存档证据、过去修复次数和失败方案。

多个现象按第一处分歧和所有者合并根因，不按拟采用的补丁合并。多个现象可以共享根因，但不能共享验收结论。

### 1.1 多问题分诊与活动批次

用户一次提交多个问题时，先建立有界分诊表，至少记录：

- Case 与玩家可见现象；
- 风险和可能的唯一所有者；
- 第一处分歧候选及能区分竞争解释的最短证据；
- 依赖关系和独立验收条件；
- 当前批次、排队、等待玩家日志或暂不纳入的决定。

分诊阶段只确定批次边界，不同时修改多个问题的生产行为。完成分诊后，同一时间只允许一个活动根因批次；只有证据证明多个现象共享同一第一处分歧且属于同一唯一所有权链时才允许合并。不可拆分的生命周期事务必须记录其不能独立提交或回滚的证据。即使合并，也要为每个 Case 保留独立的 R2 场景和验收结果。

未选中的问题必须登记排队，不得因为同一条用户消息、同一文件或同一页面而自动合并；新证据证明共享根因时，活动卡必须记录合并或拆分依据。

### 1.2 准入与修改锁

重开 Case、增加累计反证或开始生产行为编辑前，活动卡必须记录本轮准入证据、现有实现复核、允许修改范围和冻结 Case/禁止范围。准入证据只能是明确的新实测及其时间/日志/截图/哈希、用户明确改变玩法，或当前源码与安装契约共同证明的未覆盖缺口。

先搜索当前源码和静态合同并写明“已覆盖”或“未覆盖及第一处分歧”。已覆盖且没有新证据时，转为等待真实验收，不进入 R0，也不重复审计。未覆盖时，生产修改仅限活动卡声明的文件和符号；排队及冻结 Case 即使位于同一文件也不得顺手修改、重新归因或宣称完成。

## 2. 取证

生产行为修改和部署前必须完成根 `AGENTS.md` 的修改与可观测性门禁，并在活动卡记录决定。运行时修改进入 R2、构建、部署或 Runtime 收尾前，按根规则记录自动化验收决定。

根因卡必须区分：

1. 第一处错误决策或写入；
2. 决定性证据；
3. 仍成立与已排除的竞争解释；
4. 唯一所有者；
5. 最短的成功/失败区分动作。

证据不足时只增加有界、低频、不会改变行为的诊断。日志记录 Case/事务标识、状态转换和原因，不逐 Tick、逐格或逐节点输出。诊断日志不得修正、重载、排队或重新提交目标状态；没有命中时只能调整观测边界，不能把“没有记录到”写成“该路径不存在”。

根因状态只允许：`未证实`、`等待玩家日志`、`已证实`。没有排除主要竞争解释时，不得从候选直接进入修复。

## 3. 设计与原生契约

设计顺序为：行为所有权 → 鱼获/背包事务 → 生命周期 → 逐屏展示与文案。

接管原生行为前记录当前安装 DLL 的版本、哈希、类型、方法、参数/字段、状态更新顺序、成功/失败回调、鱼获数量/品质提交时机和显示生命周期。旧版 Expanded 只作比较线索，不直接复制旧架构。

对 `BobberBar`、`FishingRod`、`Farmer.caughtFish`、`CollectionsPage`、NPC 对话和手持鱼绘制，活动卡必须写明 Expanded 入口、原生上游调用、原生提交方法、后续回调/同步、Harmony 拦截点和第一处分歧。

每个新增运行时状态必须写明：

| 项目 | 内容 |
|---|---|
| 唯一所有者 | 谁创建、修改、提交 |
| 消费者 | 谁只读或展示 |
| 容量/频率 | 上限、调用频率、最坏对象量 |
| 失效 | 完成、超时、换日、地图变化等 |
| 生命周期 | SaveLoaded、DayStarted、Saving、ReturnedToTitle、分屏和远程断线 |

若需要第二套字典、超时、重试或消息协议处理同一状态，停止实现并重新审计所有权。

## 4. R0：首次完整实现

- 修改第一处错误决策，不在终点增加周期性补丁。
- 原生提交方法继续拥有鱼获和背包事务；HUD、图鉴、NPC 和绘制只投影权威事实。
- 事务失败保留来源状态，不猜目标、不半提交、不重放。
- 玩家文案进入翻译资源；代码只保存语义键和 token。
- 新的有状态子系统使用职责单一的服务，不把独立生命周期继续堆入 `ModEntry.cs`。

R0 可以在编辑过程中短暂保留旧路径，但必须明确列入 R1 删除清单。

## 5. R1：删除优先结构审计

搜索所有调用者、Harmony 顺序和写入者，逐项填写：

| 类别 | 必须证明 |
|---|---|
| 新权威入口 | 唯一可提交路径 |
| 旧入口 | 已删除或被稳定边界完整截断 |
| 旧状态 | 字段、字典、队列、消息、超时和缓存已退休 |
| 生命周期 | 旧注册和清理也已删除 |
| 兼容代码 | 实际调用者、支持版本、退出条件 |
| 复杂度 | 运行时代码增删及净增长理由 |

重点检查鱼获动画数量、最终背包堆叠、难度结算、图鉴文字、HUD 和手持绘制是否被重复写入。R1 后仍存在双写入者、双状态机或两套展示协议时，不得进入 R2。

## 6. R2：场景、多人、性能与验证

至少推演本次改动相关的边界：

- 主机单人、本地主屏、本地副屏、远程请求者和远程观察者；
- 四人混合在线、换日、保存重载、返回标题和断线重连；
- 普通鱼、非鱼类、五只鱼王、负/零/正等级和原生特殊鱼饵；
- 满背包 `ItemGrabMenu`、已有同类堆叠、数量转换和动画数量；
- 每 Tick/高频路径的对象上限、缓存失效条件和日志频率；
- 已验收相邻回归和部署文件/配置哈希。

逐屏热路径不得逐帧执行反射查找、全量扫描世界、复制纹理或提交离屏目标；共享资源必须缓存并记录失效条件。静态脚本只验证行为不变量，不能代替真实存档验收。

R2、构建和部署收尾前复核 Git HEAD、相关路径状态、聚焦 diff、治理同步、委派评估、构建结果和部署哈希。只允许来源明确且已完成当前阶段验证的修改进入逻辑检查点；检查点不代表真实游戏验收。若项目没有统一 closeout 脚本，在活动卡中逐项记录同等检查，不能把“没有脚本”当作完成证据。

## 7. 构建与部署

1. 使用 `LOCAL-ENVIRONMENT.md` 的当前项目和命令完成 Release Rebuild。
2. 核对版本字段、manifest、翻译和正式输出一致。
3. 检查新增错误、警告变化、旧路径搜索和运行契约。
4. 部署前核对当前任务指定目标、进程、文件锁以及配置文件保护边界。
5. 只替换本批次明确文件并保留备份；核对源/目标哈希。
6. 更新当前活动卡和 `BUG-LEDGER.md`；当轮玩家步骤直接发在聊天中，不写入长期回归矩阵。
7. 再次核对 Git 基线和逻辑检查点，禁止整体暂存。

操作步骤成功只表示该步骤完成；玩家验收结果分别覆盖更新对应活动卡与 `BUG-LEDGER.md`，不写入长期回归矩阵。

## 8. 对话检查点与集中测试

测试按游戏内时间、地点和操作编排，不按源码文件编排。具体 Case 的步骤、预期、禁止现象和失败证据直接发送到聊天；活动卡保存 R2 合同，`BUG-LEDGER.md` 保存当前状态。每个现象独立标记通过、失败或未执行。

判断形成时逐步同步：新 Case 的拆分、取证排除项、根因因果链、进入 R0 的玩家可见变化、R1 退休的旧路径、R2 已覆盖边界、构建/部署能证明和不能证明的内容，以及等待玩家时的完整测试步骤。

用户离开后可继续取证、R0/R1/R2、构建和准备测试；除非当轮授权，不自行启动游戏或扩大到其他安装目录。

进入 `等待玩家日志` 后，除保存证据、准备日志读取命令和回答玩家问题外，不继续修改该 Case；其他独立 Case 只有在不会改变同一写入者、日志窗口或部署 DLL 时才能继续。
