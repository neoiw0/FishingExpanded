# FishingExpanded 根因批次 SOP

本文件把 Codex 工作区的证据优先、R0/R1/R2 和状态门禁应用到 FishingExpanded。现有 `Governance\*.md` 是项目专属批次证据，不能被本 SOP 覆盖。

## 1. 批次准入

开始修改前，在 `BUG-LEDGER.md` 和对应活动卡记录：玩家可见现象、累计反证次数、当前版本/安装路径、现有实现复核、竞争解释、第一处分歧候选、允许修改的文件/符号、冻结的其他 Case 和验收条件。

多个现象先做一次有界分诊；只有共享同一第一处分歧和同一权威所有权链时才合并。否则登记排队，同一时间只实施一个活动批次。

## 2. R0 - 证实错误决策

- 读取当前源码、当前安装 DLL、相关反编译证据和现有 SMAPI 日志。
- 对 `BobberBar`、`FishingRod`、`Farmer.caughtFish`、`CollectionsPage`、NPC 和绘制问题记录完整入口、参数/字段、状态更新、回调、生命周期和第一处分歧。
- 证据不足时只加低频、有界诊断，不改变生产行为；活动卡明确写“根因尚未证实”。
- R0 结束条件是能排除主要竞争解释，并能说明为什么旧路径会产生玩家现象。

## 3. R1 - 消除旧路径

- 先写删除清单和唯一所有者，再编辑代码。
- 删除被替代的字段、方法、缓存、事件注册、消息和清理路径；新路径生效前不得保留第二个写入者。
- 重点检查鱼获动画数量、最终背包堆叠、难度结算和 HUD/图鉴展示是否被重复写入。
- 修改后立即查看前后文、编译合同、Harmony patch 目标和日志频率。

## 4. R2 - 场景与交付核验

R2 必须覆盖适用的成功/失败边界、负/零/正等级、传奇鱼/非鱼类、存档重载、日切、多人/分屏、性能和部署对应性。每个玩家现象独立标记通过、失败或未执行。

构建、打包和哈希只证明静态/文件状态；部署只证明文件替换；真实存档测试必须由用户或明确授权的本机游戏流程完成。未经授权不启动游戏、不部署、不改 `config.json`。

## 5. 状态同步

每次根因、阶段、构建、部署或实测状态变化时，同步活动卡顶部、`BUG-LEDGER.md` 和对话说明。没有新证据时不重复刷新状态，也不把旧交接文件当作当前状态。
