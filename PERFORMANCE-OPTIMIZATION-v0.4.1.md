# 性能优化报告 - v0.4.1

**优化目标**: 支持低端机器,减少每帧计算开销

## 识别的性能热点

### 热点1: CollectionsPagePatches.draw_Postfix (严重)
- **当前行为**: 收藏页面打开时,每帧遍历当前页所有物品(约20个),每个进行字符串分割、字典查找、SpriteBatch.Draw
- **频率**: 60次/秒(收藏页面打开时)
- **性能影响**: 每帧20次字符串操作 + 20次字典查询 + N次Draw调用
- **优化方案**: 
  1. 缓存星标鱼列表(仅在页面切换时重建)
  2. 预计算绘制位置
  3. 添加dirty标记,仅在数据变化时重绘

### 热点2: FarmerPatches.GetItemDrawScale (中等)
- **当前行为**: 每次Farmer.draw()调用时检查当前物品、类型判断、字典查找
- **频率**: 60次/秒 × 可见玩家数量
- **性能影响**: 每帧多次类型检查、字符串比较、字典查找
- **优化方案**:
  1. 缓存上一次的(fishId, scale)结果
  2. 仅在CurrentItem变化时重新计算
  3. 提前退出条件判断

### 热点3: GiantFishManager.CheckAndTriggerNPCReactions (中等)
- **当前行为**: 每30帧进行LINQ查询遍历所有NPC,计算距离
- **频率**: 2次/秒
- **性能影响**: LINQ分配、浮点距离计算(可能数十个NPC)
- **优化方案**:
  1. 增加间隔到60帧(1秒)
  2. 使用平方距离避免开方
  3. 直接遍历替代LINQ
  4. 添加开关:仅在持有超大鱼时启用

### 热点4: BobberBarPatches Update钩子 (轻微)
- **当前行为**: 钓鱼时每帧字典查找实例数据
- **频率**: 60次/秒(仅钓鱼时)
- **性能影响**: 每帧Dictionary.TryGetValue
- **优化方案**: 字典查找已经是O(1),但可添加快速路径判断

### 热点5: ModEntry.OnUpdateTicked (轻微)
- **当前行为**: 每帧进行模运算检查
- **频率**: 60次/秒
- **性能影响**: 两次整数模运算
- **优化方案**: 使用计数器替代模运算

## 优化实施优先级

1. **P0 (必须)**: CollectionsPagePatches缓存 - 最严重热点
2. **P0 (必须)**: FarmerPatches结果缓存 - 高频调用
3. **P1 (重要)**: GiantFishManager条件启用 - 减少无效检查
4. **P2 (可选)**: 其他微优化

## 预期性能提升

- **低端机器(30-40fps)**: 预计提升5-10fps(收藏页面)
- **中端机器(60fps)**: 稳定性提升,减少掉帧
- **多人模式**: 减少网络同步压力
